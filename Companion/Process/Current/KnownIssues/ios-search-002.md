# IOS-SEARCH-002

> Routed from `KNOWN_ISSUES.md` line 313 during the 2026-08-09 hierarchy split. The exact pre-split source is hash-pinned in [`known-issues-pre-hierarchy-2026-08-09.txt`](../../History/KnownIssues/known-issues-pre-hierarchy-2026-08-09.txt) (`SHA-256 513497704ad37e977e2fb86e4623e956e6f1ca99844122948ff74995dfa9a309`).

- Register classification: `attribution-first`
- Original row SHA-256: `9ef557ad1e2768a70d1057c986105f2314e8fe94428268f5a9541f8efbeba659`

## Status

Surfaced by the final train (Claude reviewer); **class A**; ✅ **FIXED by `635cb78b1`**

## Subsystem and search terms

FTS; `MessageHeaderRekey.apply`; collision leg; `SearchIndex.rekeyHeaders`; `appliedRekeys`; `ftsRekeys`; `staleIds`; indexed-but-unfindable; `messageUserLabel`; `MessageBody`

## Full detail

**On the drain side only, the re-key's collision leg leaves an FTS entry pointing at a deleted header.** `MessageHeaderRekey.apply` deletes the old header and its body **before** the collision guard `guard try MessageHeader.fetchOne(db, key: newId) == nil`, then returns `false`. ⚠️ **"The sync caller compensates" — CORRECTED 2026-08-07 (R16-8); see the block at the end of this row.** *One* sync-side block compensates (the UID-remap block: the id never enters `ftsRekeys` and the stale row rides `staleIds`); **two others did not.** The **drain** caller does not either: `executeSingleOp` calls `SearchIndex.rekeyHeaders` only for `appliedRekeys`, which excludes the collision, so the old id's FTS entry survives with no header behind it — the *indexed but unfindable* class. The carried `messageUserLabel` rows and the body are dropped rather than merged onto the surviving destination row. **Reachability is narrow:** it needs a local row already sitting at the `COPYUID`-proven destination address at drain time. **Why REGISTRABLE:** recoverable — an orphan sweep clears the ghost and bodies re-fetch; no message is destroyed and no mutation lands on the wrong message.

✅ **FIXED by `635cb78b1`.** **The invariant that now holds:** no FTS entry survives a drain-side re-key collision with no header behind it — the *indexed but unfindable* class is closed on the drain side, converging on the disposition the **sync** caller already had via `staleIds`. When `MessageHeaderRekey.apply` hits an occupied destination id it deletes the old row and inserts nothing, so the old header id names no header at all; `finishMove` now REPORTS those ids and the drain drops them with `SearchIndex.removeMessages`. **One implementation note that is deliberate rather than accidental, recorded so nobody "cleans it up" without knowing why:** the report channel is a **defaulted `onCollidedRekey` closure** rather than a changed return type, because `FinishTheMoveLocallyTests.swift` — the only other call site — was dirty with another agent's uncommitted work when this was written, and staging it would have committed their change. The closure works and is documented; it was kept rather than churned once that file landed.

🚨 **R16-8 — THE CLASS WAS WIDER THAN THIS ROW'S OWN SCOPING SENTENCE, AND THE SENTENCE IS WHAT HID IT (2026-08-07).** This row opened *"On the drain side only"* and asserted that *"the **sync** caller compensates"*. Both were derived from **one** of `SyncEngineFullSync`'s re-key blocks — the UID-remap block, which does route a collided id down `staleIds` and does keep it out of `ftsRekeys`. Two further blocks inside the **same** `dbPool.write` closure, owning the **same** `staleIds` / `ftsRekeys` arrays, routed **neither**: the **DraftDedup** block (collision leg dropped the id entirely; success leg never published the re-key to FTS) and the **pre-sync inbox reclaim** block (same success-leg omission). R16-8 adds four routings — `staleIds.append(oldId)` on the DraftDedup collision leg, `ftsRekeys.append(...)` on both success legs, and `staleIds.append(extraId)` for the pre-sync reclaim's `preSyncRows.dropFirst()` extras, **a third member the round's own brief did not name**. `MIS-006` (instance not class), and the tell is that the scoping phrase *"on the drain side only"* reads as a completed census when it was a description of the one site that had been looked at.

🚨 **AND THE COMPENSATING SWEEP CANNOT RUN ON AN UPGRADED DEVICE (`MIS-024`).** This row's registrability argument was *"recoverable — an orphan sweep clears the ghost"*. That sweep is `SyncEngine.pruneFTSOrphans`, whose **only production caller** is `oneTimeFTSReconciliation`, which opens `guard !defaults.bool(forKey: Self.ftsReconcileDoneKey) else { return }` on `ftsReconcile.v2.done` and sets that flag on the first clean completion. Predicate: `rg -n --pcre2 '^(?!\s*(///|//)).*(pruneFTSOrphans|ftsReconcileDoneKey)' TabMail/ Shared/ TabMailNotificationService/` → the flag is **read once, written once, and reset NOWHERE in production** (the only other reference is a test assertion). So on every device that has already completed the sweep once, **the recovery mechanism this row leans on can never run again** — and `oneTimeFTSReconciliation`'s own doc comment asserts *"Neither state can form anymore"*, which is precisely what the two unrouted blocks above falsified. The orphan is therefore **permanent on an upgraded device**, not swept. It stays REGISTRABLE rather than being promoted: it is a stale search hit that renders without subject/sender and cannot open — no message destroyed, no intention dropped, no mutation on the wrong message — and one ordinary user gesture (re-run the search, open the message from the list) reaches the live row. **What would re-open this:** a production reset of `ftsReconcile.v2.done` being added (which would make the sweep the live remedy again and this paragraph stale), or a fourth re-key block landing in that write closure without routing its ids.
