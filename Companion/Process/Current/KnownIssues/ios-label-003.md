<!-- KNOWN-ISSUES-AMENDMENT-BEGIN -->
> **AMENDMENT (2026-08-21, issue #71) — the zero-reader disposition remains, but Exchange no
> longer hardcodes this future guard to `true`.** The preserved census and IMAP trip-wire below
> still govern: there is no production consumer today, and any future reconcile must set IMAP
> authority in the same change. The Exchange producer now derives authority from two independent
> facts — `categories` was selected and the response actually decoded that field — preserving the
> distinction between missing and authoritatively empty. Tests cover both evidence axes and all
> current production parser routes without claiming that the enum alone enforces the census.
> A fixed-string census over production and test Swift sources at this candidate is 16 hits:
> one declaration, two production writes, one production doc comment, one test doc comment, and
> eleven test assertions. There are still zero production reads; this count excludes this Markdown
> amendment, so the observer does not become a member of its own measured population.
<!-- KNOWN-ISSUES-AMENDMENT-END -->
# IOS-LABEL-003

> Routed from `KNOWN_ISSUES.md` line 625 during the 2026-08-09 hierarchy split. The exact pre-split source is hash-pinned in [`known-issues-pre-hierarchy-2026-08-09.txt`](../../History/KnownIssues/known-issues-pre-hierarchy-2026-08-09.txt) (`SHA-256 513497704ad37e977e2fb86e4623e956e6f1ca99844122948ff74995dfa9a309`).

- Register classification: `closed-decision`
- Original row SHA-256: `75527fc63162011dc93d56468f85b36aabf141b9f93b452ebdbb862c6528daec`

## Status

✅ **CLOSED AS A DECISION (2026-08-05)** — **coordinator's ruling: the two "void by construction" elements are NOT-APPLICABLE, not unstated, and a row whose recoverability question is provably vacuous is fully adjudicated rather than outstanding.** The census below was re-derived independently at HEAD and is exact — 9 hits, **zero production reads** — and `MessageHeaderInfo` is `Sendable` but **not** `Codable`, so there is no serialisation path, no persisted column and no wire argument by which the value could become observable. Recoverability asks what state the user can be left in; when the value provably cannot reach any state the user can observe, the question has no content, and *"not applicable, and here is the measurement that makes it so"* is a complete answer rather than a missing one. **Both candidate discharges are refused, for reasons that do not expire:** deleting the field is the `MIS-018` half-port shape (a consumer ported later against an absent flag), and wiring the consumer up is new feature work, which THE MANTRA forbids authoring for a rare edge at freeze — and this edge is not merely rare, it is currently **unreachable**. Registered rather than mechanised is exactly the disposition the mantra prescribes. ⚠️ **TRIP-WIRE, unchanged and load-bearing:** `IMAPProvider` does **not** set the flag, so any future consumer must set it at the IMAP keyword FETCH **in the same change** — a consumer landing without it fails IMAP labels closed **forever**. That trip-wire is why the field is held rather than deleted, and it is what a future reader needs; an open status is not. **Original disposition, retained as history:** 🔓 **OPEN by decision (2026-08-05)** — **left open deliberately, with the reason stated rather than as an omission: two of the five closure elements — the recoverability clause and its non-recovering case — are VOID BY CONSTRUCTION here and cannot be stated.** Both are questions about a state the user can be left in, and this row has none: the census re-derived at HEAD is 9 hits — 1 declaration, 2 doc-comment references, 2 writes, 4 test assertions — with **zero production reads**, so no branch, query, wire argument or persisted column depends on the value and there is nothing observable to recover to or from. The other three elements ARE stated in the cell: the decision (keep the inert field), its basis (`MIS-018` — a consumer ported later against an absent flag is the half-port shape), and its accepted cost (one inert field held against rules 2/3/5). The trip-wire that makes the two void elements live is registered in advance: `IMAPProvider` does **not** set the flag, so a consumer landing without also setting it at the IMAP keyword FETCH fails IMAP labels closed **forever**. **It stays citable as outstanding on purpose** — discharging it needs a code decision (wire the consumer up, or delete the field), which a docs-only pass may not make. **Original disposition, retained as history:** 🔓 **OPEN** — inert; **registered rather than deleted, and the reason is `MIS-018`**

## Subsystem and search terms

User labels; `MessageHeaderInfo.userLabelIdsAreAuthoritative`; `EmailProvider.swift`; `GmailProvider.parseGmailMessage`; `ExchangeProvider.parseGraphMessage`; anticipatory shim; dead code; rules 2/3/5; `remoteSetIsAuthoritative`

## Full detail

**Two production writers, zero production readers, four tests.** Declaration-grammar census at HEAD (`rg userLabelIdsAreAuthoritative TabMail/ Shared/ TabMailTests/ TabMailNotificationService/`, **9 hits total, all 9 enumerated**): one declaration (`EmailProvider.swift`, `var userLabelIdsAreAuthoritative = false`), **two** doc-comment references — `EmailProvider.swift`'s own *"⚑ CURRENTLY UNCONSUMED ON v3"* block and one in `StatefulGmailActionServerTests.swift` — **two** writes (`GmailProvider.swift`, `userLabelCatalog.isAuthoritative`, pre-existing; `ExchangeProvider.swift`, `true`, added by this candidate's `e265428ff`), and **four** test assertions (`ExchangeUserLabelCategoryTests` ×1, `StatefulGmailActionServerTests` ×3). ⚠️ **An earlier wording of this row said "one doc reference", so its enumeration summed to 8 against a stated total of 9** — the ninth is the test-file doc comment, and a row whose parts do not sum to its own total guarantees the corpus gets re-censused (`MIS-007`/`MIS-008`: read the hits, do not count them). **No production read of any shape.** `MIS-021` applied rather than assumed: the field lives on `MessageHeaderInfo`, which is `Sendable` and **not** `Codable`, so there is no serialisation path off it; the value is never written to a column, never passed to a provider call, and never reaches the wire. It is genuinely inert. **The declaration already says so** — *"⚑ CURRENTLY UNCONSUMED ON v3"* — and its stated dataflow premise checks out: every sync-side junction write is `MessageUserLabel(...).insert(db, onConflict: .ignore)` (four sites in `SyncEngineFullSync`, two in `SyncEngineDeltaSync`, one each in `SyncEngineBackfillDeep` and `NSEDataBridge`) and there is **no** sync-side junction delete, so nothing can erase membership and nothing needs the guard yet. **Why it is kept rather than deleted, stated as a trade and not a preference.** Deleting it satisfies rules 2/3/5 at the cost of `MIS-018`: the consumer it guards (`remoteSetIsAuthoritative:` in the reference's `SyncEngineFullSync`, gating a reconcile that REPLACES local membership) would then be ported later against an absent flag, which is the half-port shape — mechanism ported, guard left behind. Keeping it costs one inert field and cannot make a future reconcile *less* conservative, because the `false` default is the fail-closed direction. **The trip-wire, so keeping it is not free of obligation: `IMAPProvider` does NOT set it.** Any consumer that lands must set it `true` at the IMAP keyword FETCH in the same change, or IMAP labels fail closed forever. **The four tests are not blessing tests (`MIS-014`)** — they assert a real PRODUCER contract (that Gmail stamps it only once its catalog has loaded, and that Exchange stamps it `true`), which is live behaviour of live code, not an assertion that a defect is correct. **Reconsider only when a label reconcile is actually proposed**; at that point the choice is wire-it-up, never delete-and-re-add.

**WHY THIS ROW STATES NEITHER A RECOVERABILITY NOR A NON-RECOVERING CASE — said explicitly (2026-08-05), rather than left as an unexplained omission (`MIS-IOS-008`, `MIS-019`).** Both clauses are questions about **a state the user can be left in**, and this row has none to describe. The census above was re-derived at HEAD and is unchanged — **9 hits**: 1 declaration (`EmailProvider.userLabelIdsAreAuthoritative`), 2 doc-comment references, 2 writes (`GmailProvider`, `ExchangeProvider`), 4 test assertions — with **zero production reads**. No branch, no query, no wire argument and no persisted column depends on the value, so nothing observable can be wrong; there is no state to recover *to* and therefore no state recovery can be blocked *from*. **The two clauses become live the moment a consumer lands, and the trip-wire above is exactly that transition:** `IMAPProvider` does not set the flag, so a consumer arriving without also setting it at the IMAP keyword FETCH would fail IMAP labels closed **forever** — that would be this row's first genuine non-recovering case, and it is registered here in advance rather than discovered afterwards. **A1 is inapplicable, and that is itself the finding:** `git grep -c 'userLabelIdsAreAuthoritative' 07a4bb703` returns nothing, so the field is absent at shipped and there is no counterpart to compare against. What shipped does prove is that the product ran with no such flag at all — which is why deleting it is a real option, and why the reason for keeping it is `MIS-018` (a future half-port would drop the guard) rather than any present utility.
