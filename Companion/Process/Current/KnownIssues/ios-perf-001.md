# IOS-PERF-001

> Routed from `KNOWN_ISSUES.md` line 112 during the 2026-08-09 hierarchy split. The exact pre-split source is hash-pinned in [`known-issues-pre-hierarchy-2026-08-09.txt`](../../History/KnownIssues/known-issues-pre-hierarchy-2026-08-09.txt) (`SHA-256 513497704ad37e977e2fb86e4623e956e6f1ca99844122948ff74995dfa9a309`).

- Register classification: `closed-decision`
- Original row SHA-256: `2ddf2d4ae21e255ed367640d4672639b61d2f2095d34bd7c12e5c68c3df286a2`

## Status

✅ **CLOSED AS A DECISION (2026-08-04)** — leading marker normalised 2026-08-05 so the disposition is first; **original leading text, retained as history:** ~~**OPEN — the one deliberately-open row**~~ → the runtime half is MEASURED and LANDED — **measured THREE TIMES**, and all three runs are recorded below (`GATE 77185` on `33ea05b82`: **7** waits; `GATE 42401` on `9443e8a9c`: **5** waits; `GATE fix-agent-gate 1977` on the final-audit candidate `b19dd167c`: **10** waits; **none candidate-attributable in any of the three**) — so the three owed items are recorded and **the register now has ZERO rows owing work**. The OPEN statement below is kept as history, not as a live finding. ⚠️ *deliberately*: since 2026-08-04 `IOS-TEST-009` is also 🔓 OPEN, but it was **opened by a finding, not deferred by a decision**, and it is TEST-INFRASTRUCTURE with no product-invariant impact; this row remains the only one carrying **owed work**. Static half LANDED 2026-08-04 (18 sites, 7 violations, ~~**all pre-existing, none candidate-attributable**~~ ⚠️ **CORRECTED 2026-08-05 — that verdict was a BASE-SCOPE ERROR, not a counting error: it was computed against `508e0e468`, which is commit 130 of the 184-commit release range, instead of against the release base `07a4bb703`. Re-run against `07a4bb703`, the census yields exactly ONE candidate-attributable site — `NSEDataBridge.mergeInboxRemovals`, introduced by `4c41a85dd` — and it is now FIXED. See the corrected attribution block below.**); the **runtime half is still owed** and is measured last, on the quiescent tree

✅ **UPDATE 2026-08-04: `IOS-TEST-009` IS NOW FIXED (`87c805784`), so this row is the only row in the register carrying owed work AND the only row still open — the two sets have converged.** Nothing about this row's own status changes: the runtime half is still owed, exactly as stated below. ⏳ **THAT 2026-08-04 UPDATE IS SUPERSEDED ON BOTH LIMBS — corrected 2026-08-05 during the task-#112 doc reconcile, and kept in place rather than deleted.** **(a)** *"the runtime half is still owed"* is spent: it was **measured three times** and landed, exactly as this cell's leading text now records — the two sentences were contradicting each other inside one cell. **(b)** *"the only row still open"* does not survive its own predicate. Re-derived rather than trusted (`MIS-007`), with the predicate beside the number — *rows whose Status cell **leads** with 🔓* — the count is **ELEVEN at `c6ec9f335`**: `IOS-IDENTITY-001`, `IOS-NOTIFY-001`, `IOS-SCROLL-002`, `IOS-QUEUE-007`, `IOS-QUEUE-008`, `IOS-NSE-004`, `IOS-LABEL-003`, `IOS-GRAPH-004`, `IOS-SCROLL-003`, `IOS-DRAFT-012`, `IOS-MIGRATION-003` — and this row is **not** among them. ⚠️ **THE REVISION IS NOW PART OF THE CLAIM — added 2026-08-05, round 10. The bare integer above was true when written and is FALSE at HEAD.** Re-derived with this file's own field-count predicate at **`3974e4280`**: rows whose Status cell *leads* with 🔓 number **ZERO**, over **93** register rows. The eleven-member list is retained verbatim as the historical MEASUREMENT that motivated the closure pass recorded in the legend above — every one of those eleven was adjudicated and re-marked ✅ **CLOSED AS A DECISION** by `1d1557187` — and it is **not** a statement about the current tree. This follows the legend's own *"Answer at `1d1557187`"* convention: **never write a bare integer about this register without the revision it was derived at** (`MIS-007` — the same failure mode this row already records twice). Re-derive rather than trust; the predicate is stated in the legend and `awk` cannot evaluate it (no PCRE lookbehind), so use `python3`. ⚠️ **Stated negatively so the correction does not become the next false absolute (`MIS-019`):** this corrects a COUNT and says nothing about whether those eleven constitute *"owed work"*. They are adjudicated deferrals, each carrying its recoverability and its named non-recovering case; the *"ZERO rows owing work"* sentence above is a claim about **owed work**, and this correction neither confirms nor widens it. Do not re-derive either count from the other.

## Subsystem and search terms

GRDB `Pool.get`; QoS; priority inversion; Thread Performance Checker; `disableThreadPerformanceChecker`; `WritePriority`; `PriorityGate`; `Task.detached(priority: .utility)`; harness isolation; test host; Inbox; MessageDetail; UIDVALIDITY; send initiation

## Full detail

The policy carries forward from the reference line: treat any **candidate-attributable** inversion as a blocker, count and name every residual pre-existing family, and route the existing class to a separate harness-isolation task instead of changing unrelated production priority tiers or silencing the checker. **The measurement does not carry forward** — the reference's 30-wait / nine-family census was taken on a different tree. Re-run it on the v3 candidate at the final audit and record the new family list and evidence path here.

📐 **STATIC HALF LANDED (2026-08-04), read-only census, no code changed. THE ROW STAYS OPEN — the runtime half is still owed; see "STILL OWED" below.** **Enumerated by STATE, not by one grep string:** `Task(priority:)`, `Task.detached(priority:)`, `.task(priority:)`, task-group / `async let` inheritance, `DispatchQueue.global(qos:)`, `DispatchQueue(label:qos:)`, `Thread.qualityOfService`, `OperationQueue.qualityOfService`. That found **18 sites against a 13-row first-pass grep**. ⚠️ **THE COUNT IS SEVEN VIOLATIONS, NOT SIX — corrected here because the census's own two tables disagreed with each other.** Its per-site table marked **seven** sites VIOLATION while its summary paragraph named only six, omitting `AppStartup.ensureDatabaseReady`'s tail (`TabMailApp.swift`) — which is the one with *runtime* corroboration, since `NSEDataBridge.mirrorAccountMap` appears BY NAME as a Thread Performance Checker holder frame in two archived full-suite logs. The per-site table is authoritative. **The seven** (each a `Task.detached(priority: .utility)` whose body transitively reaches a `DatabasePool` we own, main and/or FTS): `SyncEngine.scheduleMaintenanceInBackground`, `SyncScheduler.syncStartup`'s `.full`/BGProcessing arm (mitigated only by Swift's priority escalation through `await …value`, a runtime behaviour the ADR never mentions), `SyncEngineMaintenance.runPruneIfOverBudget`, `SyncEngineFTS.removeHeadersFromFTS`, `NSEDataBridge`'s inbox-removal merge, `AccountManagerSetup.removeAccount`, and `AppStartup.ensureDatabaseReady`'s tail. **Two more breach the ADR's *mechanism* but not its *letter*** — `BodyAssetStore.bumpMessageAccess` and `SettingsView`'s `attachmentsBudgetMB` `.onChange` — because they hold the App-Group **`DatabaseQueue`**, which the ADR's text (written against `DatabasePool`) does not name; a `DatabaseQueue` serialises *all* access, so a `.utility` holder blocks a higher-QoS reader **harder**, not less. **Six are COMPLIANT and three NOT-APPLICABLE** (7 + 2 + 6 + 3 = 18). ~~✅ **ALL NINE OF THE VIOLATION-AND-MECHANISM SITES ARE PRE-EXISTING — verified independently by `git blame` plus `git merge-base --is-ancestor <sha> 508e0e468`, not inherited:** `d5de3200c` (2026-05-30, the initial public release) ×5, `9f593be0b` (2026-06-28), `d6f4c5626` (2026-06-23), `4c41a85dd` (2026-08-01). **Every one is an ancestor of the candidate base, so NONE is candidate-attributable and none is a blocker.**~~

⚠️ **CORRECTED 2026-08-05 — THE ATTRIBUTION ABOVE USED THE WRONG BASE. It is struck through rather than deleted, per this file's supersede-don't-delete convention.** The `--is-ancestor` probe was run against **`508e0e468`**, which is **commit 130 of the 184-commit release range** (`git rev-list --count 07a4bb703..508e0e468` = 130; `git rev-list --count 07a4bb703..115eb85d2` = 184) — i.e. against a mid-range commit, not against the release base. Anything introduced in the range's first 130 commits therefore *looked* pre-existing. This is a **base-scope error, not a counting error**: the site list, the count of 18/7/9, and the family analysis are all unaffected, and are NOT re-opened here. **Re-run against the release base `07a4bb703`, three of the four shas are genuinely pre-existing and exactly ONE is not:** `d5de3200c` (2026-05-30) → ancestor of `07a4bb703` ✅ pre-existing; `d6f4c5626` (2026-06-23) → ancestor ✅ pre-existing; `9f593be0b` (2026-06-28) → ancestor ✅ pre-existing; **`4c41a85dd` (2026-08-01, "Let a message body outlive the header that first fetched it") → NOT an ancestor of `07a4bb703` ⇒ CANDIDATE-ATTRIBUTABLE.** ✅ **THE ONE CANDIDATE-ATTRIBUTABLE SITE IS `NSEDataBridge.mergeInboxRemovals`, AND IT IS NOW FIXED (2026-08-05).** That commit added a **new** `Task.detached(priority: .utility)` whose body calls `MessageContentStore.releaseUnowned(keys, stores: .body)`, which reaches the **main** `AppDatabase.dbPool` with one roster `pool.read`, then a `pool.read` (ownership) plus a `pool.write` (delete) **per key** — a `.utility` (QoS 17) holder on the main pool, 8 QoS levels below MainActor's `.userInitiated` (25), which is precisely what ADR-IOS-031 forbids. Corroborated by a range census of `Task.detached(priority: .utility|.low|.background)` over `TabMail/`, `Shared/` and `TabMailNotificationService/`: **11 sites at `07a4bb703` → 12 at `115eb85d2`, delta exactly +1, and the delta is exactly this site** (the pre-existing `NSEDataBridge` detached task moved from line 2515 to 3292 — same downstream-enqueue body, verified by its identical comment block — and is NOT main-pool-touching in its own body, so it is left alone). **Fix: `.utility` → `.medium` at that one call, nothing else.** The remaining eight sites stay as the row's standing policy prescribes — counted, named, routed to the separate harness-isolation task, no production tier churn. Per this row's own standing policy they are counted and named, and the class is routed to a separate harness-isolation task rather than churning production priority tiers. **TWO TRAPS SETTLED, both the way the code says rather than the way the names suggest — record them, because each would otherwise be re-derived as a false finding.** (1) `AppDatabase`'s `PrioritizedDatabase(pool:priority:)` takes **`WritePriority`** — `enum WritePriority: Int, Comparable, Sendable { case priority = 0 … }` in `DatabaseWriteQueue.swift`, the DB **write-queue tier** — and **not** a `TaskPriority`. Its `.normal` / `.background` arguments are NOT-APPLICABLE to ADR-IOS-031, and calling them violations would be a false finding; relatedly, **zero** production sites use `TaskPriority.low` or `.background` anywhere. (2) `Task.detached`'s task-local loss does **not** bite: the write tier lives in `PriorityGate.writePriorityOverride` / `.inPrivilegedContext`, whose overrides occur at exactly four production sites (`ActiveBodyQueue` ×2, `BackfillBodyQueue` ×2) plus one `privileged` site (`NSEDataBridge`), and **neither `ActiveBodyQueue.swift` nor `BackfillBodyQueue.swift` contains a `Task.detached` at all**; the one detached site inside a privileged merge loses the task-local **deliberately** and re-asserts `false` explicitly. 🔧 **THREAD PERFORMANCE CHECKER IS ALREADY ENABLED AND ALREADY FIRING — record this, because the obvious next move ("enable TPC") is a no-op that would waste a later reader's time.** 20+ archived raw `xcodebuild test` logs contain `Thread Performance Checker:` blocks. The generated scheme's `<TestAction>` carries no `disablePerformanceAntipatternChecker` and no `disableMainThreadChecker`; XcodeGen writes no diagnostics attributes for this `project.yml`, so Xcode's default (enabled) holds. **There is no `xcodebuild` flag** — `-help` offers only the sanitizers, `-enableCodeCoverage` and `-enablePerformanceTestsDiagnostics` (which is performance XCTests, not TPC). XcodeGen exposes only the NEGATIVE key `disableThreadPerformanceChecker`; **the correct maintenance action is simply never to add it, and never to hand-edit the generated scheme** (`./Scripts/xcodegen.sh` discards hand edits). Capture the log RAW — piping through `xcpretty`/`xcbeautify` drops the app-under-test's stderr, which is exactly where TPC writes. ⚠️ **Attribution is by FAMILY, not by call site, and that is a property of the data rather than a shortcut:** the top ~14 backtrace frames are GRDB-internal (`GRDB.Pool.get` → dispatch → concurrency) and the async continuation boundary erases the caller, so many blocks carry no application frame at all. **STILL OWED — the runtime half, and this row does NOT close until it lands:** one full-suite `xcodebuild test` on the exact commit candidate, on a quiescent tree with no other agent building, then the **total wait count**, the **named family list**, and the **evidence log path** recorded here. Any **candidate-attributable** inversion remains a blocker; residual pre-existing families are counted and named, never silenced.

✅ **RUNTIME HALF LANDED 2026-08-04 — THE THREE OWED ITEMS ARE RECORDED HERE AND THE ROW CLOSES.** ⚠️ **THE GATE RAN TWICE AND THIS ROW RECORDS BOTH RUNS — because the wait COUNT is a timing-dependent measurement, not a fixed property of the tree.** The first version of this entry recorded Run 1's count of **7** while citing the *mutable* path `/tmp/v3-final-gate-tpc.txt`; the gate was then re-run on the final candidate and **overwrote that file with 5 lines**, so a reader following the citation found **5** where the row said **7** — a self-contradicting entry produced by nothing but a re-run. Corrected here, and stated as the general rule this repo keeps re-learning (`feedback_line_citations_go_stale`): **a durable document must not cite a mutable path.** Pin the numbers in the row; give the path only as provenance.

**RUN 1 — `GATE 77185`, candidate `33ea05b82`, quiescent tree, no other agent building.** **(1) TOTAL WAIT COUNT: 7** `Thread Performance Checker` lines. **(2) NAMED FAMILY LIST, by RAW message — deliberately NOT collapsed into one row**, because normalizing away the QoS pair destroys the only fact that distinguishes the families (`feedback_warning_census_normalization_traps`): **4 ×** *"Thread running at User-initiated quality-of-service class waiting on a lower QoS thread running at **Default** quality-of-service class"*; **1 ×** *"Thread running at User-interactive … waiting on … **Utility** …"*; **1 ×** *"Thread running at User-interactive … waiting on … **Default** …"*; **1 ×** *"Thread running at User-initiated … waiting on … **Utility** …"*. **(3) EVIDENCE:** `/tmp/v3-gate-run1-33ea05b82/tpc.txt` — **7 lines**, archived under a sha-scoped directory precisely so a later gate run cannot overwrite it. The same directory holds that run's `marker` (recording `LOCK ACQUIRED owner=GATE 77185` and `HEAD=33ea05b82`), plus `build.log`, `test.log`, `release.log`, `warnings.txt` and `xcresult.json`.

**RUN 2 — `GATE 42401`, candidate `9443e8a9c` — THE FINAL CANDIDATE — quiescent tree, no other agent building.** **(1) TOTAL WAIT COUNT: 5.** **(2) NAMED FAMILY LIST, by RAW message, same non-collapsing rule:** **2 ×** *"Thread running at User-initiated quality-of-service class waiting on a lower QoS thread running at **Default** quality-of-service class"*; **1 ×** *"Thread running at User-interactive … waiting on … **Utility** …"*; **1 ×** *"Thread running at User-interactive … waiting on … **Default** …"*; **1 ×** *"Thread running at User-initiated … waiting on … **Utility** …"*. **(3) EVIDENCE:** `/tmp/v3-final-gate-tpc.txt` — **5 lines**, extracted from the raw `xcodebuild test` log `/tmp/v3-final-gate-test.log`, marker at `/tmp/v3-final-gate.marker` (note the **dot** — run 2's marker is `v3-final-gate.marker` while its other artifacts are `v3-final-gate-*`; looking for a dashed marker finds nothing). Captured RAW, not piped through `xcpretty`/`xcbeautify`, per this row's own capture rule.

**RUN 3 — `GATE fix-agent-gate 1977`, candidate `b19dd167c` — THE FINAL-AUDIT CANDIDATE (the gate ran on the working tree whose content commits `d08f5360d` + `b19dd167c` now carry; the marker records `HEAD=072967889`, the base at lock time, because the commits were created after the gate passed) — quiescent tree, no other agent building.** **(1) TOTAL WAIT COUNT: 10.** **(2) NAMED FAMILY LIST, by RAW message, same non-collapsing rule:** **7 ×** *"Thread running at User-initiated quality-of-service class waiting on a lower QoS thread running at **Default** quality-of-service class"*; **1 ×** *"Thread running at User-interactive … waiting on … **Utility** …"*; **1 ×** *"Thread running at User-interactive … waiting on … **Default** …"*; **1 ×** *"Thread running at User-initiated … waiting on … **Utility** …"*. **(3) EVIDENCE:** `/tmp/v3-gate-b19dd167c/tpc.txt` — **10 lines**, extracted RAW from `/tmp/v3-gate-b19dd167c/test.log`, in a sha-scoped directory that also holds `marker`, `build.log`, `status.txt`, `warnings.txt`, `tpc_families.txt` and `tpc_context.txt` (the full backtraces).

**RUN 3 ATTRIBUTION, CHECKED MECHANICALLY RATHER THAN ASSERTED.** All **10** backtraces bottom out in `GRDB.Pool.get`, as every prior run's did. The TabMail holder frames present across the 10 blocks are `SearchIndex.contentKeysMissingFromFTS`, `ThreadDetection.findRelatedMessages` (and its nested `Connected` helper), `ChatIdTranslator.ensureLoadedFromDB` / `.processResponseForDisplay`, `PrioritizedDatabase.read`, and `MessageDetailViewModel.archiveMessage` / `.lookupFolderRole` / `.loadThreadMessagesAsync`. `git diff --name-only 6391de9a5..HEAD` returns **19 files** — **which now includes this agent's own two commits** — and **none of them declares any of those five types**. **⇒ NO CANDIDATE-ATTRIBUTABLE INVERSION.**

🔑 **THE FAMILY SET IS THE STABLE FINDING; THE COUNT IS NOT.** **The same FOUR families appear in ALL THREE runs** — User-initiated←**Default**, User-interactive←**Utility**, User-interactive←**Default**, User-initiated←**Utility** — and only their *multiplicity* moves: the User-initiated←Default family went **4 × → 2 × → 7 ×**, while the other three were **1 ×** in every run. **No family appeared in any later run that was absent from Run 1, and none disappeared.** ⚠️ **RUN 3 IS THE CASE THAT PROVES THE COUNT IS A SAMPLE:** the totals are **7 → 5 → 10**, so the count went both DOWN and back UP across a strictly-growing candidate, and the entire movement lives in ONE family whose members are the same pre-existing GRDB-pool contention. Had the family SET moved, that would be a finding; a multiplicity that oscillates on a timing-dependent measurement is not. Read the family set as the property of the tree and the total as a sample of it; a future re-run will produce its own count and that alone is not a finding.

**VERDICT: NO CANDIDATE-ATTRIBUTABLE INVERSION IN ANY OF THE THREE RUNS — this row's blocking condition is NOT met.** Archived historical runs of this suite recorded **19–23** and **18–22** waits. **All three of these runs sit far below every prior measurement** (7, 5, 10 against 18–23). Across the three commits between runs 1 and 2 (`1a1962207`, `45ad66d38`, `9443e8a9c`) the count moved **DOWN, 7 → 5**; run 3 on `b19dd167c` moved it back **UP to 10**, and that rise is stated plainly rather than smoothed away — but it is **not** a regression finding, because (a) attribution was checked mechanically per-frame and no holder frame names code the candidate range touches, (b) the rise is entirely within one pre-existing family, and (c) 10 is still less than half the archived floor of 18. The residual families are the pre-existing class the static half already enumerated and attributed (nine violation-and-mechanism sites, every one an ancestor of the candidate base), and per this row's standing policy they are **counted and named here rather than silenced**: no production priority tier is changed, `disableThreadPerformanceChecker` is still not added, and the class stays routed to the separate harness-isolation task. 🚨 **ALL THREE RUNS INDEPENDENTLY CORROBORATE THAT ADR-IOS-031's DETECTABILITY CLAIM IS FALSE — a MAJORITY of waits names a *Default*-QoS holder in each: 5 of 7 in Run 1, 3 of 5 in Run 2, 8 of 10 in Run 3**, i.e. `.medium` (QoS 21), which that ADR calls *below iOS's priority-inversion detection threshold*. iOS plainly detects and reports it, three times more, on three different trees. The two archived runs said the same (19 of 23; 18 of 22), making these the **third, fourth and fifth** measurements to say it. **The ADR was already AMENDED IN PLACE by `9430ef418` (`Companion/Decisions/Active/adr-ios-031.md`) — do NOT re-open it.** This is corroboration of a correction that already landed; the ADR's decision (never `.low`/`.utility`/`.background` for GRDB work) was never in question, only its detectability claim, and promoting the `.utility` sites would therefore not clear the checker.

⚠️ **`/tmp` IS VOLATILE — all three paths above are provenance for the session that produced them, NOT durable storage. THE COUNTS AND FAMILY NAMES RECORDED IN THIS ROW ARE THE DURABLE RECORD.** If the files are gone, the row still stands on its own; re-deriving them means re-running the gate, which will produce its own timing-dependent count and must be recorded as a **fourth** run rather than silently replacing these. **Run 3 is why this row does not treat a re-run as a correction:** the gate was NOT re-run to obtain a nicer number, and any future run that produces a lower count does not retroactively make 10 wrong.

⚠️ **CORRECTED AGAIN 2026-08-05 — THE CENSUS INSTRUMENT ITSELF WAS TOO NARROW, AND ITS "ONE CANDIDATE-ATTRIBUTABLE SITE" CONCLUSION IS WRONG. This is a `MIS-007` recurrence: the census inherited the shape of its instrument.** Both instruments above read the **LINE**, never the **WORK**: (a) `git blame` of the `Task.detached(priority:)` line plus `git merge-base --is-ancestor`, and (b) a corroborating **site count** of `Task.detached(priority: .utility|.low|.background)` over `TabMail/`, `Shared/` and `TabMailNotificationService/` (11 → 12, "delta exactly +1"). **A site whose `Task.detached` line is OLD but whose CALLEE WAS WIDENED ONTO A DIFFERENT POOL INSIDE THE RANGE is invisible to both by construction** — the line never moves, so `git blame` still names a pre-release commit; the site count never moves, so the delta still reads +1. **Exactly two such sites exist and the census missed both.** **(1) `SyncEngineFTS.removeHeadersFromFTS`** — at `07a4bb703` its detached body called `SearchIndex.shared.removeMessages`, which touches only `SearchIndex`'s **private FTS pool**; at the candidate it calls `MessageContentStore.releaseUnowned(…, pool: pool)`, which does reads and a `DELETE FROM messageBody` on the **MAIN** pool (`dbPool`/`syncPool`/`backgroundPool` all wrap the same `rawPool`, and `PrioritizedDatabase.priority` is the write-queue TIER, not a `TaskPriority` — trap (1) above). Six production callers, every one an ordinary primary path: each delta sync, full sync and deletion-reconcile pass. **(2) `SyncEngineMaintenance.runPruneIfOverBudget`'s chunk release** — the same widening, weaker case, since its enclosing function already touched the main pool at QoS 17 at shipped, so only the contention with its **own delete loop** is new. ✅ **BOTH FIXED 2026-08-05: `.utility` → `.medium` at the two call sites, nothing else changed** — the same shape as `955233fde` and as the `NSEDataBridge.mergeInboxRemovals` fix above. **⇒ The sentence "THE ONE CANDIDATE-ATTRIBUTABLE SITE IS `NSEDataBridge.mergeInboxRemovals`" is SUPERSEDED: there were THREE, and `NSEDataBridge.mergeInboxRemovals` was merely the only one the instrument could see.** The row stays CLOSED because all three are now fixed, **not** because no others existed. ⚠️ **The durable rule, so the next census does not repeat it:** when the question is *which sites hold one of our pools at the wrong QoS*, the unit of attribution is the **reachable work**, not the source line — the census must be taken over the transitive callee set as it exists at **both** ends of the range, and a `git blame` / site-count instrument may only CORROBORATE, never BOUND. The static half's enumeration (18 sites / 7 violations / 2 mechanism-only) is unaffected by this correction and is not re-opened.
