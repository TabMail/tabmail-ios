# IOS-MOVE-002

> Routed from `KNOWN_ISSUES.md` line 1092 during the 2026-08-09 hierarchy split. The exact pre-split source is hash-pinned in [`known-issues-pre-hierarchy-2026-08-09.txt`](../../History/KnownIssues/known-issues-pre-hierarchy-2026-08-09.txt) (`SHA-256 513497704ad37e977e2fb86e4623e956e6f1ca99844122948ff74995dfa9a309`).

- Register classification: `closed-decision`
- Original row SHA-256: `3e30c8988b8fef42db959846e40444e075230b01497ceebdd9da92b9f8e73e94`

## Status

✅ **CLOSED AS A DECISION (2026-08-05, round-11 adjudication of the round-10 F3 finding)** — the two-sided refusal is CORRECT and stays; what was 🔓 OPEN was the absence of an exit, and an exit exists — heavyweight, but a real single user gesture that always fires — so this is an accepted fail-closed residual, not a live deferral. **MECHANISM (unchanged; the round-10 text is retained verbatim at the end of this cell).** `AccountManagerActions.optimisticMoveToFolder` moves `folderId`/`folderPath` to the destination, NILs `observedUidValidity`, and leaves the primary key and the `messageId` column at their SOURCE values. When the wire returns no admissible `COPYUID` for that member, `MessageHeaderRekey.finishMove`'s G4 arm leaves the row there; and when the destination's own UID space also answers for that numeric UID on every pass, `SyncEngineFullSync.canonicalizeLocalRows` refuses forever, because `providerAddressOwnershipProven` needs `row.id == canonicalId` or `row.observedUidValidity == sourceBoundEpoch` and the remnant has neither and can never acquire either. **WHAT THE USER ACTUALLY OBSERVES — three sub-states, and only ONE of them hides anything.** (i) *The destination assigned the moved copy the SAME numeric UID.* The remnant IS that message and renders with its own subject, sender, date and body — the caller's `guard recon.sourceAddressProven else { … continue }` arm rewrites no field — so nothing is hidden and nothing is duplicated. The whole damage is that the row is permanently gesture-inert. (ii) *A DIFFERENT message owns that UID and the identity discriminator is blind* (either side carries no RFC 822 Message-ID, or the two identities are equal). That message is never materialised locally and never enters FTS. It is intact on the server. (iii) *A DIFFERENT message owns that UID and both identities are present and differ.* `admitIncomingAsNewRow` ADMITS it — anchor-tested by `serverMessageAbsentLocallyBecomesPresentDespiteForeignRemnantAtItsUid` — so nothing is hidden, and the residual is a permanent DUPLICATE row for the moved message, one copy of which is gesture-inert. **WHAT BREAKS, EXACTLY.** Only the `message.observedUidValidity == liveEpoch` term inside `AccountManagerActions.admittedOrdinaryActionTargets`. That refuses, FOR THAT ROW ONLY: archive, delete, move-to-folder, `markRead`, `markUnread`, `markFlagged`, `applyLabel`/`removeLabel`, and any undo naming it (`exactPayload` demands whole-command admission). `AccountManager.newGestureRefusedForUnknownEpoch` does NOT fire — it reads the FOLDER's `lastKnownUidValidity`, which is known in the ordinary case this row is about — so every OTHER row in that folder stays fully actionable. One row; never a folder, account or launch brick. **NO DURABLE OPERATION STARVES — this is what keeps it out of the wedge corollary.** Admission refuses BEFORE the `PendingOperation` insert (`optimisticMoveToFolder` returns on a nil admission result, above the insert), so no op is ever created; and the op that MINTED the remnant retired legitimately on the wire, because `IMAPProvider.move`'s non-`COPYUID` arm authorises per member from the `liveSourceUIDs` probe and every member reaches an exit. Nothing is queued and nothing is dropped. **SILENT OR VISIBLE? SILENT, by the owner decision this register already carries** (`IOS-EPOCH-001`, §9 D6(a) — no dialog, no toast). Per surface: read/flag/tag revert VISIBLY when `executeIntentCycle` releases the overlay and the DB republishes; `InboxViewModel.removeUserLabel` and `UserLabelMenuModel` reconcile the label back on refusal. The MOVE family alone leaves the row hidden for the life of `InboxView`'s `dismissedMessages` `@State` and offers an Undo toast for an action that never happened, because `InboxViewModel.archive`/`delete` return `true` (they recorded the undo entry and the overlay mutation) and the refusal happens later inside `AccountManager.move`, which has no channel back to the View. Nothing durable is written and nothing is queued, so this is a transient phantom DISMISSAL, not a phantom persisted success — the row is back the next time the list is rebuilt. **RECOVERY CENSUS — by the PROPERTY *"clears or re-stamps `observedUidValidity` for this row, or removes the row"*, not by guessed symbols.** The five production statements that write a PROVEN epoch onto a `MessageHeader` are enumerated in-tree on `SearchView.resolveLocalResultHeaderId`, and ALL FIVE are blocked from this state: `MessageHeaderRekey.finishMove` needs the `COPYUID` that by hypothesis never arrived; `SyncEngineFullSync.runSyncMessages`' proven-merge arm needs the very proof being refused; and its INSERT arm, `SyncEngine.fetchOlderMessages`, `SyncEngineBackfillDeep.insertBackfillBatchGuardable` and `SyncEngineSelfHeal`'s `missingUIDs` are ALL keyed on *"(folderId, messageId) already has a row"*, which the remnant answers. (`SyncEngineSelfHeal` is the fourth such door and was NOT named in the round-10 text; it is the same shape.) `NSEDataBridge.insertNewHeaderFromStaging` writes `isInInbox: true` INBOX rows only. `selectStaleHeaders` cannot reach the remnant (its UID is in `remoteIds` on all three arms); the UID-remap healer cannot (it iterates `stale` and matches against `newRemoteIds`, which excludes any id that already has a local row); and `SyncEngineEpochVerify.verifyAndBootstrapPrePopulatedFolderEpoch` returns at step 1 because the folder's `lastKnownUidValidity` is NOT nil. **THE EXITS THAT DO FIRE, with their cost:** (1) *a proven UIDVALIDITY turnover on that folder* — `AccountManager.uidValidityResetPurgeTxn` runs `MessageHeader.filter(Column("folderId") == folderId).deleteAll(db)` and the folder resyncs clean. AUTOMATIC, but the user cannot force it and a healthy mailbox may never turn over. (2) *the server expunging whatever owns that UID* — the remnant then leaves `remoteIds`, becomes stale and is deleted. Reachable from ANOTHER client (Thunderbird, webmail), but NOT from TabMail in sub-state (i), because deleting it here is the exact gesture being refused. (3) *Settings → "Delete All Email Index Data"* — `SettingsView.localIndexWipeStatements` opens with `DELETE FROM messageHeader`, so the remnant is destroyed and the next sync inserts the correct rows with their epochs. ONE user gesture, ALWAYS fires, unconditional. Cost: a full re-download of every account's mail, plus the PERMANENT loss of Agent Chat history (`chatTurn`, `chatHistory`, `memory.db` — user-authored, on no server; that consent cost is `IOS-SETTINGS-001`). (4) *removing and re-adding the account* — always fires, same shape, scoped to one account. **RECOVERABILITY, STATED WITH ITS NON-RECOVERING CASE (`MIS-IOS-008`).** No sync pass, no retry and no ORDINARY gesture on that row ever converges it. The state where recovery does NOT fire is precisely *"the folder's epoch never turns over AND nothing external removes the message at that UID"* — which is the ordinary steady state of a healthy mailbox, so exits (1) and (2) must NOT be leaned on. What makes this CLOSED rather than blocking is exit (3): always available, one gesture, at a cost the owner has already priced. **BASIS FOR CLOSING RATHER THAN DEFERRING.** THE MANTRA's blocking set is exactly C3 wrong-message mutation or misattribution, a dropped user intention including the wedge corollary, a brick, and secret exposure — and this is in NONE of them: the refusal exists to PREVENT C3, no intention is ever persisted so nothing starves, and the inertness is scoped to one row. The register already carries the identical ruling one layer up: `IOS-EPOCH-001` is CLOSED AS A DECISION and its own named non-recovering case (an RFC-less population in an already-populated folder, `IOS-AI-003`) is a permanently gesture-inert IMAP row left open ON PURPOSE. **HOW NARROW THE TRIGGER IS, BY CONJUNCTION, FROM CODE ONLY.** (a) The wire must return no admissible `COPYUID` for that member: `IMAPProvider.copyProvenDestinations` yields NOTHING for a nil response code, a zero destination UIDVALIDITY, or source/destination lists that are not BOTH strictly ascending, and `IMAPProvider.move`'s own else-arm names the causes as *"no UIDPLUS, a withheld response code, or a COPY that copied nothing"*. (b) The destination folder must answer for the SOURCE UID on the sync pass — either because it assigned the copy that same number, or because it already held a message there. (c) For the message-HIDING sub-state (ii) only, the RFC discriminator must additionally be blind. ⚠️ Nothing here licenses a claim about how many real servers satisfy (a); the code asserts only that the path exists and that it fails closed. **THE NEGATIVE CASE THAT FLIPS THIS BACK TO BLOCKING (`MIS-019`), stated so it is mechanically checkable.** Any ONE of: (α) `SettingsView.localIndexWipeStatements` stops deleting `messageHeader` unconditionally, or the wipe gains a predicate that could spare a nil-epoch row — exit (3) is the ONLY always-firing recovery and this closure rests entirely on it; (β) admission stops refusing a nil `observedUidValidity`, or `roleMoveRejectDispositions` reclassifies its nil arm as `.terminalStale` — the first re-opens C3, the second converts a refusal into a real dropped intention; (γ) any path begins creating a `PendingOperation` for a row admission refused, which puts a starving op behind a permanent refusal and lands squarely in the wedge corollary; (δ) the shape becomes reachable for a MULTI-message or MULTI-folder population at once, or is shown to arise WITHOUT a user move (a sync-only door) — *"one row, one folder, after one gesture the user made"* is the bound doing the work here. **THE DURABLE FIX REMAINS UPSTREAM and is THE ADDRESS PROBLEM's own answer:** finish the move locally by re-keying the row to the destination UID and epoch at drain time from the `COPYUID` already in hand, so no remnant is ever minted. That is exactly what `MessageHeaderRekey.finishMove` does for servers that answer; closing the NON-answering case needs a different ADDRESS SOURCE, never a weaker proof. Candidate exits (a) re-key-then-admit and (b) the structural `observedUidValidity == nil && id != canonicalId` predicate were examined and REFUSED, and the refusal is NOT to be re-litigated — (a) can only justify itself by RFC 822 equality, which ADR-IOS-068 / D4 bans as provider-address authority, and (b) is refuted by the two anchor tests named below, whose assertion text states that admitting there hands the row to the pre-sync inbox reclaim, which DELETES the user's moved row.

**Original disposition, retained as history:** 🔓 **OPEN (2026-08-05, round-10 F3)** — an optimistic move whose `COPYUID` did not name the member leaves a REMNANT: `AccountManagerActions.optimisticMoveToFolder` moves `folderId`/`folderPath` to the destination, NILs `observedUidValidity`, and leaves the primary key and the `messageId` column at their SOURCE values, and `MessageHeaderRekey.finishMove`'s G4 arm leaves it there. When the destination folder's own UID space then hands the same numeric UID to a DIFFERENT message, `SyncEngineFullSync.canonicalizeLocalRows` refuses — correctly, since `providerAddressOwnershipProven` cannot prove the remnant owns that address (no canonical PK, nil epoch) — and returns `sourceAddressProven == false`, so the caller `continue`s BEFORE the insert. The incoming message is therefore never materialised, and no healer reaches the state: `selectStaleHeaders` cannot (the UID IS in `remoteIds`), `SyncEngineBackfillDeep.missingUIDs` is keyed on `(folderId, messageId)` so the remnant answers for the UID, and the UID-remap healer's `newRemoteIds` excludes any id that already has a local row. **The refusal is right and is NOT to be weakened; what is missing is an EXIT from the refused state.** ⚠️ **REGISTERED RATHER THAN FIXED, and the reason is that both candidate exits land in the non-recoverable set — so choosing one on an implementer's authority is the wrong move.** (a) *Re-key the remnant from the destination address currently observed, then admit the merge* requires believing the remnant IS the incoming message; the only available evidence for that is RFC 822 equality, and "RFC equality ⇒ provider-address ownership" is exactly what **ADR-IOS-068 / D4** bans, because two legitimately distinct copies share a Message-ID — it re-opens C3 wrong-message mutation. (b) *Admit the incoming as a second row on a STRUCTURAL predicate* (`observedUidValidity == nil && id != canonicalId`, i.e. "this row's `messageId` is a SOURCE-space UID and so carries zero evidence about this address") is refuted by two existing anchor tests, `identitylessRemnantKeepsTheRefusal` and `identitylessIncomingEnvelopeKeepsTheRefusal` in `TabMailTests/Sync/RFC822IdentityMergeGuardTests.swift`, which pin `rows.count == 1` on exactly that shape and state the cost in their own assertion text: on the inbox it hands the row to the pre-sync reclaim, whose gate cannot exclude a nil stored identity, **which deletes the user's moved row outright**. So (a) costs a wrong-message mutation and (b) costs a dropped user message; the refusal costs visibility of one message in one folder until the next epoch turnover or a manual re-sync. **RECOVERABILITY, stated with its non-recovering case:** the remnant and the shadowed message both persist on the server and both return on any UIDVALIDITY turnover, folder re-add, or "Delete All Email Index Data"; what does NOT self-heal is the shadowed message's local visibility while the remnant survives — no ordinary user gesture clears it, which is why this is 🔓 OPEN and not CLOSED AS A DECISION. **The durable fix is upstream and is THE ADDRESS PROBLEM's own answer: finish the move locally by re-keying the row to the destination UID and epoch at drain time from the `COPYUID` already in hand, so no remnant is ever minted.** Do not attempt (a) or (b) without the owner ruling on the C3 trade

## Subsystem and search terms

sync; merge; `SyncEngineFullSync.canonicalizeLocalRows`; `providerAddressOwnershipProven`; `admitIncomingAsNewRow`; `sourceAddressProven`; remnant; optimistic move; `MessageHeaderRekey.finishMove` G4; `COPYUID`; ADR-IOS-068; D4; THE ADDRESS PROBLEM; `identitylessRemnantKeepsTheRefusal`; `identitylessIncomingEnvelopeKeepsTheRefusal`; `serverMessageAbsentLocallyBecomesPresentDespiteForeignRemnantAtItsUid`; recoverability; non-recovering case; MIS-IOS-008; silent no-op; gesture-inert; `admittedOrdinaryActionTargets`; `newGestureRefusedForUnknownEpoch`; `SyncEngineSelfHeal`; `SyncEngineEpochVerify`; `uidValidityResetPurgeTxn`; `SettingsView.localIndexWipeStatements`; Delete All Email Index Data; `IOS-EPOCH-001`; `IOS-SETTINGS-001`; wedge corollary; THE MANTRA

## Full detail

Do not weaken the two-sided gate. Any future fix must give the refused state an exit WITHOUT deriving address ownership from RFC equality, and must keep both anchor tests green. **CLOSED 2026-08-05 as a decision — the exit is `SettingsView.localIndexWipeStatements`, and this row's closure rests on it; see the four flip-back conditions (α)–(δ) in the Status cell before changing that wipe, admission's nil-epoch arm, or `roleMoveRejectDispositions`.**
