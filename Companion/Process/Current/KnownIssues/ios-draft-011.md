# IOS-DRAFT-011

> Routed from `KNOWN_ISSUES.md` line 344 during the 2026-08-09 hierarchy split. The exact pre-split source is hash-pinned in [`known-issues-pre-hierarchy-2026-08-09.txt`](../../History/KnownIssues/known-issues-pre-hierarchy-2026-08-09.txt) (`SHA-256 513497704ad37e977e2fb86e4623e956e6f1ca99844122948ff74995dfa9a309`).

- Register classification: `closed-decision`
- Original row SHA-256: `862a92c9af250fd6a7cfbeb6ef3063329090873dd3ac4a2200a65fb96b75c1d3`

## Status

✅ **CLOSED AS A DECISION (2026-08-04)** — K strays accepted, arithmetic and bound stated; the deferred code-comment correction is now ✅ **APPLIED (comment-only)**, so the row has NO live remainder; **class A** relative to the pre-candidate but **strictly better than shipped**

## Subsystem and search terms

Drafts; `IMAPProvider.saveDraft`; `.unaddressable`; `DraftStore.applyPushCompletion`; `DraftStore.priorIdentity`; no-`APPENDUID` mailbox; stray server copies

## Full detail

**The accepted cost of `459786db1` is N stray server drafts, not one.** `.unaddressable` makes `applyPushCompletion` null `serverDraftId` / `serverDraftUidValidity` / `serverDraftFolderPath`, so `priorIdentity` returns nil on the **next** save of the same draft, the old-copy delete is skipped, and each subsequent save APPENDs another copy. The code comment says *"the user may see a stray duplicate draft to remove by hand"* — **singular**; on a mailbox that withholds `APPENDUID`, K user saves leave K copies. **Bounded by explicit user gestures:** the only `.saveDraft` producer is the user-gesture site in `AccountManagerActions`, and no sweeper re-enqueues on `serverPushStatus`, so there is no autonomous APPEND loop. Registrable as a comment-accuracy and UX-cost understatement, not a correctness defect — and the alternative it replaced expunged the **wrong** draft irreversibly.

✅ **CLOSED AS A DECISION (2026-08-04) — K strays per K saves is ACCEPTED, and the arithmetic and its bound are stated here rather than left implicit.** **The arithmetic:** on a mailbox that withholds `APPENDUID`, `.unaddressable` makes `DraftStore.applyPushCompletion` null `serverDraftId` / `serverDraftUidValidity` / `serverDraftFolderPath` / `serverPushStatus`; `DraftStore.priorIdentity` therefore returns nil on the **next** save of the same draft, the old-copy delete is skipped, and each subsequent save APPENDs another copy. **K user saves leave K server copies.** **THE BOUND, NAMED:** K is bounded by explicit user gestures and nothing else — the only `.saveDraft` `PendingOperation` producer is the user-gesture site in `AccountManagerActions`, and **no sweeper re-enqueues on `serverPushStatus`**, so **there is no autonomous APPEND loop**: K cannot grow while the user is not saving. That bound is what makes K acceptable; without it this would be an unbounded write loop rather than a UX cost. **Basis, from this codebase rather than from first principles:** the standing ranking is stated three times over — `IOS-IMAP-001` (*"incomplete visible cleanup is safer than permanently deleting an unrelated pre-deleted message"*), `IOS-IMAP-006` (*"duplicated mail is recoverable … this repo's stated ranking prefers it to both a dropped intention and a wedge"*), and Outbox Reliability Rule 4 (*"Prefer double-send over drop"*). K stray drafts is the **draft-side instance of exactly that ranking**, and the alternative `459786db1` replaced expunged the WRONG draft irreversibly (C3, and the one delete in this app that is not a move to Trash). **Recoverability, checked for the specific trap rather than asserted:** the recovery gesture is the user deleting the strays by hand, and the failure this could have had — a stray that cannot be deleted because it inherits the same refusal that created it — **does not arise**. The strays are ordinary messages in the Drafts folder, synced with real server UIDs by `SyncEngine`, so the ordinary message-delete path addresses them by their own synced UID and never needs `APPENDUID`; `.unaddressable` is a property of the **APPEND RESPONSE**, not of the mailbox's ability to report UIDs on FETCH/SELECT. Suppressing later pushes would need a new terminal `serverPushStatus` value plus a producer-side skip — a new mechanism for a rare recoverable edge, which THE MANTRA forbids.

⚠️ **ONE CORRECTION IS DEFERRED, NOT APPLIED — the code comment still says "singular".** `IMAPProvider.saveDraft`'s *"⚠️ ACCEPTED COST"* block reads *"the user may see a stray duplicate draft to remove by hand"* — **singular** — which understates by an unbounded factor the cost this row has just quantified, in the one place an implementer touching that arm will read. It must be corrected to state **K strays per K saves** and to name the bound above (sole user-gesture producer in `AccountManagerActions`; no `serverPushStatus` sweeper; therefore no autonomous APPEND loop). **`TabMail/Providers/IMAPProvider.swift` is owned by another live agent this round, so the edit is recorded here as DEFERRED rather than made.** It is the only site: a tree-wide search finds this wording nowhere else, and in particular **not** in `DraftStore.applyPushCompletion`, which an earlier statement of this correction paired with it. A register row that understates a defect is a recorded defect class here — so is a code comment that does, and this one is now tracked rather than lost.

✅ **THE DEFERRED CORRECTION IS NOW APPLIED — this row has NO live remainder.** `IMAPProvider.saveDraft`'s *"⚠️ ACCEPTED COST"* block no longer says *"a stray duplicate draft"* singular; it states **K strays for K saves**, gives the arithmetic (`.unaddressable` → `DraftStore.applyPushCompletion` nulls the four server-identity fields → `DraftStore.priorIdentity` returns nil on the next save → the old-copy delete is skipped → each save APPENDs another copy), names **the bound** that makes K acceptable rather than an unbounded write loop (the sole `.saveDraft` `PendingOperation` producer is the user-gesture site in `AccountManagerActions`, and no sweeper re-enqueues on `serverPushStatus`, so there is no autonomous APPEND loop and K cannot grow while the user is not saving), records that every stray is deletable by an ordinary gesture because the strays are ordinary Drafts-folder messages synced with real server UIDs and `.unaddressable` is a property of the **APPEND RESPONSE** rather than of the mailbox's ability to report UIDs on FETCH/SELECT, and cross-references this row by id. **The change is COMMENT-ONLY** — verified by diffing the file and confirming that every added and removed line is a `//` comment line, with zero executable, control-flow or signature changes; no test was added, because there is no behaviour to pin. It was deferred only because the file was owned by another agent at the time, and that agent has since landed.

➡️ **DESCENDANT ROW: `IOS-DRAFT-015`.** This row's accounting — strays are ordinary Drafts-folder messages, synced with real UIDs, deletable by the ordinary path — is what makes the round-5 restoration of shipped retry semantics acceptable. ⚠️ **AND ITS BOUND DOES NOT TRANSFER.** *"K is bounded by explicit user gestures / no sweeper re-enqueues on `serverPushStatus`"* was written about **`.unaddressable`**, which is a provider-**AUTHORITATIVE** "I cannot give you an address" (never-drop exit 2). It is **not** an argument about a **thrown** provider call, which is an *unknown* and therefore retryable — and it was read as one, which is how `DraftStore.pushDraftToServer` came to retire the user's Save intention after a single network failure. See `IOS-DRAFT-015` for the fix and for the residual it accepts.
