# TabMail iOS - Project Memory

> **iOS-specific knowledge.** Claude reads this before every task and updates it when discovering something new. For cross-cutting knowledge, see `../PROJECT_MEMORY.md`.

**Last updated:** 2026-04-20

---

## Key Files

| What | Where |
|------|-------|
| Project definition | `project.yml` (XcodeGen) |
| App entry | `TabMail/` |
| Secrets | `Secrets.xcconfig` (gitignored) |

---

## Summary `recipient_status` (cc detection) — 2026-07-04

Summary requests send `recipient_status: "cc"` ONLY on positive evidence: the **receiving account's** address literally in the Cc header (and not in To, and user not the author). User in neither To nor Cc (alias/plus/Bcc/list delivery) → field omitted — never claim cc when unsure (SMTP envelope is discarded at delivery, so not-in-To proves nothing). Matching asymmetry: suppress checks (author/To) strip plus-tags (loose); the Cc claim is strict exact-address. Backend injects an explicit "only Cc'd" message on `"cc"`. Classification: `PromptVariables.classifyRecipientStatus(toField:ccField:fromField:userEmails:)` (Shared — regex-extracts ALL addresses from raw header strings, parity clone of TB `senderFilter.classifyRecipientStatus`; TB is reference per ADR-IOS-008). Plumbing: optional `recipientStatus` param on `summaryVariables` / `AIService.generateSummary` / `AIService.process`. Computed at 3 sites: `ActiveAIQueue.executeSummaryJob` + `AccountManagerAI.processMessage` (pass `message.to/cc/fromAddress` + `[account.emailAddress]`), NSE `NotificationService` (passes `msg.to/cc/senderEmail` + `[accountEmail]` from the push payload). Verification logs: debug-gated `[AIService] Summary payload: … recipient_status=…("cc" SENT / omitted)`; NSE `step6a … (recipient_status=…)`. Tests: `TabMailTests/Shared/PromptVariablesTests.swift`.

---

## IMAP external-deletion blind spot — old server-deleted messages linger forever (audit 2026-07-02)

**STATUS: FIXED 2026-07-03 — ADR-IOS-051 (Phases 1+2 of `PLAN_IMAP_DELETION_RECONCILE.md`).** `SyncEngineDeletionReconcile.swift`: IDLE `.vanished` UIDs now drive targeted deletes (`SyncScheduler` → `handleVanishedUIDs`); a `localHeaderCount > STATUS messageCount` predicate (evaluated in `imapDeltaSync` per changed folder + at the end of `fullSync` per IMAP folder) triggers a chunked explicit-set `UID SEARCH` walk (`IMAPProvider.searchExistingUIDs(folder:uids:)`, chunks of `SyncConfig.deletionReconcileChunkSize`=500) that deletes local-not-on-server UIDs via the SAME stale-deletion protections + FTS-removal channel. UIDVALIDITY guard via new `folder.lastKnownUidValidity` (migration v63) — mismatch/unreported aborts the walk. Failed SEARCH chunk = delete nothing from it. Phase 3 (QRESYNC, O(changes)) remains documented-only. Audit findings below kept for reference. **Known caveat (post-implementation review 2026-07-03):** the UIDVALIDITY guard only protects walks AFTER the bootstrap — the folder's FIRST-ever walk pins whatever validity the server currently reports, so if UIDVALIDITY had reset at some earlier point, old-epoch rows below the sync window read as ghosts and get deleted. Those rows are already permanent zombies today (their UIDs are dead — unopenable, unfetchable), so removal is arguably cleanup, but sub-window history is NOT re-fetched by a completed backfill. Rare (Gmail effectively never resets; the walk only fires on count evidence); proper fix = capture UIDVALIDITY during normal sync SELECTs too, not just the walk. **Open UX follow-up (2026-07-03):** opening a message that was just deleted externally (before delta/reconcile removes the header) surfaces a generic "network error" from the body-fetch failure. Proper handling: map provider "gone" (Gmail API 404 / IMAP UID-vanished on a SUCCESSFUL lookup) to verified-permanently-gone -> show "message was deleted on the server" + remove header/FTS (the Data Integrity rule 1 404/410 exception applies). Generic network failures must NOT trigger this. **Hardening added 2026-07-03: deletion circuit breaker** — walk deletions are capped at the trigger's expected mismatch + `SyncConfig.deletionReconcileCapSlack`; a falsely-empty SEARCH response (the one single-source seam no unit test can cover) now aborts the walk before the offending chunk instead of wiping the folder. VANISHED (Phase 1) stays uncapped — server-named UIDs.

Code-verified audit (triggered by the TB FTS drift investigation, see `../tabmail-thunderbird/PROJECT_MEMORY.md` 2026-07-01/02). **Gmail/Exchange are SAFE; raw IMAP/iCloud is NOT.**

- **Gmail/Exchange:** provider deltas catch deletions of ANY age — Gmail `history.list` `messagesDeleted` (`SyncEngineDeltaSync.swift:89,106-134`), Graph `$delta` (`:444,458-483`); expired cursor → full sync fallback. No blind spot.
- **IMAP/iCloud blind spot (CONFIRMED):** stale detection (`selectStaleHeaders`, `SyncEngineFullSync.swift:459-480`) only covers the newest-`syncMessageLimit=50`-UID window per folder (full sync AND STATUS-gated delta both funnel into `runSyncMessages` limit=50). Backfill is insert-only (never deletes); `selfHealRecentMessages` is add-only + 90-day-bounded (`SyncEngineSelfHeal.swift:61,125-169`); the complete-knowledge branch only fires for folders <50 server msgs & ≤2000 local rows. So an old, already-bodied message deleted externally below the UID floor persists in GRDB+FTS indefinitely.
- **The drift evidence is captured and then DESTROYED:** `imapDeltaSync` sees `status.messageCount != folder.totalCount` (`SyncEngineDeltaSync.swift:738-744`), triggers only the windowed 50-msg sync, then OVERWRITES `folder.totalCount` with the server count (`:765-770`) — consuming the count-mismatch signal while the stale row survives. Change-detector, not reconciler.
- **IDLE `VANISHED`/`EXPUNGE` payloads are DISCARDED** — `SyncScheduler.swift:599-601` maps `.vanished(UIDSet)` to a generic `backgroundPoll`; the exact deleted UIDs the server hands us for free are thrown away.
- **SwiftMail fork already supports CONDSTORE/QRESYNC/HIGHESTMODSEQ/VANISHED** (STATUS `highestModificationSequence` when `.condStore`; `IdleHandler.swift:120-125`; `SearchCriteria.modSeq`) — the app uses NONE of it for content sync (`IMAPProvider.folderStatus` discards modseq, `IMAPProvider.swift:1528-1532`; `fetchHistory` returns nil `:1536-1540`). `HistoryDelta.swift:8-10`'s "IMAP MODSEQ" comment is aspirational.
- **FTS wiring is correct on every deletion path** (staleIds → `SearchIndex.removeMessages`, delta deletes, confirmed-gone, budget prune) — fixing GRDB reconciliation automatically fixes search.
- **Fix directions (mirrors TB's evidence-triggered design, not yet implemented):** (1) use the IDLE `.vanished` UIDSet for targeted deletes (free, library already delivers it); (2) treat the STATUS count mismatch that the windowed sync CAN'T explain as reconcile evidence → chunked full-folder UID SEARCH walk (backfill already has the chunked-UID-SEARCH primitive) diffed against local UIDs — integer set diff, bounded memory; (3) CONDSTORE `CHANGEDSINCE`/QRESYNC where advertised → O(changes). NEVER a date window (ADR-IOS-042).

## CLI testing breaks with "Simulator device failed to launch / Launchd job spawn failed" → app is UNSIGNED in DerivedData (2026-06-22)

**Symptom:** `xcodebuild ... test` (or `test-without-building`) fails at *launch*, not compile, with:
`Simulator device failed to launch ai.tabmail.ios … The request was denied by service delegate (SBMainWorkspace) … The process failed to launch … Launchd job spawn failed (NSPOSIXErrorDomain Code 163)`. `build-for-testing` SUCCEEDS (exit 0) — the code compiles & links fine — so it's NOT a source/uncommitted-change problem. This is a red-herring error: the modern iOS Simulator refuses to *spawn* an unsigned app.

**Root cause:** the app target's **CodeSign build phase never ran** → `TabMail.app/TabMail` (the thin launcher) AND `TabMail.app/TabMail.debug.dylib` (the Debug "debug dylib" that holds the real app code) are left "code object is not signed at all". A prior `xcodebuild` build was interrupted *after* `Ld`/`CopySwiftLibs` but *before* `CodeSign`; the build database then records compile/link as up-to-date, so subsequent **incremental** builds never re-run `CodeSign` and the app stays unsigned forever. Tell-tale in the build log: it jumps straight from `CopySwiftLibs` to `** TEST BUILD SUCCEEDED **` with **no `CodeSign TabMail.app` line**. (Nested `.xctest`/`.appex` were signed — only the top-level app + its `.debug.dylib` were bare. `codesign --sign - TabMail.app` by hand signs the executable but NOT the root-level `.debug.dylib`, so a manual ad-hoc sign of just the bundle still dies with dyld `@rpath/TabMail.debug.dylib … Trying to load an unsigned library`.)

**Diagnosis sequence (reuse this):** (1) `codesign -dv <built app>` → "not signed at all" = this bug. (2) `codesign -dv <app>/TabMail.debug.dylib` → also unsigned. (3) grep the build log for `^CodeSign ` — absent = the phase was skipped.

**Fix: a CLEAN build** — `xcodebuild -project TabMail.xcodeproj -scheme TabMail -destination 'platform=iOS Simulator,name=iPhone 17 Pro' clean build-for-testing`. A clean build forces the full CodeSign phase; verify with `grep '^CodeSign ' log` (should list `TabMail.debug.dylib`, `__preview.dylib`, `TabMail.app`) and `codesign --verify --deep --strict <app>` (exit 0). Then `test-without-building` runs green. NOTE: this is **ad-hoc (`-`) simulator signing** — it needs NO keychain, so the SSH/keychain limitation (`../CLAUDE.md` git recipe) is NOT the cause here; don't chase keychain. NOTE: a stale/booted simulator can ALSO throw a transient launch error — a `simctl shutdown all` + restart of `com.apple.CoreSimulator.CoreSimulatorService` is the first thing to rule out, but if the app is unsigned that won't help (the real signal is `codesign -dv`).

---

## Test flakiness: `UserDefaults(suiteName:)` isolation FALLS THROUGH to `.standard` — isolating a suite's WRITES is necessary but not sufficient (2026-06-22)

**Symptom:** a test (`FirstLaunchOverdueTests`) **passes in isolation but is flaky in the full suite**. That signature = a parallel suite is contaminating *process-global* state.

**Root cause — the read-through trap:** a `UserDefaults(suiteName: uuid)` instance is NOT a sealed sandbox. Its read search list **falls through to the application (`.standard`) domain** for any key its own suite domain doesn't contain. So even though `FirstLaunchOverdueTests` wrote ONLY to its isolated suite (via `DisabledRemindersStore.withTestDefaults` / `@TaskLocal`), its *reads* of a key it didn't write (`disabled_reminders_v2`) fell through to `.standard`. `DisabledRemindersCRDTTests` ran in parallel and wrote `disabled_reminders_v2` straight to `.standard` (its `.serialized` only orders tests *within* that suite — it does NOT stop it running parallel to OTHER suites), so the victim read the polluter's value → `#expect(map.isEmpty)` etc. flaked. Writes on a suite instance go to the suite domain (isolated ✓); the leak is purely on the read side.

**Fix:** make EVERY test that touches the store write to an isolated suite — converted `DisabledRemindersCRDTTests` to the same per-test `UserDefaults(suiteName: uuid)` + `DisabledRemindersStore.withTestDefaults` pattern `FirstLaunchOverdueTests` already uses (and dropped its now-pointless `.serialized`). With no test writing `disabled_reminders_v2` to `.standard`, the victim's read-through returns nil and the flake is gone (verified: 3× full-suite runs, 7106 tests, both suites green every time).

**Rules of thumb:** (1) A test must NEVER write `UserDefaults.standard` — always an isolated `suiteName`. (2) `.serialized` only serializes *within* a suite; it gives ZERO protection against cross-suite parallel contamination of global state. (3) You cannot mask an ABSENT key via the suite domain (no way to make a suite read return nil when `.standard` has the key) — so "isolated reads" only hold if `.standard` is genuinely clean, i.e. if every writer is isolated. The `@TaskLocal` defaults override (`DisabledRemindersStore.withTestDefaults`) directs reads/writes to the suite instance, but it does NOT change that instance's fall-through to `.standard`.

---

## Foreground-return UI freeze — main-actor DB ops must not block (2026-06-21)

**Symptom:** multi-second-to-~20s UI freeze on foreground-return for users who open the app rarely. **Root-cause class:** a *synchronous* GRDB op on the `@MainActor` blocks the main thread for the full duration of whatever is in flight on `DatabasePool`'s contended connection — when background maintenance (multi-account sync + backfill + FTS reindex + AI) over the EXISTING mailbox saturates the connection at foreground return. The new-mail catch-up is only hundreds of rows; the contention is maintenance over the archive.

- **Writes are the unmitigated hazard.** ONE serialized writer connection — no pool. A synchronous `@MainActor` `dbPool.write` waits behind any in-flight write, **uncapped by `busyMode`**. The op's own row work is trivial (5k rows ≈ 163ms); the freeze is the contention wait transmitted 1:1 to the main thread. **Fix: the async `await dbPool.write` overload** — it suspends the caller (main actor stays live) while the write runs on GRDB's own writer queue. Validated RED→GREEN in `NSEMergeMainActorBlockTests` (sync: 0 `@MainActor` heartbeats during a 2.9s merge under a real 18k-row maintenance write; async: ~149).
- **Reads are mitigated by `maximumReaderCount = 64`** (vs observed concurrent-reader ceiling ~15; raised from 10 after a 6.7s `fetchPage` block — see the `makePool` comment). A synchronous `@MainActor` `dbPool.read` only blocks if all 64 readers are held. The 64-pool is the *workaround*; async reads are the *root fix* (suspend, not block, regardless of pool size). **Keep the 64-pool as a backstop** even after converting reads.
- **Converted (off-main async writes):** `NSEDataBridge.mergeNSEStagingData` (fg-return write — now `async`, 7 callers `await`) and `NavigationStore.toggleFavorite/setFavorite/setPrimaryAccount` (sidebar writes — now `async`, 3 SwiftUI callers `Task { await }`). Each is an atomic single-transaction write (no read-decide-write across the suspension → no TOCTOU). **Data-integrity note:** GRDB transactions stay atomic whether sync or async — async NEVER splits a transaction. The only new surface is main-actor reentrancy during the suspension, safe when read-decide-write stays inside one `write {}` and callers `await` to preserve ordering (e.g. merge-before-sync).
- **Also converted (2026-06-21 startup/resume re-audit):** `AppStartup.ensureDatabaseReady` ran `AppDatabase.createNSEStagingDBIfNeeded()` (`TabMailApp.swift:480`) SYNC on the `@MainActor` — a write to the SEPARATE App-Group `DatabaseQueue` (so the 64-reader pool can't help it; writes have no pool anyway), `CREATE TABLE` + additive ALTERs on EVERY launch, `busyMode = .timeout(2)` → up to a **2s hard main-thread block on the cross-process NSE file lock**, behind the splash. Now wrapped in `await Task.detached { AppDatabase.createNSEStagingDBIfNeeded() }.value` (still completes before `dbReady`, so push / NSE-merge waiters are unaffected). NOT the 20s hazard (bounded ≤2s, low-probability) but the last sync *write* on the launch main actor — missed in the first off-main sweep (the `DemoSeed.wipe` line directly above it was already async). `createNSEStagingDB` is self-contained (opens a local queue, writes, discards — touches no main-actor state), so off-main is safe; `mirrorAllState` stays on the main actor (pool-mitigated reads, pre-contention). **↑ SUPERSEDED 2026-06-23 (see next bullet): both moved OFF the `dbReady` gate and the staging schema is now version-gated.**
- **Cold-launch BLANK-SCREEN (not the merge!) — NSE staging creation taken off the `isReady` gate + version-gated (2026-06-23):** field report of a **2–5 s blank `Color(.systemBackground)` before the inbox renders**, intermittent, specifically "NSE push arrived → I open the app." This is the COLD-BOOT path (process was killed; `isReady` gates `TabMailApp.body` on the FULL `ensureDatabaseReady()`+`loadInitialData()`). Moving DB work off-main (PLAN_HANG_FIX) did NOT shrink this — `isReady` waits the full wall-clock regardless of thread, and during the blank window there's no interactive UI to benefit. **`logmain.log` (debug build, debugger ATTACHED — so its WebKit `WebContent took 3.37s`/`GPU 4.07s` + ~3 s `[MainStall]` are debugger-INFLATED, NOT a release signal) confirmed: `schema migrations completed in 0ms`, `database ready in 181ms` — DB build is NOT the cost — yet the ~20 `duplicate column … ALTER TABLE nse_processed_message` lines fire on EVERY launch** in the untimed tail after "database ready", because `createNSEStagingDBIfNeeded` ran `CREATE TABLE IF NOT EXISTS`+20 `ALTER`s every launch behind a `busyMode=.timeout(2)` cross-process lock — which only stalls when an NSE is mid-write on a fresh push (→ the intermittent "weird timing"). **Fix (both, `AppDatabase.swift` + `TabMailApp.swift`):** (1) **version-gate** — `nseStagingSchemaVersion` const (=6) + App Group suite key `nse.stagingSchemaVersion`; `createNSEStagingDBIfNeeded` returns early (no DB open, no lock) when `fileExists && stored==current`; `createNSEStagingDB(atPath:)` now `@discardableResult -> Bool` and the marker is set only on a true apply (failure retries next launch). Gate is production-path only — the parameterized core stays ungated so unit tests (no App Group) still build a real schema. (2) **off the gate** — `dbReady`/`resumeDBWaiters()` now flip BEFORE staging upkeep; `createNSEStagingDBIfNeeded()` + `mirrorAllState()` run in a non-awaited `Task.detached(.utility)` after `dbReady`. Safe because `mergeNSEStagingData` tolerates a missing staging file and the file always pre-exists on any device that's ever received a push (a push requires a prior foreground sign-in launch, which created it); `mirrorAllState` only feeds the NSE's NEXT push and just reads main DB / writes App-Group UserDefaults (no staging dep). Verified: app builds, 71 NSE tests (6 suites incl. the `mergeNSEStagingData` main-actor-responsive probe + `v6 idempotent re-run`) green. **WebKit warm-up DEFERRED (2026-06-23, same session):** the warm-up `WKWebView` fired on a fixed **1 s timer from `TabMailApp.init`**, so on a cold boot (inbox appears >1 s in) its first cold WebContent/GPU process launch competed with cold-launch rendering — the suspected *larger* blank-screen lever. Moved out of init into `body.task`, gated on `startup.isReady` (inbox on screen) + 500 ms settle, one-shot via `TabMailApp.hasWarmedUpWebKit` (`body.task` can re-run on scene changes); now also correctly **skips cold BACKGROUND launches** (no UI → `isReady` stays false → no pointless WebKit spin-up during silent pushes). `warmUpWebKitIfNeeded()`. **CAVEAT:** whether the warm-up was actually the *dominant* blank-screen cost is STILL UNCONFIRMED — the `logmain.log` evidence was a debug build with the debugger ATTACHED (inflates WebKit process-launch times 3-4×), so validate the real win on a **Release/TestFlight** build.
- **Boot lazy-load (client-side AI tools) + the SearchIndex/Embedding priority experiment (2026-06-24).** `ToolRegistry.ensureDefaultToolsRegistered()` (idempotent) registers the 35 tools on FIRST tool use — called from `BackendClient.sendCompletionsWithTools` right before the tool-execution loop — instead of at launch. Removes the 35-tool alloc + actor hop from the post-`isReady` cold-launch CPU burst. `ToolRegistry.makeDefaultTools()` is now the single source of the tool list (was inline in `TabMailApp.init`'s boot `Task.detached`). Only agent-chat / reply-precompute / inline-edit use tools → first such request pays the one-time cost. Tests: `ToolRegistryLazyRegistrationTests`. **`SearchIndex.initialize()` + `EmbeddingService.initialize()` stay EAGER `Task.detached` at boot (original priorities):** tried lowering QoS (.utility/.background) to cut the post-inbox ~1.2 s `[MainStall]`, **REVERTED** — they're already off-main (don't block main), and `.background` risked starving FTS-readiness / embeddings under Low Power Mode for a DEBUGGER-INFLATED contention cost. Confirmed via fresh `logmain.log`: the post-inbox stall (inbox already visible) persisted ~unchanged (1357→1200 ms) after the lazy-registry, so tool-reg was a minor contributor; the residual is SearchIndex FTS open (257 k docs) + CoreML load + SwiftUI/sync churn, all post-`isReady`. Left as-is (post-visibility, debugger-inflated) — revisit only with Release-build evidence.
- **FTS writes CANNOT silently drop on an init race (verified 2026-06-24).** The scare: SearchIndex methods `guard let dbPool else { return }` → silent no-op if uninitialized. NOT a real failure mode: the PRIMARY content writers (`indexHeaders`, `updateBodies`, `removeMessages`, `removeMessagesForAccount`, `rekeyHeaders`) call `ensureReady()` (lazy `initialize()`) BEFORE the guard, so a write that beats the eager boot init SELF-INITS and writes. Completion is gated on CONFIRMED writes (`updateBodies` returns the written-id `Set`; `BodyFetchProcessor`/`NSEDataBridge` only mark `bodyComplete` for those → a no-op marks nothing → stays retryable, honoring "never mark unfetched as fetched"), and `recoverIncompleteHeaders` / `selfHealFTSBodyMembership` re-index any miss each sync. The only path to "indexHeaders wrote nothing" is `initialize()` THREW (disk/IO) → LOGGED `[SearchIndex] Lazy initialization failed`, not silent, heals on retry. **Belt-and-suspenders 2026-06-24:** added `ensureReady()` to the 5 AUGMENTATION writers that lacked it (`updateBody` single, `updateCcBcc`, `storeEmbedding`, `storeEmbeddings`, `updateFolderIds`) — they only ever touch already-indexed rows so the pool was set in practice, but now EVERY FTS write self-inits uniformly. No recursion: `ensureReady`→`initialize`→`createSchema`/`migrate*` only, none call the augmentation writers.
- **Delayed branded splash on slow cold launch (2026-06-24, shipped 1.6.14 build 316).** Cold boot is ~5-6 s on debug AND TestFlight (NOT debugger-inflated) — DB build is fast (`database ready in ~200ms`); the blank is pre-main framework/dyld load + a ~1.5 s main-thread stall (SwiftUI first-render + reactive re-evals competing with the off-main boot herd — SearchIndex 257k-doc FTS open, CoreML, ReminderBuilder, StoreKit, DeviceSync, all already off main). Can't cut much more without the QoS lever (rejected) — so `TabMailApp.body` now masks it: blank (system bg, matching empty UILaunchScreen) for the first **600 ms**, then `SplashView(mode: .loading)` if `!isReady` — a fast resume (<600 ms) never flashes it. Splash `.transition(.opacity)` fades IN (300 ms ease-in, `withAnimation` in the blank's `.task`) and OUT (300 ms ease-out, `withAnimation` wrapping `isReady=true` in `AppStartup.runIfNeeded`); `RootView` + the blank use `.transition(.identity)` so the inbox appears INSTANTLY beneath the fading splash (only the splash animates — deliberately NOT animating the inbox's ScrollViews, per the no-`.animation(_:value:)`-on-ScrollView-ancestors rule; a one-shot `withAnimation`+`.transition` is fine). Purely cosmetic — nothing gates the inbox on the animation.
- **SearchIndex FTS open DEFERRED off the first-render path (2026-06-24).** Its eager `initialize()` (the 257k-doc FTS open) fired at `dbReady` (PRE-render) and landed mid-stall in device logs (`Opening FTS database` inside the `[MainStall]` window, before `loadInitialPage`). Moved from `TabMailApp.init` → `TabMailApp.deferredSearchIndexInitIfNeeded()`, kicked from `body.task` after `isReady` (+500ms settle), foreground-only, one-shot (`didDeferSearchInit`). **SAFE, no background/data-integrity risk:** the FTS WRITE paths call `ensureReady()` (lazy init), so any earlier consumer — foreground sync indexing, and **background** push/sync (which never runs `body.task`) — self-initializes the index; completion is gated on confirmed-write return sets + `recoverIncompleteHeaders`/`selfHealFTSBodyMembership` re-index any miss, so a write can't silently drop. The deferred init only makes foreground SEARCH ready promptly without racing the paint. Verified: `Opening FTS database` now logs AFTER `loadInitialPage`. **`EmbeddingService` left EAGER `.utility` in `init` (NOT deferred):** device logs show CoreML already loads LATE (after sync/badge) because of `.utility`, so it's not a render-window contender; and making its consumer queues "wait"/self-init backfires — the embedding `repopulateFromDatabase` runs from `syncStartup` ≈ render time, so a consumer-lazy init would load the 45MB model THEN. Keeping it eager also preserves background embedding-build on cold background launches (no `body.task`). The QoS-lever approach (lowering `.utility`/`.background` on these boot inits) was tried and REVERTED earlier; gating on the post-render signal is the safe alternative.
- **Compose-dismiss path CONVERTED + firewalled (2026-06-23)** — was the "deliberately deferred" item below; a field report of a **2–3 s lag on compose dismiss (both Send AND Cancel)** forced it. The send path ran THREE sequential synchronous `@MainActor` writes before `dismiss()` (`DraftStore.save` → `queueSend` insert → `optimisticDeleteDraftHeader`), so the user ate up to 3× a contended-writer stall; cancel/discard each had ≥1. All converted to the async overload: `queueSend` is now `async` (delegates to a new drain-free `AccountManager.persistQueuedSend` single-source-of-truth), `DraftStore.saveAsync`/`deleteAsync` siblings (sync versions kept for non-critical autosave callers), `ComposeView.send`/`closeCompose`/`discardDraftAndDismiss` async + `optimisticDeleteDraftHeader` async (closure RETURNS `[String]` instead of capturing-and-mutating a `var`). **The deferral's stated double-send risk was REAL and is now closed two ways:** (1) UI guard — `send()` sets `isSending=true` (was dead `@State`, never set) after validation, before the first `await`; the guard→set critical section is await-free on the main actor so two fast taps can't both pass. (2) Persistence firewall — `persistQueuedSend` dedups inside the insert transaction on an in-flight (`.queued`/`.sending`, NOT `.failed`) row for the same `draftId` via `inFlightOutboxId`, returning the existing id. GRDB serializes writers so concurrent sends can't both pass. Tests: `OutboxDoubleSendTests` (firewall: sequential + concurrent double-tap → 1 row, `.failed` doesn't block re-send, reply double-tap sets isReplied once) + `OutboxSendMainActorBlockTests` (RED/GREEN freeze probe mirroring NSE: sync write = 0 heartbeats during a 2.5s contended write, `await persistQueuedSend` = 113). The `MEMORY.md` rule about verifying a named symbol still exists applies — `optimisticDeleteDraftHeader` is now `async`.
- **Deliberately NOT converted (cost/risk > benefit):** `InboxViewModel.fetchPage` (hot inbox path under the SwiftUI mutation-safety rules — converting risks inbox crashes; already pool-mitigated), `NavigationStore.loadInitialData` (runs pre-contention on cold launch), and the resume-path reads `InboxViewModel.flushAIBatch` + the `loadSnippetBatch`→`lookupMessage` loop (both read-then-mutate `@Observable` — an `await` between read and apply opens the reentrancy window the mutation-safety / interaction-freeze rules guard against). Converting MainActor sync *reads* to async is the path declined in **ADR-IOS-031** ("Out of scope") and again in the 2026-06-21 re-audit: the 64-reader pool is the deliberate backstop, favored over read-conversion twice for this reason. Convert these only with device-log evidence of a real block. The re-audit found NO other unmitigated main-actor *writes* on either the cold-launch or warm-resume path (the compose-dismiss writes above were the one remaining set, converted 2026-06-23).
- **Test seam:** `NSEDataBridge.mergeNSEStagingData(stagingPathOverride:)` + `AppDatabase.createNSEStagingDB(atPath:)` let unit tests (host has no app-group entitlement) drive the REAL merge against a temp staging DB. Freeze probe: count `@MainActor` heartbeat ticks during the op — a max-gap *duration* metric has a resume race (post-unblock heartbeat resume vs the test reading it → false negatives). NB: an `await clock.measure { }` wrapper hops execution off the main actor and masks the freeze; call the op DIRECTLY.
- **Message-OPEN/render freeze — same class, on the detail path (2026-06-23):** field report of a freeze "while a message is re-downloaded then rendered." The download + DB writes are off-main (`AccountManager` is an `actor`; `BodyFetchProcessor` uses `await dbPool.write`), and `loadBody`'s reads return `Sendable` structs → async overload. The two main-actor offenders are: (1) **`MessageCardView.cardUserLabels`** was a computed property running a synchronous `dbPool.read` (a `UserLabelStore.labelsForMessage` JOIN) **inside `body`** — re-executed on the main thread every render of the FOCUSED card (which re-renders several times per open as `messageBody` lands + AI summary/tag updates arrive). **Converted to async `@State userLabels` + `loadUserLabels()` (`await dbPool.read` → `[UserLabel]` is Sendable → non-blocking overload), refreshed via `.task(id: message.id)` + `.onReceive(.inboxDataDidChange)`** (the label menu posts that on apply/remove). (2) **`EmailHTMLWrapper.wrapHTML` runs SYNCHRONOUSLY on the main thread in `AutoSizingHTMLView.updateUIView:227`** (and `webViewWebContentProcessDidTerminate:426`) right before `loadHTMLString` — for full-document emails (Outlook/Exchange + most newsletters → `unwrapFullHTMLDocument` → `neutralizeCSSRules` regex over every CSS rule + 6 more regex passes) this is 100ms–1s+ of main-thread CPU on large bodies and is the likely DOMINANT half of "freeze while being rendered" (it's CPU, NOT a DB read). `wrapHTML` is a documented pure/isolation-free transform → safe to move off-main. **DONE 2026-06-23** via `AutoSizingHTMLView.Coordinator.wrapAndLoad` (shared by `updateUIView` + `webViewWebContentProcessDidTerminate`): wrap on `Task.detached(.userInitiated)`, hop back to `@MainActor` for `loadHTMLString`, gated by a monotonic `loadGeneration` token so a stale wrap (card rebound mid-wrap) can't clobber a newer load; `loadedHTML` is still set synchronously so change-detection is unaffected; reveal still handled by the existing `opacity:0`-until-`fit()` hold (no flash). The `cardUserLabels` async loader also got a `guard !Task.isCancelled` so a torn-down `.task(id:)` reload can't wipe the chips to `[]`. On-device confirm of the freeze still pending. Minor/left: `MessageDetailViewModel.resolveMessage` (init, sync — kept for instant cached-header display + read-flip) and `bodyFor` (thread cards, only on user expansion — collapsed cards never call it). Git history is squashed at the v1.6.0 public release so the exact regression point can't be pinned; the render-path sync read is not visibly *new* from the 2026-06-21/23 DB-sync work.

- **Boot/foreground "slower than before" — front-loaded concurrency + NSE mail gated behind AI. ANALYSIS + 3 FIXES IMPLEMENTED 2026-06-25/26 (interactive trace + before/after in `analysis/boot-2026-06-26/{index,cold_boot,warm_boot,nse_boot}.html` — dated snapshot folder; was `boot-analysis/`).** Distinct from the freeze class above (nothing blocks the main thread here) — the hang is gone but startup *front-loaded* a large concurrent herd and the NSE-staged-message "pops in ~10ms" behaviour was lost.
  - **FIX 1 — Gradual NSE staging (the message no longer waits for AI).** The NSE now writes the staging row in stages instead of one terminal `persistProcessedMessage`: `NSEStagingDB.stageHeader` (after metadata, UPSERT, flips `populated=1` so the merge surfaces it NOW), `stageBody` (after body fetch), `stageSummary` (after the summary LLM) — wired at `NotificationService.swift` steps 4/5/6a — then the existing terminal `persistProcessedMessage` (action + `aiCompleted=1`). `NSEDataBridge.mergeNSEStagingData` made INCREMENTAL: the existing-header branch applies body/summary/action independently (hoisted out of the old `if msg.aiCompleted` gate), and the delete decision now KEEPS non-terminal rows (`localMergedIds.append` only when `msg.aiCompleted || msg.processedAt < abandonedCutoff`) so a kept row re-merges idempotently across wakes until AI completes (then deleted) or is abandoned (NSE died >60s ago, header already durable → deleted). Split `committedCount` (any savepoint → UI refresh/recount) from the delete-list. `populated=0` orphan reap + the new abandon window share `staleStagingWindowSeconds=60`. Coordination UNCHANGED — the AI lease still prevents the main app duplicating in-flight NSE AI; the foreground-window race is benign (notification suppressed when foreground). Tests: `NSEGradualMergeTests` (lifecycle, abandoned cleanup, terminal-one-pass, + a header-only-merge speed measurement). The 5 pre-existing NSE merge suites still pass.
  - **FIX 2 — Foreground reorder (inbox first, then herd).** `SyncScheduler.startForegroundPolling` now does a ONE-SHOT `await Task.yield()` + `Task.sleep(SyncConfig.firstForegroundSettleSeconds=0.35)` on the FIRST foreground per process (instance flag `didFirstForegroundSettle`) so the cold-launch SwiftUI first render paints before the merge+sync herd starts. Warm returns skip it.
  - **FIX 3 — NSE→inbox merge is now a PRIVILEGED, single-threaded boot step (2026-06-26; follow-up to the de-scoped items, snapshot `analysis/boot-2026-06-26/`).** Root cause of the residual "sometimes 3–5s" merge→render lag AND the slow notification-tap deep link: (a) **visibility gate** — the inbox query filters `headerComplete == true` (`InboxViewModel.fetchFullRange`) but a brand-new merged header is inserted `headerComplete=false` and only flipped inside the *detached* `flushNSEBatchToFTS`, which posted NO reload signal → the row stayed invisible until some unrelated later `.inboxDataDidChange`; (b) **uncoordinated merges** — 6 call sites each opened the staging DB + fought the 2s/5s busy lock + the GRDB writer concurrently with the herd; the deep-link tap `await`s the merge on its nav critical path (`MailNavigationView.swift:647` — its post-merge lookup is NOT `headerComplete`-gated), so that contention IS the tap delay. **The fix (4 parts):** (1) **`NSEMergeCoordinator`** (new `actor`, file-scope in `NSEDataBridge.swift`) — serial task chain; all callers funnel into one in-flight run, never two at once. `mergeNSEStagingData` is now a thin wrapper; the work moved to `performMerge`. (2) **synchronous FTS flush** — `performMerge` now `await`s `flushNSEBatchToFTS` (was a detached `Task`) so `headerComplete=1`/`bodyComplete=1` are set BEFORE it signals (safe to await because the merge runs alone — nothing contends the GRDB/FTS writers). (3) **ONE end-of-merge post** — removed the mid-merge `.inboxDataDidChange` posts (committed-message branch + `mergeInboxRemovals`); a single post fires at the very end when `didMutate`, carrying `Notification.Name.inboxReloadImmediateKey=true`. (4) **immediate bypass on the consumer** — `InboxViewModel`'s listener reads that key and reloads AT ONCE via a new single-flight `runReloadCoalesced()` (shared with the throttled path so the immediate + debounced paths never run `reloadMessages` concurrently); the 500ms debounce is KEPT for the noisy background producers only. Herd ordering was already correct (foreground `await`s the full merge before `syncStartup` spawns sync/backfill/AI). **Audit (2026-06-26):** no correctness bugs — verified coordinator predecessor-capture/tail-clear, no actor deadlock (Task body is nonisolated), single-flight reload never loses a signal nor overlaps `reloadMessages`. Two deliberate trade-offs: (a) the deep-link `await`s the synchronous flush when it's the merging pass (negligible per-message; bounded by staged count); (b) a task-result-only merge now fires one no-op inbox reload (rare; also refreshes reminders). **New regression guards (all green):** `NSEGradualMergeTests.concurrentMergesSerialized` (coordinator exactly-once under 5 concurrent), `mergeFlipsHeaderCompleteSynchronously` (visibility — `headerComplete`/`bodyComplete` true BEFORE merge returns; catches a revert to detached flush), `mergePostsSingleImmediateSignal` (exactly ONE post, immediate-flagged), `InboxViewModelBackgroundChangeDirtyBit.immediateSignalBypassesDebounce` (<250ms vs 500ms floor) + `immediateDuringReloadNotLost` (single-flight coalesce). Plus the pre-existing dirty-bit + NSE-main-actor-block suites; full app build green.
  - **FIX 4 — `PriorityGate` (cooperative quiescence) makes the merge ACTUALLY privileged against the background herd (2026-06-26).** Diagnosed from `logmain.log`: a warm-foreground merge of a pushed email took **3185ms** (`mergeNSEStagingData: DONE in 3185ms`) because a deep **backfill** (historical Archive body-fetch) was mid-flight from a prior background cycle — back-to-back 50-item batches whose `process=` step holds the GRDB + FTS writers and saturates the cooperative pool. FIX 1-3's "merge before herd" only gates THIS foreground's `syncStartup`; it does nothing to a backfill already running, and the synchronous FTS flush put FTS contention on the merge's path too. **Root-cause fix:** `TabMail/Services/PriorityGate.swift` — an `actor` with count-based `begin`/`end` + `privileged { }` and a `yield()` that background loops call at SAFE batch boundaries (park while privileged, resume the instant it clears). The merge runs inside `PriorityGate.privileged { performMerge }` (in `NSEMergeCoordinator`). `yield()` wired into every heavy background loop: `BackfillBodyQueue.dispatchBatch` (top) + right before its `flushBatch` write, `BackfillAIQueue.dispatchBatch`, the 3 embedding queues' `processBatch` (Active/Backfill/BackfillMemory), and the `SyncEngineBackfill` header walk (next to `getShouldPauseBackfill`). **Why cooperative (not QoS / GRDB priority):** SQLite serializes writers FIFO and can't preempt an in-progress txn, the cooperative pool can't be reserved, and QoS only deprioritizes (tried + reverted) — so the privileged path raises a flag and loops yield at boundaries (the GRDB/FTS analogue of the IMAP priority lock ADR-IOS-014 / user-activity prioritization ADR-IOS-002). **Bound:** a batch already mid-flight runs to its next boundary, so the merge waits ≤ ~one batch (was the whole backfill run). **No deadlock:** the merge never calls `yield()` and never awaits a parked task. **Gotcha:** `privileged` is STATIC — an instance generic `privileged<T>` tripped Swift 6 `sending`/non-Sendable-result across the actor boundary (`body` + `T` must stay in the caller's isolation; only `begin`/`end` cross in). Tests: `PriorityGateTests` (5 — no-op-when-clear, park/resume, count-based nesting, multi-waiter, static-helper bracketing). New file → ran `./Scripts/xcodegen.sh`.
  - **FIX 5 — `PrioritizedDatabase` facade: priority is now ENFORCED at the DB-access chokepoint, not hand-placed (2026-06-26, per user "make it uniform / wrap all DB calls in a class").** `PrioritizedDatabase` (struct in `PriorityGate.swift`) wraps the GRDB pool and mirrors its exact `write`/`read`/`writeWithoutTransaction` overloads (this GRDB build uses `T: Sendable`, NOT `sending T` — match it or 530 sites won't resolve). The ONLY behavioral delta vs the raw pool: an ASYNC write at `.background` QoS first `await`s `PriorityGate.shared.yield()`. `AppDatabase.dbPool` now returns the `.normal` wrapper (behaves identically to the old raw pool — zero behavior change at the ~530 `.write`/`.read` call sites, which compiled UNCHANGED); `AppDatabase.backgroundPool` is the `.background` variant; `AppDatabase.rawPool` is the raw `DatabasePool` for GRDB APIs that need it (`ValueObservation.publisher(in:)`, `any DatabaseReader/Writer` params — ~6 sites switched to `rawPool`). Helper params that thread a pool (`ChatStore.evict*`, `DraftStore.evictSync`, `SyncEngineMaintenance.run*`, `QueueStorage.retryWrite`, `SyncEngineFullSync.runSyncMessages`) were retyped `DatabasePool`→`PrioritizedDatabase` so priority propagates; read-only `ThreadDetection` kept `DatabasePool` (reads never gate) with its one production caller passing `pool.pool`.
  - **FIX 5b — collapsed to a TWO-tier model: privileged + normal (2026-06-26, per user "make the merge priority and everything normal; three-tier isn't worth it").** Dropped `.background`/`backgroundPool`/the `QoS` enum. Now: `PrioritizedDatabase.write` (async) ALWAYS `await`s `PriorityGate.shared.yield()`, and `yield()` is a no-op unless a privileged section is active AND the caller isn't it. "The caller isn't it" = a `@TaskLocal static var inPrivilegedContext` that `PriorityGate.privileged { }` sets true for the merge's task tree (via `$inPrivilegedContext.withValue(true, operation: body)`); `yield()` early-returns when it's set, so the merge's OWN writes never wait on the gate it holds (deadlock guard — see `PriorityGateTests.privilegedContextExemptFromYield`). **Net:** EVERY normal write yields to the merge automatically — no per-subsystem classification to forget (the thing the user disliked); `AppDatabase.dbPool` is the one accessor, `rawPool` stays for GRDB APIs. **Why two-tier is fine (three-tier's only edge — interactive writes not waiting for the merge — is invisible):** interactive writes are optimistic-UI (row animates away instantly, the write is already async/queued), so a normal write briefly yielding to a fast merge can't be seen. The three redundant FIX-4 top-of-batch yields (`backfill-body`/`backfill-ai`/`backfill-header`) were REMOVED — their GRDB writes auto-yield through `dbPool` now. KEPT: the FTS/memory **sidecar** yields (`BackfillBodyQueue` before its `flushBatch`, the 3 embedding `processBatch`es) — `SearchIndex`/`MemoryIndex` are separate pools the main wrapper can't see, so their background callers still yield explicitly before invoking them. Verified: full app build clean (no warnings), 24 tests across PriorityGate (incl. the deadlock guard) / NSEGradualMerge / InboxDirtyBit / RetryWrite green. **GRDB note:** the wrapper mirrors GRDB's exact `write`/`read`/`writeWithoutTransaction` overloads — this build uses `T: Sendable`, NOT `sending T`; match it or the ~530 call sites won't resolve. Test seam: tests passing a raw `DatabasePool` to a retyped param wrap it (`PrioritizedDatabase(pool:)`); `AppDatabase(dbPool:)` + `MessageDetailViewModel(dbPool:)` inits kept raw-pool params (wrap internally).
  - **FIX 6 — merge as a read-through delta + CoreML pinned to first paint (2026-06-26, per user "have them already merged by the time the app renders" + "merge at the read, not per-path").** Two parts: (a) **read-through merge** — `PrioritizedDatabase.read` (async) now `await`s `NSEDataBridge.mergeIfStagingPending()` before every app read, so EVERY consumer (UI render, silent push, BGAppRefresh, BGProcessing) drains staging without per-call-site placement; `AppStartup.runIfNeeded` also merges once BEFORE `loadInitialData`/first paint so the new mail is in the very first inbox draw. `mergeIfStagingPending` is a drift-free cheap probe: a CACHED App-Group `DatabaseQueue` (`Mutex<DatabaseQueue?>`) runs `SELECT EXISTS(... WHERE populated=1)` against the REAL staging DB — NO dirty-bit flag (the earlier `nse.stagingDirty` UserDefaults flag was REMOVED; cross-process flags drift, and the EXISTS read can't). Recursion-guarded by `PriorityGate.inPrivilegedContext` (the merge's own reads skip it). (b) **CoreML regression + fix** — putting the merge before first paint exposed that `EmbeddingService.initialize()` (CoreML ~45MB, `.utility`) was only **`dbReady`-gated** (`awaitReady()`), NOT first-paint-gated. That was SOFT: it *empirically* landed after paint because the fast inbox render won the CPU race — true ONLY while first paint was fast. With the merge now pre-paint, a contended merge stretches the pre-paint window; `logmain.log` (debug cold boot, NSE message pending) showed the 11s CoreML load running CONCURRENTLY with the ~50ms merge, starving it to **10.5s → first paint at 17.6s**. The `PriorityGate` couldn't help — a CoreML load isn't a DB write. **Fix:** new `AppStartup.awaitFirstPaint(timeoutSeconds:)` — a HARD gate (polls `isReady` at 100ms, or a timeout for cold BACKGROUND launches that never paint). The EmbeddingService boot task is now `awaitReady()` → `awaitFirstPaint(3)` → `initialize()`. Acyclic: the merge doesn't need CoreML, CoreML waits on `isReady`, `isReady` waits on the merge — so the pre-paint merge runs UNBURDENED (fast), paints with the message, then CoreML loads. This realizes the "post-render signal gating" that the EmbeddingService bullet above (2026-06-24) already named as the safe alternative to the rejected QoS lever. The common fg case (no NSE pending) is unchanged except CoreML now loads a touch later — exactly the documented expectation, now GUARANTEED not soft. Not unit-tested (real-time boot ordering, not an API surface); the gate logic is a trivial poll-or-timeout.
  - **FIX 6b — the WHOLE foreground herd now gates on first paint, not the DB / a fixed timer (2026-06-26, per user "everything should be blocked on boot until first render — these can wait").** Generalized the FIX-6 principle: `awaitFirstPaint` is the single signal for "deferrable boot work waits for the inbox to paint." `SyncScheduler.startForegroundPolling` previously held its herd (NSE merge, sync, queue repopulation, push resub, poll) behind a FIXED `firstForegroundSettleSeconds=0.35` timer (FIX 2) — a guess, not the real signal. Now it `await`s `AppStartup.shared.awaitFirstPaint(timeoutSeconds: SyncConfig.firstForegroundPaintTimeoutSeconds=8)` on the first foreground per process (`didFirstForegroundSettle` gate; warm returns skip it — `isReady` already true → returns at once). `firstForegroundSettleSeconds` deleted. Safe because nothing in the herd is needed to draw the first frame: cached mail is already in GRDB, and NSE-staged mail merged PRE-paint via runIfNeeded's read-through (or via `loadInitialData`'s sidebar read, which also drains staging through `PrioritizedDatabase.read`), so the post-paint `mergeNSEStagingData` at the top of the herd is just a redundant re-drain. The 8s timeout is a pure safety net — `startForegroundPolling` only runs on a foreground scene activation, so `isReady` always flips. **Audit of what's already correctly placed:** EmbeddingService → `awaitFirstPaint` (FIX 6); SearchIndex + WebKit warm-up → `body.task` after `isReady` (2026-06-24); ToolRegistry → lazy on first tool use; `RootView.task` boot work (DeviceSync connect, ReminderBuilder, `revalidateAISubscriptionGate`) → already post-paint because `RootView` only exists once `isReady` flips. **Left on `dbReady` (`awaitReady`) DELIBERATELY — NOT herd:** the background entry points (`AppDelegate` push / notification-action / silent-push, `SyncScheduler.handleBackgroundSync`/`handleBackgroundAIProcessing` BGTasks, `PushNotificationService`) — they fire on cold BACKGROUND launches that never paint, are the time-critical reason the app woke (budget-bound), and must run ASAP; gating them on a paint that never comes (or its 8s timeout) would waste their budget. The rule is "deferrable FOREGROUND herd waits for paint," not "everything waits for paint."
  - **FIX 6c — consolidated the two launch-gate waiters into ONE (2026-06-26, per user "single function with a bool for background-or-not").** `awaitReady()` (dbReady) and `awaitFirstPaint(timeoutSeconds:)` (first paint) — referenced in FIX 6/6b above under their OLD names — were merged into **`AppStartup.awaitLaunchReady(background:firstPaintTimeoutSeconds:)`**. `background: true` == old `awaitReady` (resumes at `dbReady`); `background: false` == `ensureDatabaseReady()` THEN poll `isReady`-or-timeout (so a foreground waiter is also guaranteed a usable DB on the timeout path — a small correctness gain over the old standalone `awaitFirstPaint`). All ~9 call sites migrated: background entry points (`AppDelegate` ×4, `PushNotificationService`, `SyncScheduler.handleBackgroundSync`/`handleBackgroundAIProcessing`) → `background: true`; the foreground herd (`startForegroundPolling`) and the CoreML load (`TabMailApp.init`) → `background: false`. **Timeouts moved to config (per user "not hardcoded"):** `SyncConfig.firstPaintGateTimeoutSeconds=8` (herd safety net, the `awaitLaunchReady` default) and `SyncConfig.embeddingLoadGateTimeoutSeconds=1` (CoreML load's background fallback — was an arbitrary 3s; cut to 1s, sized just above the unburdened merge's worst case incl. a cold first FTS open, since on FOREGROUND the embedding resumes at first paint and the timeout is irrelevant — it ONLY affects cold background launches). Build + the 19 merge-path regression tests green.
  - **FIX 6d — adversarial audit fixes (2026-06-26, after committing 4a32d04).** An independent review of the boot/merge work found three issues; all fixed: (1) **read-through probe could stall reads** — `PrioritizedDatabase.read` probes staging before EVERY async read, and the staging DB is the NSE's NON-WAL shared file (`AppDatabase.swift:204`, ADR-IOS-041), so a `.timeout(1.0)` reader could block up to 1s behind an NSE commit, per read. Fixed: probe connection now `busyMode = .immediateError` (fail-fast, never stalls — a false negative is re-probed/merged by the explicit triggers, row stays populated=1) AND probes are COALESCED to ≤1 per `SyncConfig.stagingProbeCoalesceMs=100` (`lastProbeMs` Mutex), so frequent reads don't each hit the cross-process file. (2) **cancellation busy-spin** — `awaitLaunchReady(background:false)`'s `try? await Task.sleep` loop would burst through `limit/step` no-op iterations on cancellation; added `if Task.isCancelled { return }` + `do/catch`. (3) **priority-inversion taint** — `performMerge` enqueues ActiveAI/ActiveEmbedding INSIDE `privileged{}`; `enqueue` spawns the drain via unstructured `Task {}` which COPIES the `inPrivilegedContext` task-local, so a drain first kicked from a merge inherited the merge's exemption and never yielded to a LATER merge (defeating even the embedding queue's explicit sidecar yields). Fixed by wrapping the enqueue loop in `PriorityGate.$inPrivilegedContext.withValue(false)`. Guard: `PriorityGateTests.resettingPrivilegedContextReExposesToGate`. Audit verdicts that held (NOT bugs): merge recursion guard, deadlock-freedom, merge coalescing/no-lost-update. **This taint is the likely cause of a 939ms warm-return merge seen in logmain.log** (merge of 2 msgs dispatched ActiveEmbed/ActiveAI mid-merge, which then contended with its synchronous FTS flush instead of yielding).
  - **WARM-RETURN gap is inherent, distinct from cold boot (2026-06-26).** "Saw the inbox without the new email briefly" on a FOREGROUND RETURN (`startForegroundPolling enter (firstForeground=false)`): the inbox is ALREADY painted with the cached messages, and the runIfNeeded "merge before first paint" only applies to COLD launches (a warm return has no first paint to gate). So a pushed message that arrived while backgrounded appears only AFTER `startForegroundPolling`'s merge completes + the immediate reload (e.g. 11→13). The gap == merge duration, so the lever is keeping the merge FAST (→ FIX 6d's taint fix). The cold-boot path already shows it pre-paint.
  - **VERIFIED ASSUMPTIONS (per user 2026-06-25):** NSE AI timings typical = summary 4–8s, action 1–2s (both 12s timeout, `NSEConfig.summaryTimeoutSeconds`/`actionTimeoutSeconds`, `actionVoteCount=1`) → NSE delivers ~10–16s, not the 30s budget. **The NSE computes summary + action ONLY — NOT reply** (confirmed in `NotificationService.swift`; reply precompute is a main-app `ActiveAIQueue` step, `system_prompt_reply` with tools, the slow 4–8s+ one that can time out). If reply should also be staged by the NSE (4th gradual stage, timing out to main app), that's an unbuilt follow-up. `MessageBody.id` IS the headerId (1:1) — no separate `headerId` column.
  - **DEFERRED as overkill (user de-scoped) — STILL OPEN after FIX 3:** collapsing the two foreground sync passes (`backgroundSyncAll` + `foregroundSyncAll`), whoami ×3–4 de-dupe, taking `recoverIncompleteHeaders` off the critical path, staggering the 6 queue repopulations, parallelizing the NSE AI cache probe with the body fetch. (The 500ms debounce bypass and the duplicate inner `mergeNSEStagingData` cost are now DONE — see FIX 3 above: the merge posts one immediate signal, and `NSEMergeCoordinator` coalesces the duplicate call.) Original findings retained below for if/when those are picked up. **Two root findings:** (1) **NSE mail is invisible until ALL its AI finishes.** The foreground merge reads `SELECT * FROM nse_processed_message WHERE populated = 1` (`NSEDataBridge.swift:281`); `populated=1` is written ONLY by `NSEStagingDB.persistProcessedMessage` (`NSEStagingDB.swift:55`, literal `1` in the VALUES list) which runs in NSE **step 7 — AFTER** summary+action LLM (`NotificationService.swift:503/521`, ≤24s). `AIOwnershipLease.ensureRow`/`tryClaim` placeholder keeps `populated=0`. So foregrounding while the NSE is mid-AI ⇒ merge sees nothing even though header+body were fetched ~5s in; the message only appears when the NSE finishes or the app's own (slower) sync re-fetches it. **Fix = split the NSE staging write:** INSERT header+body with `populated=1, aiCompleted=0` right after body fetch, UPDATE AI columns when they land (merge already fills AI incrementally via `flushAIBatch`; AI lease still prevents dup spend). (2) **The foreground herd.** `scenePhase .active` → `startForegroundPolling` (`SyncScheduler.swift:345`) fires, in the first ~1–2s and concurrent with the ~1.3s SwiftUI `[MainStall]`: `mergeNSEStagingData` **twice** (`:366` outer + `:173` inside `syncStartup`), **two** full sync passes (`backgroundSyncAll` via `syncTask`/`:254` + `foregroundSyncAll` via `poll()`/`:682`), **6** queue repopulations (`:192` taskgroup), `recoverIncompleteHeaders` on the critical path (`:183`), push re-subscribe (`:382`), +5s FTS self-heal (`:228`), and `revalidateAISubscriptionGate` **×3–4** (RootView `:531/:550` + `backgroundPoll :641` + `poll :714`). Plus a **500ms inbox reload debounce** (`InboxViewModel.swift:389`) delays even a successfully-merged row from drawing. **Ranked fixes:** split NSE write (#1) ▸ defer non-essential herd ~1–2s past first paint (#2) ▸ bypass the 500ms debounce for merge-INSERTs (#3) ▸ collapse the 2 sync passes (#4) ▸ de-dupe whoami + skip the inner merge on the fg path (#5) ▸ take `recoverIncompleteHeaders` off the critical path + stagger the 6 repopulations (#6) ▸ run the NSE AI cache probe in parallel with the body fetch (#7). All timings are code-trace estimates — confirm on Release via `BackgroundSyncLogger` `fgReturn[+Nms]`/`syncStartup[+Nms]` + `MainActorStallDetector` + NSE `DELIVERED (Nms)` os_log.
  - **FIX 7 — header visibility DECOUPLED from body indexing in the NSE merge (2026-06-28, commit `07ebeb5`).** The reported residual "1–3s minor cases" where a pushed message doesn't appear right after foreground was NOT a merge race — the merge is well-guarded (coordinator/PriorityGate/synchronous-flush/one-immediate-signal, FIX 1–6d). Root cause: `flushNSEBatchToFTS` flipped `headerComplete=1` (the inbox visibility gate — `InboxViewModel.fetchFullRange/fetchPage` filter `headerComplete == true`, `:794`/`:916`) ONLY for messages in the body-text FTS batch (`ftsBatch`, built in `persistRenderedBodyFromStaging` for `!text.isEmpty && !hasUnresolvedCIDs`). So **image-only / empty-text / unresolved-CID / NSE-fetch-failed** pushes merged into GRDB but stayed `headerComplete=0` → invisible until a later `recoverIncompleteHeaders` pass (`SyncEngineFTS.indexHeadersForFTS`, which posts NO `.inboxDataDidChange`) → appears only on some unrelated later signal. **Plaintext was always visible** (always entered `ftsBatch`) — that's the tell. **Fix:** `performMerge` now collects EVERY committed header id (`allMergedHeaderIds`; existing-branch = `row["id"]`, new-branch = `MessageIdentity.headerId(...)` == `MessageHeader.id` by construction), appended only after the per-msg savepoint releases. `flushNSEBatchToFTS(headerIds:bodyItems:)` indexes + flips `headerComplete=1` for the FULL set (visible NOW via the merge's existing immediate signal); `updateBodies` + `bodyComplete=1` stay scoped to the bodied subset. Invariants held (index-then-flip; `bodyComplete` only on confirmed body writes — never mark unfetched as fetched). **Side benefit:** body-less rows now match the body queue's `headerComplete=1 AND bodyComplete=0` repopulate filter (`ActiveBodyQueue.swift:80`) → background body fetch (CIDs resolved main-app-side), softening the on-open re-fetch too. AI queue is gated on `bodyComplete=1` (`ActiveAIQueue.swift:169/498`) so body-less rows do NOT trigger premature AI. Unread badge already ignored `headerComplete` (counted body-less as unread while hiding them) → fix makes badge & list consistent. Tests: `NSEGradualMergeTests.mergeFlipsHeaderCompleteSynchronously` (rewritten — it PINNED the old `headerComplete==false`) + new `unresolvedCIDMergeStillVisible`. Adversarial audit: no correctness bug.
  - **FIX 8 — PriorityGate INVERSION (foreground/UI is priority by default; the heavy queues opt out) + a foreground herd-launch settle (2026-06-29).** Symptom: on a QUICK-OPEN (user opens right after the push, before silent-push/BGAppRefresh drains staging) the pushed message still lagged 1–5s AND the **unread badge updated late**. Root cause: `PriorityGate` only privileged the NSE merge, so background DB writes yielded to the *merge* but a UI write — the **badge recount, the foreground inbox reload, an optimistic mark-read** — was NOT priority and queued behind an in-flight background batch in the single-writer FIFO. FIX 5b had deliberately NOT privileged interactive writes ("yielding to the *fast merge* is invisible") — but never considered a UI write waiting behind BACKGROUND writes. The "reply-WIP shimmer the instant the message appears" is correct state (`showShimmer = actionTag != nil && cachedReply == nil`; the NSE computes summary+action but NEVER reply), not a race — it was the *tell* that the heavy reply-precompute herd had just been kicked. **Two parts:**
    - **(1) Inversion** — moved the priority line from "merge vs. everything" to "**foreground/UI (priority) vs. tagged background queues**" (still two tiers, per the user's explicit "three-tier isn't worth it"). `PriorityGate` (`TabMail/Services/PriorityGate.swift`) gains: `inBackgroundContext` task-local (mirror of `inPrivilegedContext`); a separate `priorityCount` (so `isPrivileged` keeps meaning "a MERGE is in flight" — existing tests intact) + `beginPriority`/`endPriority`; `priority { }` (UI bracket — raises the gate, does NOT set `inPrivilegedContext`) and `background { }` (sets the task-local) helpers; `isGateRaised`. `yield()` now parks while `gateRaised` (privileged OR priority). `PrioritizedDatabase` gains a stored `isBackground` flag; its async `write`/`writeWithoutTransaction`: a BACKGROUND write (`isBackground` OR `inBackgroundContext`, and not the privileged merge) `yield()`s; the privileged merge writes straight through (`inPrivilegedContext`); **everything else — the DEFAULT (UI/badge/reload/sync/optimistic) — brackets in `priority { }` so background yields to it.** `AppDatabase.backgroundPool` = the background-tagged wrapper (re-introduces the 5b-dropped variant, now justified). **Tagged background:** `ActiveAIQueue` + `BackfillAIQueue` (computed `dbPool` prop → `backgroundPool`); `ActiveEmbeddingQueue` + `BackfillEmbeddingQueue` (their one `embeddingComplete` write → `backgroundPool`); `BackfillBodyQueue` (prop → `backgroundPool` + its inline PayloadTooLarge write, AND the **shared** `BodyFetchProcessor.process`/`flushBatch` calls wrapped in `PriorityGate.background` — because that helper is ALSO used by the priority on-demand / `ActiveBodyQueue` fetch, so it can't be blanket-tagged); `SyncEngineBackfill`'s 3 async reset/reindex writes → `backgroundPool` (the walk's batch inserts are SYNC pass-throughs gated by the existing boundary `yield()`s). `BackfillMemoryEmbeddingQueue` needs NOTHING (writes only to `MemoryIndex`, a sidecar pool, already yields). **The background loops' existing boundary `yield()` calls now yield to UI for free** (gateRaised covers priority too). The user-opened DIRECT path (`processOpenedMessage`→`processMessage`) is NOT through `ActiveAIQueue`, so it's never gated. Two-tier consequence: a UI write does NOT yield to the merge (priority peers, contend ~one txn on the single writer) — accepted; the merge stays uncontended by BACKGROUND (the bulk), which is what FIX 4/6d protected.
    - **(2) Foreground herd-launch settle** — `flushNSEBatchToFTS` step 6 (the reply-precompute + embedding enqueue) is now a DETACHED task that, on a FOREGROUND real-app merge, awaits first paint (`AppStartup.awaitLaunchReady(background:false)`) + `SyncConfig.nseMergeHerdSettleSeconds = 1.0` before enqueueing — so the freshly-surfaced message RENDERS before the herd's NON-DB cost (SSE/tool reply LLM + CoreML embedding) the gate can't see. Gated on `AppStartup.dbReady` (exposed `private(set)`): true at any production merge time (the merge needs the DB), FALSE in unit tests (never boot AppStartup) → tests skip the gate, enqueue immediately (old behavior), never trigger `ensureDatabaseReady`. Background merges (silent push/BGAppRefresh, `DatabaseSuspension.isAppActive == false`) also enqueue at once — precompute-before-open is the whole point (why the non-quick-open path "works great"). Resilience: a dropped detached task → `ActiveAIQueue.repopulateFromDatabase` re-discovers on the next foreground poll. **Tests:** 5 new in `PriorityGateTests` (priority parks background = the badge fix; priority+privileged compose; priority≠privileged; static `priority{}`; `background{}` task-local). Verified: app build + `build-for-testing` green; `PriorityGateTests` (12) + `NSEGradualMergeTests` (8, incl. `mergeFlipsHeaderCompleteSynchronously`/`mergePostsSingleImmediateSignal`/`concurrentMergesSerialized` — the deferred enqueue did NOT break the merge's synchronous header-visibility/single-signal invariants) all pass.
  - **FIX 9 — app-icon badge updates immediately + survives a quick background on read/unread (2026-06-29).** Symptom (user, OOO): "read an email in the foreground then quickly background → the unread BADGE is slow to decrement; the in-list read-mark is fine." Root cause: `markRead`/`markUnread` (`AccountManagerActions`) decremented `folder.unreadCount` in the optimistic txn but only triggered the **debounced** recount (`requestRecount` without `notifyImmediately` → `unreadRecountDebounceSeconds=0.2` + chained `Task`s + a `performRecount` write + `updateBadge` read + `setBadgeCount`), and NONE of it was protected by a background-task assertion — so the chain raced iOS suspension and `setBadgeCount` never ran; the badge stayed stale until the next foreground recount. **Fix:** (1) `markRead`/`markUnread` pass `notifyImmediately: true` (the optimistic write already adjusted `folder.unreadCount`, so the badge can update NOW, not behind the debounce); (2) new `UnreadCountManager.updateBadgeProtected()` wraps `updateBadge()` in a UIKit `beginBackgroundTask` assertion (atomic `Mutex` for the expiration/`defer` race, mirroring `ActiveAIQueue.executeJob`) so the read + `setBadgeCount` complete across the quick background. `recordRecentUnreadForNSE` stays `.active`-gated so no risky staging connection opens on the way out (ADR-IOS-041). Build + 134-test write-path audit green; the actual quick-bg badge behaviour (system badge API + real suspension) needs on-device confirmation. **INVESTIGATION CONTEXT (the broader "1–5s lag" hunt):** logs showed the local path is fast (NSE merge 57ms, header surface ~400ms, badge recount 200ms) — the genuinely multi-second item is the network-bound AI (reply LLM ~4s, worse on a poor OOO network) + a confirmed cancel-race (`syncStartup` step0 merge enqueues the reply, step1 `cancelAllInFlight` cancels it → retry + 2× LLM, `SyncScheduler.swift:173/178`) — now MITIGATED by FIX 8's herd-settle: the FOREGROUND merge's reply enqueue is deferred past first-paint+settle, so it lands AFTER `cancelAllInFlight` (step1) and AFTER the `repopulate` re-enqueue (step3, which was already post-cancel). Every fresh AI enqueue now happens after the cancel, so the merge's reply is no longer cancelled→retried. Pending on-device confirmation; an order-independent `cancelAllInFlight` spare-fresh guard was considered but de-scoped (needs fg/bg discrimination + partial-cancel accounting, and the deferral already orders it correctly). Boot-to-first-paint is network-free (audited: `AppStartup.ensureDatabaseReady`/`loadInitialData` are local; the `/whoami` consent check is `!hasCompletedConsentGate`-gated so skipped for established users). Cooperative-pool starvation is NOT reproducible in the simulator (`CooperativePoolStarvationTests`: cpu-busy/network-await/blocking all ~0ms — macOS gives the pool too much thread headroom; needs a device).
  - **FIX 10 — chunk `insertBackfillBatch` into async `backgroundPool` transactions (2026-06-29).** Completes FIX 8: `insertBackfillBatch` (`SyncEngineBackfillDeep.swift`) was the one big writer that couldn't yield to UI — a SYNC write on the priority pool inserting up to `backfillChunkSize` (500–1000) headers in ONE transaction (~100s of ms), so a priority/UI write (badge recount, NSE merge) arriving mid-batch waited the WHOLE batch (SQLite can't preempt an in-flight txn; the contention mock measured 56ms for a 2000-row batch vs 7ms chunked). Now **async**, split into `SyncConfig.backfillInsertChunkSize=100`-row transactions, each via `AppDatabase.backgroundPool` (yields to priority BETWEEN chunks). Invariants preserved: pending-op snapshot + existence check stay INSIDE each chunk's write txn (TOCTOU — a user action mustn't slip a PendingOperation between a separate read and the insert); dedup holds across chunks (each chunk commits, so a later chunk's existence check sees earlier inserts); `markParentsReplied` per-chunk (idempotent). The `@Sendable` async write requires the closure to RETURN accumulated values (no capture-mutate) → `FTSHeaderRecord` made `Sendable`. Per-chunk txns mean partial progress survives a mid-batch failure (idempotent — re-fetch dedups). `insertBackfillBatch` is now `async` → 5 callers gained `await` (`SyncEngineBackfillWalk/Deep/SelfHeal`). Tests: `InsertBackfillBatch{Dedup,ReplyDetect,FTS}` + backfill-state suites (37) green. **NOTE:** the measurement PROVED DB-write contention is tens-of-ms (NOT the 1–5s lag — that's network-bound AI, see FIX 9) — this is a cleanliness/robustness completion of the inversion, not the lag fix.
  - **FIX 11 — parallelize boot account-connect + connectivity-dependency audit (2026-06-29).** The cold-boot PAINT and the cached/NSE-merged DISPLAY are network-INDEPENDENT — audited: `runIfNeeded` = `ensureDatabaseReady`/`mergeNSEStagingData`/`loadInitialData`, ALL local; `NetworkMonitor.start()`/`connectAccount` are post-paint (`RootView.task`); the inbox + `NavigationStore` do NOT gate on `isConnected`; the ONLY display-path connectivity check (`MailNavigationView.swift:180 && isConnected`) correctly HIDES an offline re-consent CTA. The OOO-specific "slow to go live / get new mail" is post-paint `connectAccount` (IMAP/Gmail login, `withTimeout(SyncConfig.connectTimeoutSeconds=15)` — a CAP, not typical: a dead network fails in seconds, only a flaky-but-present one drifts to 15s; `provider.connect()` likely has its own shorter timeout) run in a **SERIAL loop** (`RootView` connect `.task`) → N accounts = up to N×timeout before ANY provider was live, and sync (needs a connected provider) waited that long. **FIX:** wrap the connect loop in `withTaskGroup` — `AccountManager` is an `actor`, so per-account bookkeeping serializes while the `provider.connect()` network waits OVERLAP (reentrancy) → providers come up in ~one timeout total, new mail flows as the first connects. Background connectivity gates (sync/AI/backfill/queue draining via `NetworkMonitor.checkConnected`) correctly DEFER when offline (post-paint, don't block display). NOTE: `checkConnected()` defaults `true` before the first `NWPathMonitor` update, so background ops briefly assume connected at boot (self-corrects ~1s; doesn't affect display). **The "merged outcome shows late" symptom is NOT connectivity** (display is local) — STILL OPEN, needs a fresh on-device merge-timing capture (`merge DONE → first-row-visible`) to pin whether it's the merge trigger or the signal→reload path.
  - **FIX 12 — pre-paint NSE merge is now FAIL-FAST (cold-boot regression fix, 2026-06-29).** Symptom (user): cold boot was "super fast in previous releases", now slow OOO ("didn't notice at home" → network-correlated). Root cause: **FIX 6** added an UNCONDITIONAL, BLOCKING pre-paint merge to `AppStartup.runIfNeeded` (`await mergeNSEStagingData()` BEFORE `isReady`) so a pushed message is in the first frame. On a SLOW network the NSE's AI (backend LLM) runs long → the NSE (+ its 1Hz lease heartbeat) is STILL writing the NON-WAL staging file when the user cold-opens → the pre-paint merge's staging read blocks up to the busy timeout (`performMerge`'s `for timeout in [2.0,5.0]`) → delays FIRST PAINT. Recent (FIX 6) + network-correlated (NSE-AI duration ∝ network) = fits "fast before / not at home." **FIX:** `runIfNeeded` now calls `mergeIfStagingPending()` (the FAIL-FAST probe path, `busyMode=.immediateError`) instead — if the NSE holds the lock the probe returns "not pending" and the pre-paint merge SKIPS rather than blocks; the post-paint read-through merge picks the row up the instant the NSE frees the file (row stays `populated=1`, nothing lost). Common fast path (staging readable → NSE done, e.g. good network) KEEPS FIX 6's "message in first frame"; a busy NSE no longer regresses cold-boot. Same principle as FIX 6d ("a probe must never make a read wait"), now extended to the pre-paint merge. **Residual:** if the probe succeeds then the NSE writes before `performMerge`'s reads, `performMerge` still uses `[2.0,5.0]` (small window; full fix would thread fail-fast through `NSEMergeCoordinator` — deferred). Tests: NSEGradualMerge/StagingMergeBugfix/MainActorBlock (19) green (they call `mergeNSEStagingData` directly, unaffected). **CONFIRM (sim + Network Link Conditioner slow profile + a push, then cold-open):** `BootProfiler` — pre-fix would show `mergeNSEStagingData DONE in 2000–5000ms` before `★ FIRST PAINT`; post-fix the pre-paint merge skips (fast) and the row appears post-paint via read-through. Distinct from FIX 11 (post-paint parallel `connectAccount`) — that's "slow to go live"; this is "slow to first paint."
  - **FIX 13 — `DatabaseWriteQueue`: real priority-ordered scheduler for the GRDB writer (SUPERSEDES FIX 8's priority tier, 2026-06-30).** Symptom (boot_logs 4, on-device): the NSE→inbox merge's phase-1 header-surface write waited **3.7–7.8s** to acquire the writer (`merge phase1: header upsert tx done` Δ) while `backgroundPoll (sync all accounts)` ran. Root cause: FIX 8's cooperative gate only made low-priority writes *yield at a boundary*; a sync write that had ALREADY entered GRDB's single-writer FIFO could not be reordered, so the merge queued BEHIND the all-accounts foreground-sync write pile (5 accounts × N folders; each account awaits sequentially → up to ~N concurrent in the FIFO). The gate stops NEW sync writes piling on, but can't jump the ones already in flight/queued. **FIX:** new `DatabaseWriteQueue` (`TabMail/Services/DatabaseWriteQueue.swift`) — a serial (maxConcurrency=1), 3-tier (`.priority` merge/user-actions/badge > `.normal` foreground delta/full sync > `.background` backfill/AI/embedding/maintenance/FTS/drain) actor mirroring `ProviderWorkQueue`'s tiered-waiter design. EVERY async write routes through it via `PrioritizedDatabase.write`; on release it drains the highest-priority non-empty tier FIRST, so a `.priority` write JUMPS the lower-tier backlog (the backlog waits app-side where priority wins, NOT in GRDB's un-reorderable FIFO). Privileged (merge) section via `enterPrivileged`/`exitPrivileged` blocks non-`.priority` writes from STARTING even between the merge's writes (uncontended-by-bulk invariant; underflow-guarded). `PriorityGate` trimmed to ONLY loop-boundary `yield()` (CPU loops pause for the merge) + `privileged{}`/`background{}`/`normal{}` task-local brackets + read-through merge; FIX 8's `priority{}`/`priorityCount` UI tier REMOVED (queue subsumes it). Tier by context (`effectivePriority`): `inPrivilegedContext`→`.priority`; `writePriorityOverride`→that tier; else pool tier (`dbPool`=.priority, `backgroundPool`=.background). `SyncEngine.dbPool`→`AppDatabase.syncPool` (`.normal`), so foreground SYNC (fullSync/deltaSync/pull-down/on-demand/pagination) writes are `.normal` — even under `Task.detached` (the POOL tier survives task-local loss, unlike a `PriorityGate.normal{}` override). Backfill walk/deep + FTS self-heal write `AppDatabase.backgroundPool` directly (`.background`); maintenance takes a `backgroundPool` param; embedding/AI/drain/body-queues use their own pools. (Refactored 2026-06-30 from the original `dbPool→backgroundPool` + `.normal{}`-wrap design to a clean 3-pool split: `dbPool`=.priority, `syncPool`=.normal, `backgroundPool`=.background.) **Validated (boot_logs 5, on-device):** merge Δ collapsed to mostly **sub-10ms**. Tests: new `DatabaseWriteQueueTests` (serial / **jump-ahead** = priority beats queued normal+background not FIFO / privileged-reserve / throw-release) + rewritten `PriorityGateTests` + `NSEGradualMerge` + `PrioritizedWriteContention` all green. Adversarial concurrency audit (subagent): NO confirmed deadlock/lost-wakeup/double-grant; applied the one defensive fix (`exitPrivileged` underflow guard). **GOTCHA (load-bearing, unenforced):** `Task.detached` drops `writePriorityOverride`+`inPrivilegedContext` → `effectivePriority` falls back to the POOL tier, and `dbPool` defaults to `.priority` → a detached bulk write that forgets `backgroundPool` becomes merge-EXEMPT `.priority`. fullSync's per-folder writes run in a `Task.detached` passing `self.dbPool`=`backgroundPool` (correctly `.background`, just miss the `.normal` bump). **RESIDUAL — RESOLVED (2026-06-30, FIX A/B).** The debug-gated `DBwrite EXEC[background]` mark pinned the residual to a single `runSyncMessages` write for Gmail **All Mail = ~7.4s of EXECUTION** — NOT queue-wait. (Correction to the earlier hypothesis: "fullSync is wait-dominated / do-not-chunk" holds for SMALL folders, but All Mail's write genuinely executes for seconds.) Root cause: an UNBOUNDED full-folder `SELECT messageId WHERE folderId=?` (tens of thousands of rows) inside the writer, feeding only `newRemoteIds = remoteIds − local` for UID-remap detection. **FIX A:** extracted `SyncEngine.newRemoteIds(in:folderId:remoteIds:cachedLocalIds:)` = bounded chunked (`sqlChunkSize=500`) query-builder membership (≤ `remoteIds` rows); byte-identical, ADR-042-safe (pure messageId membership, no date/UID window — independently audit-confirmed, does NOT touch `selectStaleHeaders`), empty-safe (query builder not raw `IN ()`); 7 unit tests (`RunSyncNewRemoteIdsTests`). **FIX B:** `InboxViewModel.checkLargeInbox` sync `@MainActor` double-`fetchCount` → `async` off-main (every inbox `onAppear`). **v62:** `messageHeader_folderId_isRead_date` `IF NOT EXISTS` (unread-filter display query; brick-history: first draft dup'd the existing `_folderId_date` → collision failed the DB build, no data loss → grep ALL index names before adding a migration index). Concurrency re-audit (subagent): NO hard bugs; 4 hardening notes — latent `privileged{}`+`Task.detached` deadlock, `Task.detached`→`.priority` footgun, cancellation-non-cancellable docstring ADDED, intended `.background` starvation — #1/#2 + FIX D (BackfillBodyQueue) DEFERRED. **FIX C DONE:** `SyncConfig.staleDetectionMaxFullScan=2000` gates the complete-knowledge `fetchAll` (`:597`) — a large folder returning < limit falls to the bounded windowed slice (no mass-stale, no whole-folder load), `selectStaleHeaders`/`windowMode` untouched; tested against the REAL `runSyncMessages` via `MockEmailProvider`. **Archive "initial unread tick" slowness = DISPLAY path (`InboxViewModel.fetchPage:916` sync @MainActor unread query), fixed by v62 + FIX A, NOT FIX C** (`:599` is off-main/deferred/bounded for a large Archive; logmain.log worst writes are all `fullSync write[All Mail]: 50 msg` @ 6–14s). **151 sync/queue/merge suite tests green**; still UNCOMMITTED. Handoff: `PLAN_IOS_BOOG_FIX_DB_OPT.md` §10. Auto-memory: `project_db_write_queue`.
  - **FIX 14 — FIX E: snippet-pipeline main-actor stall ("stall whenever the snippet shows up", 2026-07-02, uncommitted).** User-visible main-actor stall when a new email row paints with an empty snippet and the snippet then fills in. Root cause (code-verified, full main-actor audit of the "new mail arrives while inbox visible" window): `InboxViewModel` is `@MainActor`, and `loadSnippetBatch` tier-0 looped up to `snippetOnDemandChunkSize=15` **synchronous blocking `dbPool.read`** point-reads (`lookupMessage`) on the main thread — firing ~100ms after every reload (exactly when merged/synced rows paint) while the post-merge write burst has the DB hot (reader-pool contention + multi-thousand-frame WAL); `flushAIBatch` had the same sync read ~300ms after AI lands. **FIX E:** tier-0 → ONE `await dbPool.read` batch fetch (dict by id) with a `headerFor` closure preserving the ADR-IOS-049 `pendingStagedRows` fallback (a staged-not-yet-durable row must not be blacklisted into `snippetFailed`); tier-1's `lookupMessage` fallback reuses the dict; `flushAIBatch` made `async` (sole caller = the 300ms throttle Task; apply loops re-look-up indices by id after the await → mid-await mutation safe). Debug-gated timing added: `snippet batch header read Xms (n=…)` ≥50ms gate. After FIX E, that window has ZERO auto-firing main-actor blocking DB I/O (remaining sync reads audited: init-only `resetMessages`/`fetchPage` initial paint, cold-start deep-link, Device-Sync AI-probe handler, user-tap paths — `lookupMessage` stays sync for action handlers, intentional). **boot_logs 10 lessons:** (1) the 14–43s `DBwrite EXEC[priority]` merge entries are **process-suspension wall-clock artifacts** (background wakes; app-wide log silence during the gap; no `scenePhase → active` in the window; inner ops self-report ms; ADR-IOS-049 in-memory render fired instantly) — NOT real execution; don't chase them; EXEC marks need a suspension annotation for clean telemetry. (2) REAL residual: unlabeled multi-second FOREGROUND `EXEC[priority]` writes (4.0s/3.6s) that no subsystem's marks claim, with `fullSync write` totals ≈ all queue-wait behind them — TODO: debug-gated `label:` on `DatabaseWriteQueue.execute` for attribution (suspects: `UnreadCountManager.performRecount`, AI-cache writes, pending-op drains). (3) FIX A/B/C verified on-device: `staleDetect` 0–7ms, `newRemoteIds` ~0ms (worst 3.5s on All Mail under load = reader contention, not the old unbounded load). **HARDENING ROUND (same day, second-agent review):** (a) both new async reads moved to `AppDatabase.rawPool.read` — `PrioritizedDatabase.read` front-runs the read-through staging merge, which would queue them behind an in-flight multi-second merge AND make them fresh TTL re-merge triggers (precedent: MailNavigationView notification-tap fix); (b) `flushAIBatch` got an `isFlushingAIBatch` re-entry guard + extracted `scheduleAIFlushTick` (the async conversion opened an interleave window → out-of-order stale-header application; a re-entrant call leaves ids in `pendingAIBatch` and re-arms); (c) **PHANTOM STAGED-ROW FIX** (the actual "new message with no snippet" row, boot_logs 11 +16592): `insertStagedRows` deduped by exact headerId but the merge dedups by (accountId, messageId)+rfc822 fallback → headerId skew (UID remap/folderPath variant) inserted a duplicate phantom that the Pass-1 anti-flicker guard protected FOREVER (its id never appears in a GRDB reload). Fixed all in-memory (zero DB I/O on the render path): identity dedup in `insertStagedRows` (accountId-scoped messageId OR rfc822 match), `pendingStagedRows` guard expiry (`SyncConfig.stagedRowEvictionGuardSeconds`=60 — survives 40s suspension-delayed merge writes; expiry lets reload eviction win + drops the guard; staging/GRDB never touched), and `.messagesStaged` re-post suppression (`NSEDataBridge.stagedSetChangedSinceLastPost`, pure headerId-normalized memo, `borrowing Mutex` param — NB Swift Testing `#expect` can't capture noncopyable args, bind results to locals first; drained set resets the memo). New tests: `InboxStagedInsertTests` +4 (rfc822-remap dedup, sibling-folder same-messageId dedup, account-scoped NON-dedup, guard-expiry), `StagedSetRepostSuppressionTests` (added inside StagingSignatureSkipTests.swift — no new file, no xcodegen). **Adversarial audit (subagent): NO BLOCKERS** — deadlock/livelock/starvation/isolation/render-path-I/O all clean-bill; applied its RISK-1 (`resetMessages` re-seeds from `NSEDataBridge.latestStagedRows` — zero DB I/O — because suppression removed the re-posts that self-healed a VM that missed the post: nav-churn recreation, cold boot, wrong-folder VM), RISK-2 (`expiredStagedIds` tombstones so a stage-transition re-post can't re-arm an evicted phantom), NOTE-3 (`!row.messageId.isEmpty` dedup guard), NOTE-6 (`#if DEBUG` test hook). Full suite 7,277 tests / 1,003 suites PASSED before AND after the audit fixes. Handoff: `PLAN_IOS_BOOG_FIX_DB_OPT.md` §11. **FOLLOW-UP (boot_logs 12): stall GONE (snippet batch read 4ms), residual = snippet stayed empty until reply-AI done. Two causes, both fixed:** (1) `flushAIBatch`'s `// Preserve snippet (not in header…)` line unconditionally stomped the FRESH DB snippet with the stale in-memory (empty) one on every AI-stage repaint — its premise was wrong, `MessageSnapshot(from:)` DOES copy `header.snippet`; now fresh-wins, in-memory kept only when fresh is empty (SnippetLoader fills can lead the DB). (2) IMAP NSE stages `snippet=""` always (`NSEIMAPConnection.swift:231`, metadata fetch is body-less by design; Gmail/Outlook get provider snippets) → ADR-IOS-049 in-memory rows rendered snippet-less even when staging carried the body; `mergeNSEStagingData`'s stagedRows payload now computes it from `msg.textContent` when empty (bounded `snippetFromPlainText`, in-memory only). Tests: `freshSnippetWins` / `inMemorySnippetKeptWhenDBEmpty` in `InboxViewModelAIBatchTests`. §12 of the plan. **FOLLOW-UP 2 (c0ab0084-boot_logs): snippet fixed; residual main-actor stall pinned to a 4.8s ONE-ROW `merge.phase1` EXEC[priority] in the FOREGROUND** (+2682517 insertStagedRows → user leaves at +2687081 → EXEC completes +2687328). Lookups ARE indexed; `wal_autocheckpoint=0`; no main-DB sync-write bypass outside Settings. Can't distinguish writer-queue wait vs slow closure from the old mark → **instrumentation added (§13)**: EXEC marks now `DBwrite EXEC[tier] <label> in Xms (sched=Yms body=Zms)` (`bodyStart` = `OSAllocatedUnfairLock<Double>` stamped at GRDB closure entry — `Mutex` is noncopyable, unusable as an optional param; labels: `merge.phase1`/`merge.body`/`unreadRecount`), plus `BootProfiler.startStallWatchdog()` (armed in `runIfNeeded`, runtime-gated): 250ms main-queue pings, >500ms turnaround stamps `⚠ MAIN THREAD STALL ~Xms` into the BootProfile timeline at recovery. Decode next capture: sched≈5s ⇒ hunt a GRDB-writer-queue holder outside DatabaseWriteQueue; body≈5s ⇒ per-statement phase-1 timing next; stall marks correlate (or don't) with EXEC windows.
  - **NSE CID resolution is ALREADY done (corrects a stale `BodyRenderer.swift:12-14` comment).** All 3 NSE clients pass `headerId` → `BodyAssetStore.makeInlineImageWriter` (disk-backed, App-Group shared) → CIDs resolve to `tabmail-asset://` refs, low-memory. `hasUnresolvedCIDs=true` only for the narrow edge: inline image >1MB (`BodyRenderer.maxInlineImageBytes`) / >20 images (`maxInlineImages`) / fetch failure. So "render CIDs in the NSE" (a tempting Fix) is mostly a no-op; raising the caps re-introduces the 24MB NSE-budget risk — NOT worth it. The body-render delay the user saw on "silent push / BGAppRefresh" mail is the lazy-body cache-miss + the 2s poll floor (`MessageDetailViewModel.startBodyPoll` sleeps 2s before its first DB check, `:203`) — separate, NOT addressed by FIX 7. **Immediate-check FIXED 2026-06-28 (commit `7965b00`); the risky parts stay deferred.** Root cause of the slow NOTIFICATION-TAP open (even plaintext — fg-then-tap was fast): the deep-link handler awaits its own NSE merge → fires an inbox reload that, racing the navigation (+ AI reply-precompute writes + a pending-op drain), CANCELS the freshly-presented `MessageDetailView`'s `.task` during `loadBody`'s initial DB read (GRDB throws `CancellationError` on async reads in a cancelled task) → `loadBody` defers to `startBodyPoll`, which slept 2s before its first check even though the deep-link's merge had ALREADY cached the body (`logmain.log`: NO server fetch during the 2s — pure waste). **Fix:** `startBodyPoll` now does an IMMEDIATE `MessageBody` check before the first sleep (pure DB read, no server fetch) → renders the cached body at once (~95ms in test). `startBodyPoll` made internal for the test (`OnDemandBodyFetchGRDBTests.startBodyPollImmediateCacheHit`). **STILL deferred (the risky half — do NOT do naively):** shortening the poll INTERVAL or removing the deferral — 3 traps: (1) step-2 server fetch is coupled to the 2s cadence (shortening hammers IMAP + dup-fetch storm); (2) no body-landed signal, and `.messageDataDidChange` reuse storms via the shared `BackfillBodyQueue` (observers: detail / `InboxViewModel:451` flushAIBatch / `ReminderBuilder:62`); (3) the "cannot connect" deferral rationale may predate the connection pool (`loadBody` already fetches via `pool.withConnection(priority:true)`). The deeper fix — preventing the `.task` cancellation — is SwiftUI-lifecycle territory, untouched.

---

## OAuth / Google Cloud

- **iOS OAuth client ID**: iOS type, bundle ID `ai.tabmail.ios`, stored in `Secrets.xcconfig`
- **Credentials flow**: `Secrets.xcconfig` → loaded via `project.yml` configFiles → exposed in `Info.plist` → read at runtime by `OAuthConfig` in `GoogleAuthService.swift`
- Shares GCP project with Thunderbird (see `../PROJECT_MEMORY.md` for GCP details)
- **Merged OAuth flow (single prompt)**: Google/Microsoft sign-in uses direct OAuth → `signInWithIdToken` → auto-create email account. Google requires adding the iOS `GOOGLE_CLIENT_ID` as an Additional Client ID in Supabase dashboard (Auth → Providers → Google). Microsoft needs no extra setup — same application ID across platforms. See `SETUP.md` for details.
- **Account-add UI lag — refresh NavigationStore BEFORE navigating away from the add screen (fixed 2026-06-25). Affects BOTH the onboarding router AND Settings → Add Account.** Shared root cause: `navigationStore.accounts` is repopulated only ~100ms after an add, via the **debounced** `.backgroundDataDidChange` listener in `NavigationStore.loadInitialData` (100ms `Task.sleep` — debounced to coalesce sync-burst notifications; do NOT remove it, the fix is view-side). All `add…` functions in `AccountManagerSetup.swift` `await dbPool.write` (committed) BEFORE `return account`, so an *immediate* `await navigationStore.refresh()` (the RenderGate-free `refreshNow`) right after the add reliably reads the new row.
  - **Onboarding (`RootView.routedContent`):** the `if/else` chain re-evaluates synchronously the moment `PendingAccountAdd.shared.clear()` runs; in the 100ms gap it falls through to `else if navigationStore.accounts.isEmpty { AddAccountGeneralView() }`, briefly **flashing the generic "Add Account" screen** before the next gate (AI consent). Fix: `RootView.finishPendingAccountAdd()` = `await refresh()` *then* `withAnimation { clear() }`. `pending` stays non-nil across the `await`, so the router keeps showing the add gate (no premature flash) until accounts is populated. Both the iCloud `onConnected` closure and `commitGmailOutlookAdd` route through it. `AddAccountICloudView.onConnected` was made `() async -> Void` so the Connect spinner stays up until the screen actually changes.
  - **Settings (`AddAccountGeneralView` pushed from `SettingsView` via `NavigationLink`):** on success each handler `dismiss()`ed back to the `ForEach(navigationStore.accounts)` list — which was still stale for 100ms, so the new account wasn't there on return ("blink, nothing happened"). Fix: `AddAccountGeneralView.finishAddAndDismiss()` = `await navigationStore.refresh()` *then* `dismiss()`; all four handlers (gmail/outlook/icloud/imap) route through it. Required injecting `@Environment(NavigationStore.self)` — its preview (`AddAccountGeneralView+Preview.swift`) now injects a bare `NavigationStore()` or it crashes (non-optional env). NB `dismiss()` is a harmless no-op when this view is the onboarding `RootView:159` branch (not presented); the refresh re-routes RootView forward there too.
  - **Rule: any account-add success path MUST refresh the nav store BEFORE navigating away (route through `finishPendingAccountAdd` / `finishAddAndDismiss`).** A more systemic alternative (a non-debounced `.accountsDidChange` for structural account add/remove) was considered but declined — it risks the sync-burst coalescing the debounce exists for.
  - Note `.calendarOnly` legitimately keeps `accounts` empty (calendar accounts filtered out of `sidebarRequest`: `isActive && !calendarOnly`) → it correctly proceeds to `AddAccountGeneralView` for email setup; that is NOT the bug.
  - **iCloud CalDAV-failure handling (fixed 2026-06-25, same work).** `addICloudAccount` commits the email account + registers its provider BEFORE `setupCalDAV`, which can throw. Old behavior: the whole add threw → email account stranded behind a "failed" message (and the onboarding form has no Mail-only option, so a transient CalDAV hiccup could block iCloud Mail entirely). Now:
    - **`addICloudAccount`** — calendar is **best-effort**: a CalDAV failure is caught, the email account is kept, and the failure is *recorded* on the account via a new persisted `Account.calendarSetupFailed: Bool` column (migration **`v61_addCalendarSetupFailedToAccount`**, additive, default false). The catch sets it in-memory (returned account → drives the alert) AND `UPDATE account SET calendarSetupFailed = 1` (row → drives the Settings note); write failure is debug-logged, in-memory flag still shows the alert.
    - **Surfacing** — both add screens read `account.calendarSetupFailed` after the add: if true they show a single-button **`.alert("Calendar Not Connected") { Button("Continue") }`** whose Continue proceeds (onConnected / finishAddAndDismiss); else they navigate immediately. Persistent indicator: **`SettingsView` account row shows a `Label("Calendar not connected", systemImage: "calendar.badge.exclamationmark")` in `.orange` below the "Primary" tag** whenever `account.calendarSetupFailed`.
    - **`addCalDAVAccount`** (calendar-only) stays **atomic** — rolls back the synthetic account on CalDAV failure (calendar IS the account there; no email to keep). `setupCalDAV` self-cleans its Keychain entry on failure (config.id is a fresh UUID, unreachable by `removeAccount` cleanup).
    - **Dead code removed (was audit finding F1):** `AddAccountGeneralView.connectICloud`'s `catch CalDAVError.authFailed` (unreachable once `addICloudAccount` swallows CalDAV errors). The same catch in `AddAccountICloudView` is KEPT — still reachable via the `.calendarOnly` → `addCalDAVAccount` (rethrows) path.
    - **Not built (follow-up):** nothing CLEARS `calendarSetupFailed` (no retry-calendar UI); since `addICloudAccount` has no dedup, re-adding makes a fresh row (flag false) and leaves the old flagged duplicate. A "Reconnect Calendar" action in `AccountDetailView` (set flag false on `setupCalDAV` success) is the clean fix.

---

## Architecture Patterns

### Email image aspect-ratio distortion — gated post-load correction (2026-06-26)
- **Symptom:** full-width images in rendered emails render too tall (vertical stretch). **Root cause:** the responsive-image CSS (`EmailHTMLWrapper.swift:230-231`) is `img { max-width: 100% }` + `img[width] { height: auto }` — `height:auto` is scoped to images that carry a `width` *attribute*. A sender that pins a fixed `height` (height attr or inline `height`) with fluid/capped width and NO `width` attr gets its width capped by `max-width:100%` on a phone while the height stays fixed → distortion. It is **fundamentally not CSS-fixable**: a `height="29"` logo (keep small) and a `height="300"` + `width:100%` hero (must scale) are *syntactically identical* — the only disambiguator is the true intrinsic aspect ratio, which (per the HTML spec's width/height→`aspect-ratio: auto w/h` mapping) the browser itself only knows after the image loads.
- **Fix = gated post-load JS correction** (`fixImgAspectFnJS` / `fixImageAspectRatioJS` in `AutoSizingHTMLView.swift`, registered after `heightMonitor`; `tmFixImgAspect(img)`). On each image's `load`, if `|renderedRatio − naturalRatio| / naturalRatio > 0.02` it sets **`height:auto !important` and NOTHING ELSE**. Two invariants make it safe and are the whole point:
  1. **Only ever touches `height`, never width** → the document's horizontal extent is unchanged, so `fitViewportJS`'s horizontal-overflow widen decision (the thing the logo-balloon regression feeds) can't be perturbed.
  2. **Gated on rendered-vs-natural ratio divergence** → a proportional logo (rendered ratio ≈ intrinsic) is left untouched and can never balloon to full container width (the exact regression the scoped CSS guarded against). `naturalWidth/Height` are EXIF-orientation-corrected by WebKit, so rotated photos compare correctly.
- **Why post-load and not render-time stamping:** remote images are deferred (`src`→`data-tmsrc`, loaded after first paint) and never downloaded in Swift, so their dimensions are unknowable until load — and the user's distorted full-width heroes are almost always remote CDN images. Render-time stamping in `BodyRenderer` would only help inline `cid:` images AND adds real surface (manual EXIF handling, NSE 24MB budget, regex img-tag matching) — deferred as a non-fix for the reported case. Post-load is the spec mechanism (browser switches to intrinsic ratio on load), not a band-aid.
- **Wiring caveat:** `monitorHeightJS`'s image listeners do `if (img.complete) continue` — a deferred remote `<img>` is `complete` (no src) at documentEnd, so it's skipped there. The aspect-fix script attaches its OWN non-`{once}` load listeners + re-scans (rAF + 1600ms, after `deferredImageLoadJS`'s 1500ms failsafe swap) so the swapped-in remote load is caught. It calls `window.__tmReportHeight()` after a correction so the webview frame resizes; idempotent via `img.__tmAspectFixed`.
- **Tests:** `ImageAspectRatioFixTests` (JSContext, mock `img`, same zero-drift pattern as `walkUpToWrapStart`) — distorted→corrected, logo/proportional/unloaded/zero-box→untouched, idempotent, + string-level guards that the production script never touches width. The pinned CSS test (`EmailRenderPipelineTests:imgHeightAutoScopedToWidthAttr`) is UNCHANGED — this fix is purely additive JS, the `img[width]` CSS stays.
- **Debug logging:** `tmFixImgAspect(img, log)` takes an optional `log` callback (no-op in prod/tests, same pattern as `walkUpToWrapStart`'s `logFn`); the production script injects a `DebugModeManager`-gated `[ImgAspect]` logger (mirrors `fitViewportJS`'s `log`, lands on the `consoleLog` handler). `fixImageAspectRatioJS` is therefore a computed `var` (not a `let`). Each line: `FIX|skip(reason) nat=WxH(ratio) render=WxH(ratio) div=N% attrW= attrH= <src-tail>`.
- **Validated on a real newsletter (2026-06-26, `logmain.log`):** the distorted images (div 15–23%) **carried BOTH `width` and `height` attributes** yet still rendered at their pinned height — i.e. `img[width]{height:auto}` (a non-`!important` stylesheet rule, spec 0,1,1) was being beaten by the sender's **inline `style="…height…"`** (spec 1,0,0,0). So the CSS rule was silently INEFFECTIVE for this whole class; the JS works only because it writes inline `height:auto !important`, the one thing that outranks an authored inline height. First scan logged all imgs `skip(unloaded nat=0x0)` (deferred remote not yet loaded → no premature fix), later scans fixed the distorted ones and `skip(already-fixed)` the rest (idempotency + load-listener wiring confirmed). Distortion (15–23%, +one 2.5%) vs proportional (0–1.2%) separated cleanly around the 2% gate — well-calibrated. NOTE: the `img[width]{height:auto}` CSS rule is doing less than its comment implies whenever inline height styles are present — the JS is the real fix.

### IMAP Connection Pool (supersedes ADR-IOS-014)
- `IMAPProvider` is a Swift `actor` with an `IMAPConnectionPool` (also an actor) managing multiple concurrent connections
- **Connection pool** (`IMAPConnectionPool.swift`) replaces the old serial lock AND temp connection infrastructure
- Operations checkout a connection via `withPoolConnection(priority:) { server in ... }` — each gets its own connection, SELECTs independently
- **Priority checkout**: user-initiated ops (move, markRead, fetchMessage) use `priority: true` — jump to front of waiter queue ahead of background ops
- **Adaptive concurrency**: detects server connection limits from `mail_max_userip_connections=N` rejections, adjusts automatically (cooldown on full rejection, gradual recovery)
- **Connection reuse**: idle connections persist in the pool across operations (no create/destroy per batch)
- **Liveness checks**: NOOP before reuse if idle > 2 minutes, dead connections discarded
- **Idle pruning**: connections unused > 5 minutes are closed (called during reconnect)
- **Batch checkout**: `pool.checkoutBatch(count:)` for parallel body fetch — replaces `createTempConnections`
- Gmail/Exchange HTTP providers unaffected — URLSession handles connection pooling natively

### ProviderWorkQueue Cancellation Semantics (2026-06-09)
- The **throwing** `execute<T>` overload is cancellation-aware: a task cancelled while waiting for a slot throws `CancellationError` immediately — the waiter entry is removed, no slot is consumed, and `work` never runs. Before this, cancelled waiters (e.g. abandoned remote searches) queued up in tier 0 ahead of real user actions and still executed doomed network calls — the cause of the 2026-06 search-mode hang.
- The **non-throwing** (fire-and-forget) `execute` overload intentionally keeps the old behavior: always waits, always runs.
- **Overload-resolution gotcha:** a non-throwing `Void` closure resolves to the fire-and-forget overload even when written with `try await`. To get the cancellation-aware path, the closure must throw or return a value (tests force this with `let _: Int = try await queue.execute { ...; return 1 }`).

### FTS Tokenizer — NO tokenchars (ADR-024, 2026-06-09, lockstep with Rust crate)
- `SearchConfig.ftsTokenize` (mirrors `config.rs FTS_TOKENIZE`) has **no tokenchars**. unicode61 treats all non-alphanumeric characters as separators by default; the old `tokenchars '-_.@'` made those four token-internal, gluing addresses into single tokens that could only be prefix-matched from the token start — root cause of the email-address search bug. Without it, addresses index as parts and any part is matchable ("dmarc" finds `noreply-dmarc-support@…`).
- **Tokenizer changes migrate IN PLACE, invisibly** — `SearchIndex.rebuildStaleTokenizerShards(deadline:)` detects stale shards from their CREATE statement in sqlite_master (currently: contains "tokenchars"), rebuilds each via rowid-preserving `INSERT..SELECT` + rename swap in one transaction, newest year first. No UI. Entry points (reentrancy-guarded, all AFTER sync work so push/sync is never starved): post-init background Task (primary, unbounded); BGProcessing after `syncStartup` (unbounded — minutes of budget); BGAppRefresh and silent-push main path after their sync with a small slice (`SearchConfig.retokenizeShortWindowBudgetSec`). `Task.isCancelled` checked between shards so expiration/deadline races wind down at a shard boundary; pending shards count as "work" in the BG reschedule condition (`hasStaleTokenizerShards`). Stateless detection makes it idempotent/resumable: kill mid-shard → rollback + redo, kill between shards → converted shards stay done. Concurrent writes are a non-issue: each conversion is one serialized-writer transaction, old/new tables share the column list (only `tokenize=` differs), and FTS5 stores text — tokens are re-derived on insert. On TB the same runs host-side inside init under a 45s budget — **NEVER bump the crate's `SCHEMA_VERSION` for this** (the addon treats that as "full re-index from Thunderbird", hours on big archives; see ADR-024).
- `SearchQueryParser.buildFTSMatch` auto-quotes tokens containing `- @ . : +` as prefix queries (`"..."*`) — under the splitting tokenizer these become adjacency-phrase prefixes (correct for partial AND full addresses). `extractColumnScopeFilter` (and Rust `extract_column_scope_filter`) consume the trailing `*` after quoted field values.
- `MemoryIndex` shares the tokenize string via `SearchConfig.ftsTokenize`; memory.db is derived, so its `schemaVersion` bump (v4) drop-and-rebuilds it (Stage A refills).

### SwiftUI preview convention

- **All `#Preview`s live in `TabMail/Views/Previews/<Group>/<View>+Preview.swift`, wrapped in `#if DEBUG`** — NOT inline in the view file. Groups are feature-area folders: `Account`, `Agent`, `App`, `Components`, `Compose`, `Consents`, `Demo`, `Inbox`, `Tooltips`. XcodeGen globs `TabMail/` recursively, so a new group folder is auto-picked-up (just rerun `./Scripts/xcodegen.sh`).
- **`PreviewMocks` (`Views/Previews/PreviewMocks.swift`) is the shared factory** for rendering REAL host views (not stand-in mocks): `bootstrapAppDatabase()` (ephemeral on-disk WAL pool), `seedInbox()` (1 account / 1 inbox / 5 fully-classified messages w/ tags+summaries+replies), `seedOutbox(account:)`, `navigationStore()` (real `loadInitialData()` + opens `AISubscriptionGate`), and `spotlight(_:)` for isolating one TipKit tip, and `hideAllTips()` for host-view previews that shouldn't show ANY tip (else tooltips pop in on interaction — Xcode reuses the preview process so a prior `spotlight` leaks). To show a real `InboxView`: `NavigationStack(path: .constant([scenario.inbox]))` + `.navigationDestination(for: Folder.self)` → `InboxView(...)`, `.environment(navStore)` + `.environment(\.hasTabMailSession, true)`. Pattern reference: `EnableInboxPushTip+Preview.swift`. To make a banner/notification-driven nav testable in the canvas, host the seeded view in a `NavigationStack` and bridge its notifications to a `path` push (reference: `UsageThrottleBanner+Preview.swift` — also injects a fresh `StoreKitManager` for `PlanPickerView`).
- For state that gates a view (subscription, throttle, etc.), add a `#if DEBUG` preview-only setter on the relevant `@Observable` store (e.g. `UsageThrottleStore.previewConfigure`) rather than constructing a full backend response.
- Consolidated 2026-06-23: moved the last inline previews (`SplashView`, `AgentChatSheet`, `DemoBanner`, `DemoSeedingSplash`, `DemoAccountDashboardView`, `WorkingLogoView`, `AppTips`) into the `Previews/` tree.

### Inbox usage-throttle banner (ADR-IOS-044)

- **`AccountInfo.queueMode` / `quotaPercentage` / `planTier` from `/whoami` are the proactive throttle signal** (the 429 `.throttled` SSE events only flow through the chat path `sendCompletionsWithTools`; the background summary/action path `sendCompletions` has NO throttle callback). `UsageThrottleStore` (`Services/AI/UsageThrottleStore.swift`, `@Observable` singleton) caches them + `hasOwnAPIKeys` (`AIService.shared.byokBundle != nil`) and exposes `banner: UsageThrottleBannerKind?`.
- **Fed from the existing always-on foreground whoami** in `RootView.revalidateAISubscriptionGate()` (launch-`.active` + sign-in) + `AccountDashboardView.fetchAll`; `refreshKeyState()` from `APIKeysView` key persist. NO new poll. `hasCheckedOnce` is in-memory → no cold-launch flash.
- **Tier branch:** Basic+throttled → upgrade-to-Pro (`PlanPickerView`); BYOK+no-key → TabMail Settings' **AI Provider** section (provider picker per tier + Manage API Keys — NOT the raw keys page, since BYOK needs both); Pro → nil (placeholder for Max/pay-as-you-go). `UsageThrottleBanner` (`Views/Shared/`) is subtle, no dismiss button, inbox-only (`isInboxView`). Preview renders the REAL `InboxView` (`Views/Previews/Inbox/UsageThrottleBanner+Preview.swift`) via `PreviewMocks.seedInbox/navigationStore` + `UsageThrottleStore.previewConfigure` (DEBUG-only) — see "SwiftUI preview convention" below.
- **Navigation:** `.navigateToPlanPicker` → `selection = .planPicker`. `.navigateToAIProvider` → set `TabMailSettingsView.pendingScrollAIProviderKey` (UserDefaults) + `selection = .prompts`; `TabMailSettingsView` is wrapped in a `ScrollViewReader` and scrolls (on appear) to the `.id`-anchored AI Provider section, then clears the flag. NO standalone provider/keys `MailboxSelection` route — provider selection only exists inside TabMail Settings. The nav `.onReceive`s live in the `NavigationNotificationHandlers` ViewModifier — keeping them inline blows the `body` type-check budget.

### Remote Search (SearchView)
- Typing only searches locally (legacy string match + FTS after 150 ms debounce). **Remote search fires only on keyboard submit** (`triggerRemoteSearch`) — never per keystroke. Scope toggle re-runs an already-submitted remote search (`hasSubmittedRemote`).
- The FTS debounce UNIONS with the legacy substring hits (FTS ranked first, deduped by headerId) — never replaces. A substring scan can still catch what FTS misses (e.g. mid-part fragments like "marc-sup"), so never hide what's already shown (graceful degradation, ADR-IOS-007).
- **`SearchResult` identity MUST be `accountEmail+folderPath+messageId`, never `UUID()` or bare `messageId`** (SearchView). IMAP `messageId` is the per-folder UID (`IMAPFetchMapping.messageIdString` returns `"\(uid)"`), so it repeats across folders; a search-all hitting several folders of one account would mint duplicate/`UUID()`-churning identities → SwiftUI `List` paints the wrong subject on a row (correct on open). The same composite scopes the remote-merge dedup (account-wide `folderPath==""` for Gmail/Graph globally-unique ids; folder-matched for IMAP). Fixed 2026-06-16.

### FTS↔GRDB id drift (folder baked into the key) — "indexed but unfindable" / wrong-subject class
- **The FTS `headerId` IS the GRDB `MessageHeader.id` = `"accountId:folderPath:messageId"` — it embeds the folder.** So ANY folder move re-keys the GRDB header and the FTS entry MUST be re-keyed in lockstep or it drifts: search/AI/embedding re-hydration does `MessageHeader.fetchOne(key: ftsHeaderId)` and **silently drops** the hit on a miss (`compactMap` → nil). Confirmed real: Gmail archive (INBOX→`__GMAIL_ALL_MAIL__`) and trash (→`TRASH`) left stale `:INBOX:` FTS entries.
- **Drift SOURCE fixed (`SyncEngineDeltaSync` Gmail delta, ~line 168):** the label-removed branch did `existing.delete(db)` (header left a folder) WITHOUT `removeHeadersFromFTS` — unlike the messages-deleted path. Now threads `removedIds` out of the write and calls `removeHeadersFromFTS`. Other delete-and-reinsert edges still leak (Gmail/Exchange optimistic-sent dedup, `DraftStore` placeholder→real migration) — catch via reconciliation.
- **Drift HEAL on access (`ftsResultsToSearchResults`):** exact `fetchOne(key:)` miss → recover by `(accountId, messageId)` and `SearchIndex.rekeyHeaders` the stale entry to the current id (+ `updateFolderIds`). Repairs the INDEX so the fix propagates to ALL consumers, not just search. Gated to **Gmail/Graph** (move-stable, globally-unique messageId); IMAP UID is unsafe to recover by (changes on move, repeats across folders) → owned by sync-side re-key + source fix. Log: `healedDrift=N`.
- Proper-but-deferred fix = key GRDB+FTS on a move-stable id (`stableId`/rfc822) so folder is a plain mutable column (big 253k-row migration). Recommended central heal = make `pruneFTSOrphans` RE-KEY (not just delete) and run recurring (currently one-shot via `ftsReconcileDoneKey`). Distinct from the EXISTING `staleCorrections` path, which only fixes `meta.folderId` for the orphan-reclaim case where the id still resolves.
- **Search-all fan-out**: Gmail/Outlook accounts get ONE account-wide search (`folder: ""` — Gmail omits labelIds, Graph hits `/me/messages`; both exclude spam/trash by default). Per-folder fan-out is IMAP-only (no account-wide IMAP SEARCH). Per-folder Graph fan-out trips the MailboxConcurrency throttle (429) — don't reintroduce it.
- The per-folder remote fan-out runs as child tasks whose cancellation is propagated manually from `searchTask` (the Swift 6 region-isolation checker rejects `group.addTask` closures capturing a SwiftUI view). Cancelled/failed/timed-out folder searches resolve as empty results; whatever completed is merged.
- `triggerRemoteSearch` bumps `searchGeneration` so a re-submit of the same query invalidates the previous wave (otherwise stale children corrupt the `pendingAccounts` counter).

### "Searchable but can't open / no snippet / not in its folder" — GRDB-side stuck headers (NOT FTS orphans), IMAP (under investigation 2026-06-22)
- **These are NOT FTS orphans.** For IMAP, `SearchView.ftsResultsToSearchResults` DROPS an FTS hit whose GRDB header is missing (`droppedNoHeader`, returns nil — the Gmail/Outlook drift recovery at `:509-527` is gated off for IMAP). So any message that still SHOWS in local search has a live `MessageHeader`. The breakage is in the header's STATE, not the index.
- **Signature** (the row exists → searchable; but): `bodyComplete=0` (→ no snippet; open fails because the stored `messageId`/UID is stale for the folder it now claims) AND/OR a folder anomaly: `folderId` matches no `folder` row (empty/orphan → invisible in every browse, which filters strictly on `folderId == fid`, `InboxViewModel.swift:756/878`), or the PK `id` ("accountId:folderPath:messageId") disagrees with the current `folderPath`/`messageId` columns (optimistic-move remnant). `optimisticMoveToFolder` (`AccountManagerActions.swift:211-215`) updates folderId/folderPath/isInInbox but leaves `id`+`messageId` (stale UID) untouched.
- **Why it never recovers on IMAP:** the only two reconcilers both have gaps. Orphan-reclaim (`SyncEngineFullSync.swift:976`) matches by **id-key** → misses UID changes. UID-remap (`:568-613`) matches by **rfc822MessageId** but ONLY inside the sync **overlap window** (`:499/:513`) → an older archived message, or one with no Message-ID (`rfc822MessageId==nil`, common on IMAP), is never compared. **"Smart Reindex" (`resetCrawlState`) does NOT fix it** — resets crawl cursors + retries body fetch, never touches folderId/messageId, so the re-fetch fails identically.
- **Confirmed secondary bug (FTS scoped search):** `SearchIndex.rekeyHeaders` (`SearchIndex.swift:986-993`) moves `message_ids.headerId`+`message_meta.headerId`+FTS `msgId` but **NOT `message_meta.folderId`**. The SYNC-side callers (`SyncEngineFullSync.swift:116/280`, `DraftStore.swift:287`) don't follow with `updateFolderIds` — only the search-side heal (`SearchView.swift:587`) and one backfill path (`SyncEngineFTS.swift:572`) do. So even a successful UID-remap leaves the FTS folder tag stale → folder-scoped search misses these (global search still finds them). Candidate fix: have `rekeyHeaders` carry `newFolderId` (single source of truth) so meta.folderId moves with the headerId.
- **Diagnostic (READ-ONLY, debug-gated):** `StuckMessageDiagnostics.run()` → Debug menu ▸ "Stuck Message Diagnostics" ▸ Scan; report on the `stuck_messages` `BackgroundSyncLogger` channel (shareable). Counts + bounded samples per signature (NOT-browsable / PK-mismatch / bodyless-in-Archive) + a per-folder **per-month histogram** + each folder's `bfComplete`/`cursor`/date-range. Each sample row tagged `folderExists`/`role`/`sibling`(healthy copy elsewhere → safe-to-delete leftover)/`inFTS`/`rfc822`. Mutates nothing.

### Stale-detection window MUST match the fetch's ordering dimension — IMAP Archive "missing months" data-loss (CONFIRMED ROOT CAUSE + FIXED 2026-06-23)
- **This is the real bug behind the stuck-header report.** `SyncEngine.runSyncMessages` (full-sync) fetches only the newest `SyncConfig.syncMessageLimit` (=**50**) messages of a folder, then stale-DELETEs local rows in the "overlap window" the server didn't return. The window was bounded by **date** (`date >= min(fetched dates)`) for ALL providers. IMAP `fetchMessages(limit:)` returns the **highest UIDs = archive-time**, which is DECORRELATED from message `date`: archiving an OLD-dated email gives it a fresh HIGH UID. That single row drags the date floor backwards (e.g. to a just-archived Apr-29 mail) so every mid-range month not in the newest-50 (all of May) is deleted. It re-fires every full-sync → "stuck forever"; Smart Reindex re-fetches but the next full-sync re-deletes. **IMAP-only**: Sent/Trash are safe (UID≈date order), Gmail/Exchange fetch most-recent-by-date so their date floor is correct (Gmail All Mail was intact). Confirmed from the `StuckMessageDiagnostics` per-month histogram: IMAP `Archive` showed `2026-04=135, 2026-05=0, 2026-06=48` with `bfComplete=Y` while Sent `2026-05=128`/Trash `2026-05=322` were intact.
- **Fix:** `EmailProvider.staleWindowMode: StaleWindowMode` (`.uid` IMAP override, `.date` default for Gmail/Exchange). **`SyncEngine.selectStaleHeaders` is the SINGLE SOURCE OF TRUTH** (pure/`nonisolated static`, used by BOTH production `runSyncMessages` AND the `simulateRunSyncMessages` test harness so they can't drift): `< limit` ⇒ whole folder ⇒ delete anything not remote; `.uid` ⇒ candidate iff `Int64(messageId) >= min(fetched UID)`; `.date` ⇒ `date >= min(fetched date)` (unchanged). Production still loads only the bounded slice from SQL (`CAST(messageId AS INTEGER) >= floor` for `.uid`) to keep the memory budget, then defers the decision to `selectStaleHeaders`. `deltaSync` has no windowed date-stale path (unaffected). **Any future windowed stale-delete MUST route through `selectStaleHeaders`.**
- **Regression:** `E2ESyncScenarioTests.StaleDetectionWindowTests` (3 tests): `.uid` preserves mid-range Archive mail when an old-dated mail is archived; characterization that `.date` over-deletes it (the bug); `.uid` still deletes a genuinely server-removed row inside the UID slice.
- **Field auto-heal (v59 migration):** the fix only STOPS deletion; already-deleted Archive mail must be RE-fetched. `v59_rewalkImapArchiveAfterStaleWindowFix` (body = `AppDatabase.rewalkImapArchiveFolders`, shared with its test) runs once on upgrade and resets backfill (`backfillComplete=0`, `backfillUidCursor=NULL`) for **IMAP archive-role folders only** → next backfill re-walks + re-fetches; the UID-window fix keeps them. MUST ship in the SAME build as the fix (else the re-walk just gets re-deleted). Without v59 each user would have to tap Smart Reindex manually. Scoping test: `ArchiveRewalkHealTests`.

### GRDB Persistence
- All persistence via GRDB `DatabasePool` (`AppDatabase.swift`) — thread-safe concurrent readers, serialized writers via WAL journal mode
- `dbPool.write { db in ... }` for mutations, `dbPool.read { db in ... }` for queries
- **`read`/`write` overload selection depends on the closure's return type being `Sendable`.** `try await dbPool.read { ... }` only picks the *async* (non-blocking) overload when the closure returns a `Sendable` value. Returning a non-`Sendable` type — most commonly `[Row]` (GRDB `Row` is not `Sendable`) — silently falls back to the *synchronous, thread-blocking* overload, which surfaces as the compiler warning `no 'async' operations occur within 'await' expression` AND violates the never-block rule. **Fix: map to a `Sendable` type inside the closure** (e.g. `try Row.fetchAll(db, sql: …).map { $0["id"] as String }` → `[String]`) so the async overload is chosen. Do NOT "fix" the warning by deleting `await` — that keeps the blocking overload. (Cleared 4 such sites 2026-06-12.)
- No background `DispatchQueue` needed — GRDB handles thread safety internally
- **`journal_size_limit` is ALREADY 32768 (32 KB) on a WAL `DatabasePool` — do NOT add a `PRAGMA journal_size_limit` "cap" thinking the residual WAL is unbounded.** SQLite's *raw* default is `-1` (unbounded), but Apple's system libsqlite3 (what GRDB uses on iOS; GRDB's own source sets it nowhere) defaults it to 32768 for WAL. So after every passive checkpoint the WAL is already truncated to 32 KB — there is no "unbounded residual WAL" problem to fix. Raising it (e.g. to 16 MB) makes the post-checkpoint residual *larger*, and `journal_size_limit` never bounds growth *during* checkpoint starvation (long-lived readers) anyway — only the residual after a successful reset. Verified empirically 2026-06-13 (a WAL pool with no custom config reads back `PRAGMA journal_size_limit = 32768`). This killed a proposed PLAN_HANG_FIX follow-up; the warm-foreground hang is addressed by the async-read work, not by WAL-size knobs.
- Stale detection runs for **all folders including inbox** — `MessageAICache` preserves AI state for re-inserted messages
- `NavigationStore` provides reactive UI updates (replaces SwiftUI `@Query`)
- **Startup gating is two-phase + migration-splash is conditional (`AppStartup`, PLAN_HANG_FIX).** DB build runs OFF `TabMailApp.init` because the O(mailbox-size) thread-repair migrations (v9/v27/v47/v53/v54) + one-time destructive resets can take minutes after a multi-version jump (the "hang on boot"). `AppStartup.runIfNeeded` runs: **Phase 1** `AppDatabase.makePool()` (opens the pool, NO migrations) → `AppDatabase.hasPendingMigrationWork(_:)` (cheap, read-only: migrator's applied set + `StartupMigrations.allResetsComplete`); **Phase 2** `AppDatabase(pool:runStartupResets:true)` (runs migrations + resets) then the DB-dependent launch steps, then `isReady`. The `SplashView(mode: .migrating)` "Updating…" screen shows **only** when `hasPendingMigrationWork && dbExisted` (the file pre-existed the launch) — i.e. real, possibly-slow work on an existing mailbox. The common path (already-migrated DB) and fresh installs (empty DB migrates instantly) stay on a **blank `Color(.systemBackground)` screen matching the empty `UILaunchScreen`** until `isReady` flips straight to the inbox — no per-launch "Updating…" flash. `dbExisted` MUST be sampled before `makePool()` (which creates the file). `AppDatabase()` no-arg init was removed; production builds via `makePool()` + the designated `init(pool:runStartupResets:)`; the test/preview `init(dbPool:)` is now a convenience → `runStartupResets:false` (never touches global UserDefaults flags / FTS dir). `StartupMigrations.resetFlagKeys` is the single source of truth for the 4 reset flags (pinned by a test).
- **The DB build MUST be triggered from a path that runs on EVERY launch — NOT only `body.task`.** A cold BACKGROUND launch of a terminated app (silent push / BGTask / background notification action) does NOT run the SwiftUI `WindowGroup` body / `.task` / `.onAppear` until foregrounded (documented lifecycle) — so a `body.task`-only trigger left `AppDatabase.shared` nil on those launches → BGTask handlers crashed on the `AppDatabase.dbPool` force-unwrap, and push/notification-action handlers hung on `awaitReady()` (dropping the user's action). Fix: `ensureDatabaseReady()` is navigationStore-free and kicked off from `AppDelegate.didFinishLaunchingWithOptions` (always runs) AND `body.task` (foreground, which adds the sidebar load + `isReady`); `awaitReady()` self-bootstraps it. `dbReady` (DB usable) is the gate background paths await; `isReady` (DB + sidebar) is the UI-only gate and is never set on a pure background launch. **EVERY background entry point that touches `AppDatabase.dbPool` MUST `await AppStartup.shared.awaitReady()` first** — both `SyncScheduler` BGTask handlers (`handleBackgroundSync`/`handleBackgroundAIProcessing`) and the AppDelegate push/notification handlers do. Pre-gating this was free because the DB was built in `TabMailApp.init` (always runs); don't reintroduce a view-lifecycle-only trigger.
  - **2026-06-15: the APNs device-TOKEN path was a GAP (boot crash).** `AppDelegate.didRegisterForRemoteNotificationsWithDeviceToken` → `PushNotificationService.didReceiveDeviceToken` → `registerDeviceWithWorker` touched `dbPool` (line ~141) with NO readiness gate. After an app update (migrations delay the DB build), APNs delivered the token before the pool existed → `AppDatabase.dbPool` force-unwrap (`shared.withLock { $0!.dbPool }`) → `EXC_BREAKPOINT` crash on the first boot(s), then it stopped once migrations finished (and a debugger's timing hid it). Fixed by `await AppStartup.shared.awaitReady()` inside `registerDeviceWithWorker`, after the no-session/no-token early returns and before the first `dbPool` access. Lesson: the "push/notification handlers gate" claim covered the *received-notification* callbacks, NOT the *device-token registration* callback — audit every APNs/AppDelegate entry independently.

### GRDB ValueObservation — DO NOT put on `messageHeader`
- **NEVER** use `ValueObservation` to drive the inbox list off the `messageHeader` table. `messageHeader` is written on every sync commit, every AI field update (`actionTag`, `summary`), every unread recount, every backfill batch. A row-level observation would re-emit on every one of those writes, causing full list re-fetch + diff + @Observable invalidation + SwiftUI re-render many times per second during sync bursts. This is strictly worse than the current design.
- **Current design is load-bearing and correct:** explicit `.inboxDataDidChange` notifications fire at deliberate checkpoints; the observer has a 500ms throttle + dirty-bit coalescer; AI updates route through `.messageDataDidChange` into `flushAIBatch` (per-row in-place snapshot replacement, no full reload).
- **`ValueObservation` IS appropriate for `folder` table** — small set (5–50 rows), rarely changes (only at startup classification and folder add/remove), cheap per emission. `InboxViewModel.startFolderObservation()` uses it to drive `VM.folders` authoritatively; dedup on `(id, role)` keeps `unreadCount` churn from reaching the sink.
- `NavigationStore.outboxMessages` correctly uses `ValueObservation` on the outbox table — small set, user-driven writes only.
- Rule of thumb: `ValueObservation` is only OK on tables where write frequency is low (user-initiated, not sync-driven) AND the tracked set is small.

### AI Priority Processing (ADR-IOS-013)
- **Dual-path architecture** (matching TB's `onMessagesDisplayed`):
  - Direct path: `processOpenedMessage()` — when user opens a message in MessageDetailView, AI runs immediately bypassing queue
  - Queue path: `processMessagesForAccount()` — background batch processing with semaphore (32 workers)
- Per-message dedup in AIService prevents duplicate LLM calls between paths
- `fetchBody` also triggers `processMessage` for inbox messages after body is fetched

### IMAP Message IDs & UID Resolution
- `info.messageId` from SwiftMail is often `nil` (many messages lack Message-ID header)
- Fallback chain: `info.messageId ?? UID ?? sequenceNumber` — stored in `MessageHeader.messageId`
- `resolveUID()` helper in IMAPProvider: numeric IDs → construct `UIDSet(UID(...))` directly; non-numeric → search by `Message-ID` header
- All IMAP operations use `resolveUID()` — never raw `server.search(criteria: [.header("Message-ID", id)])`
- **CRITICAL: IMAP MOVE changes UIDs.** When a message is moved between folders via IMAP, it gets a NEW UID in the destination folder. The old UID is invalid. For any undo/move-back operation on IMAP accounts, ALWAYS use `rfc822MessageId` (RFC 2822 Message-ID header) — NEVER the numeric UID. This ensures `resolveUID` does a header search in the destination folder and finds the correct UID. Gmail uses stable IDs (no fix needed).
- **Stale detection UID remap check**: Before deleting a "stale" local message (not in remote set), fullSync checks if any NEW remote message in the same folder has matching `rfc822MessageId`. If found, it's a UID remap (not stale) — local row is migrated in-place to preserve body/AI cache.
- **Self-send appears as two `MessageHeader` rows (INBOX + Sent) with the same `rfc822MessageId`.** Gmail (and other shared-storage IMAP servers) represents this as one underlying message; iOS materializes it as two rows keyed by `folderId`. `markRead`/`markUnread` use `expandWithSiblingsByRfc822` (in `AccountManagerActions.swift`) to flip BOTH rows in one transaction, decrement BOTH folders' `unreadCount`, and queue ONE `PendingOperation` per folder. For Gmail the second op is a no-op server-side; for plain IMAP it correctly issues a `STORE \Seen` against the sibling folder's UID. Sync is the safety net if rfc822 is missing.
- **`MessageHeader.id` vs `stableId` — CRITICAL distinction**:
  - `MessageHeader.id` (GRDB PK) = `"{accountId}:{folderPath}:{messageId}"`. For IMAP, `messageId` = UID which **changes on MOVE**. For Gmail/Exchange, `messageId` is a stable provider ID.
  - `MessageHeader.stableId` (computed) = `rfc822MessageId` for IMAP (survives folder moves), `messageId` for Gmail/Exchange (already stable). Falls back to raw UID if no rfc822MessageId.
  - **Rule**: Any key that must survive folder moves (PendingOperation messageIds, chat session keys, draft keys) MUST use `stableId`, NEVER `message.id`. Using `message.id` causes orphaned records when IMAP messages move folders.
  - **Pattern**: `"{accountId}:{stableId}"` for unique-per-account keys (chat sessions use `"msg:{accountId}:{stableId}"`, drafts use `"reply:{accountId}:{stableId}"`).
  - See `MessageHeader.swift:108` for the stableId implementation.
- **Body architecture (CORRECTED 2026-06-23 — the old "backfill is FTS-only / MessageBody only on open" note was STALE and wrong)**:
  1. **FTS** (`fts.db`) = persistent plain-text store for search. Written by `BodyFetchProcessor.flushBatch`.
  2. **MessageBody (`tabmail.sqlite`, HTML)** = the on-disk body cache. **Written at DOWNLOAD time, not just on open.** `BodyFetchProcessor.process` (`bodyToInsert.insert`, `BodyFetchProcessor.swift:150/182/218`) persists it, and that processor is shared by **all** body-fetch paths: `ActiveBodyQueue` (inbox, enqueued right after each sync via `SyncEngine*.enqueueBatch(newHeaders)`), `BackfillBodyQueue` (all other folders), `AccountManagerFetch.fetchBody` (on open), AND — since 2026-06-23 — the inbox **snippet loader** (`InboxViewModel.loadSnippetBatch` Tier 2: it already downloads the whole body for the snippet, so it now routes it through `BodyFetchProcessor` to cache it instead of discarding — this fixed the "first open re-downloads + renders slowly" report; `enableAI = isInInbox` mirrors the Active/Backfill split).
  3. **Eviction is OVER-BUDGET ONLY** — `SyncEngine.runPruneIfOverBudget` deletes bodies (then headers, oldest-first, keeping `floorPerFolder`) only when `StorageEstimator.isOverBudget()` (2 GB default). There is NO aggressive TTL eviction (`evictStaleBodies` no longer exists). So a fetched body stays cached until the budget is hit.
  4. **The "open re-downloads" you may still see is a RACE** (open before the background body fetch reached that message) or a body pruned when over budget — NOT a missing-cache bug. `loadBody` checks `MessageBody.fetchOne` first and skips the network if present.
  5. **Memory** = zero bodies in memory except the one currently displayed.
- Snippets derived from body text during FTS indexing / body processing. Failed fetches get sentinel `" "` to prevent retry.
- **NEVER fetch bodies inline during sync or backfill header insertion** — body FTS indexing runs as bounded background pass after header backfill completes.
- **Calendar invite ICS resolution is on-demand for Gmail/Exchange + IMAP-batch (race fixed 2026-05-26)**: A `text/calendar` part is classified as an *attachment*, and its ICS bytes are resolved via a SEPARATE network round-trip (`attachmentFetcher`) for Gmail/Exchange (their `FullMessageInfo.icsData` is always `nil`) and for IMAP *batch* fetch when the pipelined part-fetch drops the calendar section. The IMAP *single* user-open fetch (`fetchMessageOnConnection`) reliably prefetches all parts via `fetchAllMessageParts`, which is why **reloading an invite fixes the display**. Previously `BodyRenderer.render` swallowed that on-demand fetch failure with `try?` → empty HTML → `BodyFetchProcessor.process` hit the `else if hasAttachments` branch (true, the invite IS an attachment) → persisted an empty body + `bodyComplete=1` → MessageCardView showed "This message has no content." permanently. Fix: `RenderedBody.hasUnresolvedICS` is set when this render *owned* ICS resolution (fetcher present, no prefetch) but got no usable bytes (`do/catch`, not `try?`); `process` excludes that case from the attachment-only write (`hasAttachments && !hasUnresolvedICS`) and routes it to the retry path bounded by the 3-strike `emptyFetchCount`. NSE passes no fetcher → never flagged (it defers ICS to the main app by design). Tests: `BodyRendererTests` `unresolvedICSWhenFetchThrows` / `resolvedICSOnDemand` / `noFlagWithoutFetcher`.

### iOS Keychain Persistence
- Keychain items survive app deletion/reinstall
- Must detect fresh installs via UserDefaults flag and clear stale Keychain data

### Persistent Offline Action Queue (ADR-IOS-003, ADR-IOS-018)
- **Core principle: Never Drop User Intention** — see CLAUDE.md and DECISIONS.md foundational principle. Both PendingOperation and OutboxMessage implement the same contract: persist before acknowledge, execute asynchronously, survive crashes/kills/disconnections.
- `PendingOperation` GRDB model with `status` (queued/inFlight), supports `setTag`/`removeTag` ops
- Pattern: optimistic local update → `queuePending()` → async `drainPendingQueue()` (AccountManager)
- Queue drains on: network restore (`NetworkMonitor`), foreground return, after each sync poll, app launch
- Conflict detection: "message not found" errors during drain → drop op (server wins)
- **Remote state wins on conflict** — IMAP tag changes from other instances (TB addon) override queued local tags. The most recent writer wins regardless of device origin.
- Sync protection: messages with pending ops skipped during stale detection and re-insertion
- Tag writes (AI + manual) go through queue — replaces `writeActionTagWithRetry`
- Undo: cancels queued ops if still pending, otherwise queues counter-operation (move-back)
- All action methods are synchronous (no async/throws) — queue handles remote execution

### Progressive Backfill & Storage Budget (ADR-IOS-005, ADR-IOS-006)
- **No age cutoff** — backfill always walks to completion: IMAP walks UID range from UIDNEXT-1 to UID 1, Gmail/Exchange exhausts all page tokens. Storage budget is the only gate.
- `StorageEstimator.budgetMB` (UserDefaults, default 2048) — global storage soft cap across all accounts
- After initial sync, `SyncEngine.startBackfill()` fetches older messages in background
- **Backfills ALL folders** (not just primary/secondary) — sorted by priority: inbox → favorites → secondary → custom
- IMAP backfill uses UID range walking (no SEARCH, avoids NIO 8KB buffer limit)
- Gmail/Exchange uses page-token cursor walk from newest to oldest
- **Backfill inserts via GRDB**: `insertBackfillBatch` uses `dbPool.write { }` — GRDB's `DatabasePool` is thread-safe, no background DispatchQueue or ModelContext needed.
- **Strictly incremental backfill** (no large object materialization): Two-step approach — (1) provider SEARCH/LIST returns lightweight IDs, (2) batch `fetchSet` existence checks via GRDB (never materializes objects), (3) FETCH only missing IDs. `insertBackfillBatch` uses a batch `fetchSet` for final dedup (one query, not per-row). Returns `(inserted, ftsRecords)` — FTS indexing coalesced per window by caller.
- `Folder.backfillComplete` / `Folder.oldestSyncedDate` track per-folder progress
- Backfill pauses for user activity via `signalUserActivity()` → `waitForIdle()` at folder, window, deep-crawl, BodyFTS, and SnippetFill boundaries. Inner loops also check `isUserActive` per-iteration for near-immediate yield. Views signal activity via `.onScrollPhaseChange` → `AccountManager.signalViewActivity()`
- **IMAP pool priority checkout**: `pool.checkout(priority: true)` inserts at FRONT of waiter queue — used by all user-initiated IMAP methods (fetchMessage, markRead, archive, delete, move, search, fetchAttachment, fetchOlderMessages). Background backfill batches use `priority: false` (FIFO). Ensures user ops never wait behind multiple background batches.
- **fullSync**: GRDB writes are immediate and don't trigger SwiftUI re-renders directly — `NavigationStore` handles UI refresh.
- **No age-based eviction** — fetched messages are never deleted based on age
- **Storage pruning**: `pruneIfOverBudget()` runs after sync — deletes bodies first (re-fetchable), then headers, oldest-first across ALL accounts, but keeps ≥50 messages per folder
- **StorageEstimator**: measures actual file sizes (GRDB `tabmail.sqlite` + FTS `fts.db` + WAL/SHM files) across Application Support
- **MessageBody**: only stores `htmlContent` (no `textContent`). Plain-text-only emails converted via `plainTextToHTML` on ingest using RFC 3676 format=flowed: trailing space before line break = soft wrap (join with next line), no trailing space = hard break (preserve). `white-space: pre-wrap` on container preserves multiple spaces. FTS holds stripped plain text for search.
- **Infinite scroll**: `fetchOlderMessages()` in SyncEngine/AccountManager, triggered by sentinel view at bottom of inbox list. IMAP uses bounded SEARCH windows to avoid buffer overflow.
- **Power-aware backfill (BackfillProfile)**: Two profiles — `normal` (default) and `aggressive` (on power + user idle 30s+ + not low-power + not thermally throttled). Aggressive mode uses larger chunks (1000 vs 500), higher batch sizes, and much shorter delays (0.1s vs 0.5s between batches, 0.5s vs 3s between cycles). `UIDevice.current.isBatteryMonitoringEnabled = true` set in TabMailApp.init. Profile checked dynamically at each decision point via `SyncEngine.backfillProfile`. **Battery gate**: backfill pauses entirely when battery < 20% and not charging (`shouldPauseBackfill`). **Cellular gate**: on metered connections (`NetworkMonitor.isExpensive`), only inbox-role folders are backfilled.
- **Per-batch pool checkout**: `fetchMessageHeaders` checks out a pool connection per batch (100/200 UIDs) instead of holding one for the entire call. Each batch gets a fresh connection, re-SELECTs the folder, and returns it. Other operations can use the pool between batches.
- **Connection failure backoff**: After 3 consecutive connection failures within a backfill cycle, the cycle aborts. Next sync poll restarts backfill with a fresh connection.

### Backfill diagnostics: `backfill.log` channel (2026-07-02)
- Key backfill lifecycle events now route through **`BackgroundSyncLogger.logBackfill`** → `backfill.log` (debug-gated, exported as "Backfill Logs" in the Debug menu): walk worker START/EXIT, cycle start+profile, pause reasons (low-battery / WiFi-only-on-cellular), folder fully-crawled, failed-range retries, connection-failure backoff, learned server connection limit, body-queue repopulate/batch DONE/FAILED, miss/confirm-gone/re-key outcomes. Fine-grained per-chunk logging (`[UIDCursor]`, SEARCH found counts, dispatch internals) remains `print()`-only (console). Historical note: before 2026-07-02 ALL backfill logging was print()-only, so exported logs showed nothing about backfill stalls; `backfill_ai_logs.txt` is the **BackfillAIQueue** (AI), not the header/body backfill.

### Backfill stall fixes (2026-07-02) — UID-remap re-key, no fast-sync cursor reset, persisted server limit
- **"Stuck at 99%" root cause**: a message whose stored UID died (UIDVALIDITY rotation / cross-client move outside the full-sync remap window) missed on every batch fetch; at `missFetchCount >= 5` the rfc822 probe said "still exists" → counter reset → retry the dead UID forever → `pendingBodyCount` never 0 → `isFullyComplete` never fires. **Fix**: `GoneConfirmation.stillExists(newUID:)` carries the server's current UID (`MessageExistenceProbe.currentUIDs`, highest-UID-wins for duplicates); `BackfillBodyQueue.rekeyRemappedHeader` migrates the header in place (delete+reinsert PK, body preserved, `SearchIndex.rekeyHeaders` moves the FTS entry) and the body is fetched under the new UID. `newUID == nil` (same UID still present) = transient miss → reset counter (old behavior). `ActiveBodyQueue` (inbox) previously had NO miss machinery at all (retried dead UIDs forever) — it now mirrors the same wiring, reusing `confirmGoneAtThreshold`/`rekeyRemappedHeader` cross-actor (they only touch DB/FTS, never queue storage). Tests: `ConfirmGoneAtThresholdTests`, `HandleMissedItemsTests` (incl. re-key + mass-remap regression).
- **`resetForFastSync()` REMOVED**: it nulled `backfillUidCursor`/`backfillPageToken` on every incomplete folder each time FastSyncView appeared — restarting partially-walked folders from the top (IMAP re-fetched every existing header; Gmail/Exchange re-paged all known pages) and racing running workers' 1Hz cursor snapshots (it didn't cancel them). Fast sync now only flips the profile to `.turbo`; walks resume from stored cursors. Do NOT reintroduce a cursor reset on the fast-sync path — `resetCrawlState()` (Smart Reindex) is the deliberate full re-walk.
- **IMAP server connection limit is persisted** per host+username (`IMAPProvider.persistedServerLimit`, UserDefaults `imapServerConnLimit:<user>@<host>`), loaded in `init`. Before: the limit was learned only from a live `mail_max_userip_connections` rejection and kept in memory, so every launch re-oversubscribed (up to 18 walk workers vs e.g. Dovecot 15/user+IP → login failures → walk workers break → backoff, plus bgSync failures). No artificial worker cap — walk parallelism stays `poolMaxConnections()` (deliberate: max speed; the persisted limit is the only brake).

### `bodyComplete` = FTS-indexed truth; display cache has NO flag (ADR-IOS-050, 2026-07-02)
- **`bodyComplete` means exactly one thing: body text is indexed in FTS.** Set only after a confirmed FTS write; cleared only when the indexed truth is invalidated (user Refetch, FTS-loss self-heal, Smart Reindex). The rendered-HTML cache (`messageBody` row + `BodyAssetStore` files) has no flag — **the row's existence IS the cache state**; detail view fetches on cache-miss. All three cache evictors (`runEvictStaleBodies` TTL, `runPruneIfOverBudget` storage budget, `BodyAssetMaintenance` asset cap) delete rows WITHOUT touching `bodyComplete`.
- **The bug this fixed ("indexing goes backwards then forwards")**: `BodyAssetMaintenance` used to flip `bodyComplete=0` per eviction victim → every victim re-entered the backfill body queue → refetch → asset cache refilled past its 1 GB cap → evicted again. Closed loop; `backfill.log` showed pending 6,788→9,347 while batches completed. NEVER reintroduce a `bodyComplete` write in any cache-eviction path, and never add a "cache present" flag (drift). Regression: `BodyCompleteRestoreTests`.
- **One-time reverse heal** `oneTimeBodyCompleteRestore` (gate `bodyCompleteRestore.v1.done`, runs in the startup healTask): pending rows with FTS body text (`SearchIndex.headerIdsWithFTSBody`, length>1) AND no `messageBody` row → `bodyComplete=1`, zero network. The no-cached-HTML guard keeps NSE unresolved-CID mail (no FTS body anyway) and suspension-aborted flag writes on the refetch path.
- Asset evictions now log to `backfill.log` (`[AssetEvict] reason: N messages, X MB reclaimed`). `EmailReadTool` body fallback: MessageBody → FTS `bodyText` → snippet.

### Backfill / Fast Sync Completion — gate on `pendingBodyCount`, NEVER a server total
- **`BackfillProgress.isFullyComplete` gates on `headersDone && totalEmails > 0 && pendingBodyCount == 0`** (`AccountManager.swift`). `pendingBodyCount` = body-eligible headers still awaiting fetch (`headerComplete=1 AND bodyComplete=0 AND bodyEmptyConfirmed=0`), the same criteria `BackfillBodyQueue`/`ActiveBodyQueue` select on. It is local and self-terminating (empty/404/oversized bodies confirm-empty), so it reaches 0 once the body queues have nothing fetchable left.
- **DO NOT gate completion on `ftsIndexed >= totalEmails`.** For Gmail/Exchange, `totalEmails` is a *server-reported* count that counts a **different population** than the mail headers we store, and can permanently exceed it → completion becomes unsatisfiable:
  - **Exchange**: `serverMessageTotal = SUM(folder.totalCount)` = sum of Graph `totalItemCount` across ALL folders, incl. Deleted Items, Junk, and hidden/system folders (`includeHiddenFolders=true` in `ExchangeProvider.fetchFolders`), plus non-mail items that `parseGraphMessage` drops (`compactMap` → nil). One row per message (folders don't overlap).
  - **Gmail**: `getMessagesTotal()` = `profile.messagesTotal`, **deduplicated** across labels (and includes Spam/Trash/Chat). But `grdbTotal` counts per-`(label, message)` rows (`MessageHeader.id` is keyed by `folderPath`), so the units don't match — Gmail "worked" only by accident when label memberships inflated `grdbTotal` past `messagesTotal`.
  - **IMAP**: unaffected — `uidTotal > 0`, so the server-total override is skipped and `totalEmails = grdbTotal`.
- **`totalEmails` (server total) is only the progress-BAR denominator while crawling** (`if uidTotal == 0 && !headersDone` in `SyncEngineBackfill.updateBackfillProgressForAccount`). Once `headersDone`, it falls back to `grdbTotal` so the bar reaches 100% consistently with completion. `serverMessageTotal` is cached once per session and never reconciled down.
- **Symptom of the old bug** (fixed): an Outlook/Exchange account that "never completes sync" — Fast Sync screen stuck below 100% / no "Sync Complete", Settings sync row unfinished, and the chat pill's "agent search results may be incomplete" banner (`DynamicIslandChatButton`, driven by `isBackfillInProgress`) stuck on forever. It is a **completion-criteria/reporting defect, not a runaway loop** — the backfill task itself exits on `isFolderWalkComplete` (headersDone), independent of `isFullyComplete`. The "deleted messages aren't counted right" intuition was exactly correct: `totalItemCount` over-counts deleted/hidden/non-mail items. Regression: `BackfillProgressCompletionTests`.

### Ever-Rolling FIFO Queues (ADR-IOS-027)
- **Philosophy**: Items NEVER leave queue until confirmed success or confirmed stale. No fire-and-forget.
- **Dispatch pattern**: move to back → mark in-flight → fire task → on success: remove; on failure: clear in-flight, item stays at back for retry.
- **Two-phase dispatch**: Phase 1 synchronous (collect candidates, no await). Phase 2 async (resolve deps, launch tasks). Prevents actor reentrancy bugs.
- **Boot-time recovery**: Every queue has `repopulateFromDatabase()` called from `SyncScheduler` on foreground return + BGProcessingTask.
- **Queues**: `ActiveBodyQueue`, `ActiveAIQueue`, `BackfillEmbeddingQueue` — all follow identical pattern.
- **No pause mechanism** — IMAP pool priority checkout handles user-vs-background contention naturally.
- Self-heal/consolidation should never need to do real work — purely a safety net.

### Bounded Memory (CRITICAL)
- **Every loop/batch operation MUST process a bounded number of items** — never accumulate unbounded arrays or materialize all objects
- All chunk/batch sizes are centralized in `SyncConfig` enum (`Services/Sync/SyncConfig.swift`) and `BackfillProfile` enum — never hardcode numeric limits elsewhere
- Current defaults (normal profile): sync=50, snippets=25, backfill=500, prune=50, FTS index=200, embeddings=50, body FTS=20
- Aggressive profile: backfill=1000, IMAP fetch batch=200 (per-lock), Gmail fetch batch=100, body FTS=50
- GRDB queries: use `Column("folderId") == fid` where `folder.id` is `"accountId:path"`, unique across accounts

### Optimistic UI Rollback
- `toggleRead`/`toggleFlag`: change local state first, revert on remote failure
- `archive`/`delete`/`move`: local state only updates after remote success — no rollback needed, but error shown
- **Never use `try?`** for remote operations — always `do/catch` to show errors and revert
- **Same-role actions are no-ops (2026-06-09): archive-from-Archive AND delete-from-Trash, guarded at three layers**: (1) `AccountManager.move` filters out messages whose `folderPath == destinationPath`, and `AccountManager.archive/delete` drop messages via `messagesNotInRole` (choke points for ALL surfaces incl. agent tools); (2) `InboxViewModel.archive`/`archiveThread`/`delete`/`deleteThread` early-return (no undo entry, no overlay) and expose `archiveIsNoOp(_:)` / `deleteIsNoOp(_:)`; (3) the eight InboxView archive/delete handlers guard on those predicates BEFORE inserting into `dismissedMessages` — the dismiss-then-act ordering means a VM-only guard would still make the row vanish. `MessageDetailViewModel.archive()/archiveMessage()/delete()/deleteMessage()` return `@discardableResult Bool` ("performed") so detail-view call sites skip dismiss/flash on no-op.
  - **The check MUST be role-based first, path-comparison second.** Accounts can carry MORE THAN ONE folder per role (e.g. iCloud "Trash" + "Deleted Messages" — the v48 dedup migration left edge cases; Account Detail shows a ⚠ icon). `lookupFolderPath(accountId:role:)` is `fetchOne`-arbitrary among duplicates, so a path-only no-op check fails in the folder the user is viewing AND the "delete" silently moves the message to the OTHER trash folder — which in the unified Trash view keeps it inside the folder set, so `loadedMessages` never shrinks, the `expandedThreads` prune never fires, and the expansion/selection visuals linger. This was the trash-only bug report of 2026-06-09; archive worked only because that account had a single archive-role folder.
  - Drafts unaffected (draft deletion goes through `deleteDraftMessage`, not move-to-trash). Delete-in-Trash does NOT perma-delete — it's a plain no-op by design. Tests: `SameFolderNoOpTests` (incl. duplicate-trash scenarios).
  - **A no-op `Button(role: .destructive)` in `.swipeActions` still ghosts the row.** SwiftUI plays its automatic destructive row-removal animation when the button activates, REGARDLESS of what the action closure does — a guard-and-return closure leaves the row yanked-then-snapped-back with selection-looking artifacts (the residual "trash still broken" report of 2026-06-09; archive looked fine only because its button has no destructive role).
  - **UX decision (user, 2026-06-09): same-role no-op swipe buttons stay VISIBLE but GRAYED OUT** (`disabledSwipeTint` = systemGray3); tapping just closes the swipe menu (action no-ops via the handler guards). Do NOT hide the buttons (tried, rejected). In trash contexts the Trash button drops its `.destructive` role (ghost-animation above) — `Button(role: isTrashContext ? nil : .destructive)` + conditional tint via `isTrashContext`/`isArchiveContext` (mirrors `isDraftsContext`). Handler guards log `[NoOpGuard] …` via `BackgroundSyncLogger.logInbox` (console + inbox.log) for repro confirmation.
  - **FIXED (2026-06-09): Gmail optimistic move left a duplicate row.** Optimistic move keeps the PK (`acct:INBOX:gid`) while setting folderId/folderPath=TRASH; for stable-id providers the remnant's messageId stays in the remote set forever, so it never reaches the stale/UID-remap path that re-keys IMAP rows — and historical insert paths left BOTH the remnant AND a canonical `acct:TRASH:gid` row → phantom 2-member self-threads in Trash (evidence: logmain.log 2026-06-09 `[UndoStack] DB state` dump for msgId 19eaf470a1c6e217). Fix: `SyncEngine.canonicalizeLocalRows` runs in the fullSync upsert loop — merges duplicates (preferring the canonical-PK row, keeping AI fields + richest body; `bodyComplete`/`bodyEmptyConfirmed` are NOT OR-merged — they must describe the survivor's OWN FTS row) and re-keys remnants to the canonical PK (delete+reinsert since `messageBody` FK CASCADE forbids PK UPDATE; body reattached; canonical-PK-held-by-another-folder collision skips the re-key). Merge-loser ids ride `staleIds` out of FTS. Drafts/Sent exempt (DraftStore manages row identity). Trash is in `secondaryRoles` → existing duplicates self-heal on the next periodic fullSync or on opening the folder. The synthetic `__GMAIL_ALL_MAIL__` archive heals too — `GmailProvider.fetchMessages` implements the All-Mail query, so on-demand/periodic sync runs the canonicalizer there (field-verified 2026-06-09: 86 trash + 75 archive re-keys + the phantom merge, 0 collisions). Known residual: FTS→GRDB orphans from the pre-fix era (FTS entries whose header id no longer exists) have no pruning pass — occasional stale search hit with degraded display; bounded, can't grow anymore. Tests: `HeaderCanonicalizeTests`, `SearchIndexRekeyTests`.
  - **Header re-keys go through `SearchIndex.rekeyHeaders` — IN-PLACE id update, never delete+reindex.** FTS rows are keyed by `message_ids.headerId` (PK) → rowid; the rowid carries the indexed BODY text and the `messages_vec` embedding. Deleting + re-indexing loses both (body refetch + re-embed churn; the PLAN_FTS_BODY_LOSS class). `rekeyHeaders` UPDATEs `message_ids.headerId` + `message_meta.headerId` (+ FTS `msgId` column on UID remaps), with a collision fallback (newId already indexed → drop the old entry). The `SyncMessagesResult.ftsRekeys` channel feeds it from both consumers (`processSyncResult` + instance `syncMessages`). The pre-existing **UID-remap path never touched FTS at all** (ghost entries under dead header ids + new ids invisible until backfill self-heal) — it now emits `ftsRekeys` too. Beware: `staleIds` was ASSIGNED (`= staleFiltered.map(\.id)`) mid-function — it's now `append(contentsOf:)` so earlier appends survive.

### Gmail Label System
- Gmail folder paths are label IDs: `"INBOX"`, `"SENT"`, `"TRASH"`, `"SPAM"`, `"DRAFT"`
- Gmail has no explicit archive label — archiving = removing `"INBOX"` label
- Gmail undo archive: `move(from: "", to: "INBOX")` — removing "" is a no-op, adding INBOX works
- IMAP undo: must pass real folder paths (e.g., `"Archive"`, `"Trash"`) from `account.folders`
- Gmail delta sync uses `history.list` API — returns nil on 404 (expired historyId), falls back to full sync
- `Account.lastHistoryId` stores the Gmail history cursor; captured after each full sync via `getCurrentHistoryId()`
- **Gmail IMAP keywords ≠ Gmail REST API labels**: Gmail does NOT create REST API labels from IMAP keywords set via `STORE +FLAGS`. (Historical context — iOS no longer writes either; see ADR-IOS-036.)
- **Action tags are local-only (ADR-IOS-036).** iOS does NOT write `tm_*` Gmail labels / IMAP keywords / Exchange categories. `MessageHeader.actionTag` is populated by `MessageAICache.restoreIfCached` + AIService only; never from provider labels. Cross-instance state is served by Device Sync `ai_cache_probe` when peers are online, and iOS re-runs the LLM when they're not. Any `tm_*` labels still present on user servers from prior TabMail versions are treated as residue and filtered by `UserLabelStore.shouldExcludeLabel` (which matches the `tm_` prefix).
- **Synthetic `__GMAIL_ALL_MAIL__` must NEVER reach the Gmail API** — it is TabMail's internal All Mail/archive folder path (`GmailProvider.archivePath`), not a real label ID; Gmail returns 400 "Invalid label". Every method that scopes by folder must translate it: omit `labelIds` and add `GmailProvider.allMailExclusionQuery` to `q` (see `fetchMessages`, `search`, `listBackfillMessageIds`, `listOlderMessageIds`, `listMessageIdsPage`, `fetchOlderMessages`). `GmailProvider.request()` has a boundary guard that throws `ProviderError.syntheticFolderPath` if the sentinel leaks into an API path (caught a real bug 2026-06-09: `search()` was missing the translation, breaking remote search and contributing to a search-mode hang).

### SwiftUI Observable Array Mutation Safety
- **NEVER remove items from an `@Observable` array synchronously during a lifecycle callback (`onAppear`/`onDisappear`) when that array feeds the same `ForEach`** — SwiftUI is mid-layout and will crash
- **Safe pattern for removals**: defer to the next run loop via `Task { @MainActor in ... }` — this lets the current layout pass complete before the mutation fires
- **Appending is generally safe** from lifecycle callbacks — new items don't invalidate existing layout
- **User action handlers** (button press, swipe action, gesture) are safe for both append and remove — SwiftUI processes these between layout passes
- When evicting from a paginated array, keep evicted IDs in the dedup set to prevent infinite re-fetch cycles — reset the set on full reload only
- Example safe eviction:
  ```swift
  // Called from onAppear flow — must defer
  private func scheduleEvictionIfNeeded() {
      guard loadedMessages.count > maxLoaded else { return }
      Task { @MainActor in  // Deferred — safe
          self.loadedMessages.removeFirst(trimCount)
      }
  }
  ```

### SwiftUI Layout Gotchas
- **Label width in compose rows**: `labelWidth` constant in ComposeView must accommodate the longest label ("From" = 4 chars). Currently 42pt at `.subheadline`. If adding longer labels, increase this.
- **Custom Layout (FlowLayout)**: Unlike built-in views, custom `Layout` protocol implementations don't fill available width. Always add `.frame(maxWidth: .infinity, alignment: .leading)` on the container.
- **HStack baseline alignment**: Use `.firstTextBaseline` (not `.top`) when mixing labels with text fields of different font sizes — avoids manual padding hacks.
- **Menu vs Picker for full-width dropdowns**: Use `Menu` instead of `Picker` when you need the collapsed label to fill available width with truncation. `Picker` auto-sizes its label to content width.
- **Menu tint override**: SwiftUI `Menu` applies the default accent (blue) tint to its label, overriding any `.foregroundStyle()` on the label content. Always add `.tint(.primary)` on the `Menu` itself when you want non-blue icons.
- **NEVER nest `.animation(_:value:)` modifiers** on ancestor views of a ScrollView+LazyVStack — multiple parent `.animation()` modifiers create competing animation contexts that cause layout feedback loops (infinite hang). Use explicit `withAnimation` at state change sites instead. ComposeView (one `.animation()` at root) works; InboxView/MessageDetailView (multiple nested `.animation()`) caused hangs.
- **ScrollView bottom-pinning**: Use `.defaultScrollAnchor(.bottom)` + `ScrollPosition` with `.scrollPosition($pos)` for chat-style scroll. Use `pos.scrollTo(edge: .bottom)` in `onChange` handlers for discrete events (message count, isWorking, expand, keyboard focus). Avoids manual per-line `scrollTo` which causes layout feedback at high frequency.
- **NEVER use LazyVStack in chat-style ScrollView with `scrollTo`**: LazyVStack recycles off-screen cells, causing `contentHeight` to fluctuate wildly (e.g. 4513→917→3148) when scroll position changes. This creates visible jumps. Use regular `VStack` instead — chat pills have <20 messages so lazy loading provides no benefit. ([Apple Forums thread](https://developer.apple.com/forums/thread/685461))
- **ProgressView + `.geometryGroup()` + `.background(.regularMaterial)` in ScrollView**: this combination can cause layout loops. Removing `.geometryGroup()` and parent `.animation()` modifiers fixes it. `.geometryGroup()` is rarely needed if expand/collapse uses explicit `withAnimation`.
- **No sticky/floating section headers** — `List` pins section headers on scroll; `Form` does not. Always use `Form` (not `List`) for settings-style screens to avoid floating headers. This is a design rule.

### HTML Email Render Pipeline (AutoSizingHTMLView) — MUST stay idempotent (ADR-IOS-039)
- The render is a measure→mutate→re-measure pipeline with **two feedback-loop arms**, both now closed. Do not reopen either:
  - **Height arm** (closed earlier): `html,body{height:auto;min-height:0;max-height:none}` in `EmailHTMLWrapper` stops `scrollHeight` inheriting the layout-viewport floor; `monitorHeightJS` reads `window.__tmLayoutVp` (not `innerWidth`, WebKit bug 170595).
  - **Width arm** (closed 2026-06): `fitViewportJS` has an **idempotency guard** — bails if `window.__tmLayoutVp` is already set. Without it, every re-entry (esp. the `didBecomeActive` foreground re-fit) re-measured a widened document against an unreliable `innerWidth` and re-mutated it → fonts shrank on every background→foreground cycle.
- `fit()` stamps `window.__tmDeviceWidth` from `webView.bounds.width` before running the script — **never trust `window.innerWidth`** anywhere in this pipeline (bug 170595). `monitorHeightJS` vp fallback chain: `__tmLayoutVp || __tmDeviceWidth || innerWidth`.
- Re-fit after a REAL width change (rotation/sheet resize) goes through `viewportResetJS(deviceWidth:)` which clears `__tmLayoutVp` + restores `width=device-width` BEFORE `fit()` — required or the guard bails and heights scale against the stale viewport.
- **ScrollFreezeGate** (in AutoSizingHTMLView.swift; Mutex-based, NOT @MainActor — the Coordinator is nonisolated): while the detail List scroll phase is non-idle, Coordinators buffer changed heights in `pendingHeight` and flush on `.scrollFreezeReleased`. Prevents row resize mid-pan → List self-sizing repositioning rows under the finger → overlapping cards. Driver: `MessageDetailView` `.onScrollPhaseChange`; `end()` also called in `onDisappear` (a stuck gate freezes height application process-wide). Exception: `height <= 1` (never-sized row) applies immediately.
- Key invariant making this work: `handleHeightMessage` only writes `height` when the value CHANGED — so idempotent re-measurement is free, and only genuine changes (late image loads) ever hit the freeze buffer.
- **HeightSeedCache** (in-memory, NOT disk — ADR-004): List dismantles far-offscreen rows and **`@State height` does NOT survive** recreation (log-proven 2026-06-09: same message reloaded 5×/session with `frameH=1` at onload — row collapsed to 1pt and re-inflated mid-scroll = the residual overlap). `AutoSizingHTMLView.init` seeds `@State` from the cache (keyed by `headerId`); Coordinator writes on every applied height. Seeded row re-enters at its real height; the idempotent re-measure returns the identical value → `!=` guard drops it → row never moves. Seeding is only sound BECAUSE the fit is idempotent — never add a seed/persist layer to a non-convergent measurement pipeline.
- **`img { height: auto }` MUST be scoped to `img[width]`, NOT applied to every image** (`EmailHTMLWrapper.swift` ~line 131). The wrapper's responsive-image reset was `img { max-width: 100%; height: auto; }`. `height:auto` (author CSS, specificity 0,0,0,1) OVERRIDES an email's `height="29"` presentational attribute (specificity 0). For a logo sized ONLY by `height="29"` with no `width` attr (the Apple Support survey header), stripping the height removed its only size cap → the image rendered at its full natural size capped to the container width (a ~29px logo **ballooned to ~420px wide+tall**), overflowing the 288pt device viewport. That cascaded: `fitViewportJS` saw a 420px culprit → widened the layout viewport to 440 → whole email scaled down and the **right edge clipped** (screenshots 2026-06-16: giant black Apple logo + "…how satisfied were you[r]" / "Apple Suppor[t]" cut off). Apple Mail / Thunderbird render the logo small because they honor the `height` attr. **Fix:** `img { max-width: 100%; }` for all images + `img[width] { height: auto; }` only for images carrying an explicit width attr (where capping width via max-width must scale height to avoid distortion). A width-less image keeps height:auto behavior implicitly (height defaults to auto when only width or neither is set), so honoring its explicit `height` is safe. Test: `EmailRenderPipelineTests.imgHeightAutoScopedToWidthAttr`. **Misdiagnosis log (don't repeat):** first attributed it to fitViewportJS measuring a *not-yet-loaded* image (added a skip-loading-images guard) — WRONG; `logmain.log` showed `skipped 0`, the image was fully loaded and genuinely 420px because of the `height:auto` balloon. Guard reverted.
- **`fitViewportJS` widen is ITERATIVE (re-measure after reflow), not measure-once** (`measureMaxRight()` helper + `MAX_PASSES=4` loop). After the logo fix the Apple survey still clipped "a bit on the right": `table#main` (`width="100%"`) has a 420px min-content while its parents are 288, so we widen to 420 — but 288→420 crosses the email's OWN `@media (max-width:415px)` query, flipping it out of the mobile layout into a wider one (desktop voting buttons `width:442px` etc.) that overflows the 420 we set. A single measure-then-widen undershoots → right clip. Fix: after each `meta width=` set, force a synchronous reflow (`void documentElement.offsetHeight`) and RE-measure `maxRight`; widen again to the revealed width. Bounded: the loop only GROWS `targetWidth`, stops when a pass asks for no more than already set (so `width:100%` tables that track the viewport can't loop), pass-capped at 4, ceiling 1200. All passes run in ONE JS execution (synchronous reflows, no paint between) so the user sees only the final widen — no flicker. `window.__tmLayoutVp` is set to the FINAL `targetWidth` after the loop. This email's mobile min-width (420) exceeds its own mobile breakpoint (415), so there is NO width where its mobile layout both fits and is active — widening to the stable desktop width + scaling is the only clip-free outcome (matches mainstream clients). Test: `EmailRenderPipelineTests.fitViewportIterativeWiden`. The loop is hard-bounded (can't recreate the ADR-IOS-039 width/height runaways): synchronous `for` with (1) `pass < MAX_PASSES`, (2) monotonic non-decreasing `targetWidth` + no-progress break (`want <= targetWidth`), (3) 1200 ceiling + explicit break; never calls `fit()` (no re-entry), no paint between passes, and sets `__tmLayoutVp` so the idempotency guard blocks future re-entry. Test: `fitViewportWidenTerminates`.
- **`fitViewportJS` RUNAWAY GUARD — abort the widen on a FLUID culprit instead of shrinking to a tiny scale (2026-06-29, Scholar Inbox digest).** Symptom: the whole email rendered at "desktop size" — i.e. **shrunk to a sub-0.5× scale** (tiny text, whole newsletter crammed in), while Apple Mail renders the same email at device width with large readable text. `logmain.log 2026-06-29`: the culprit was the header logo `<a><img width="160" height="auto" style="max-height:40px;width:auto;…"></a>`, and it was **deferred** (our `data-tmsrc` first-paint optimization strips the `src`). An unloaded `width:auto` image is NOT clamped by `img{max-width:100%}`, so it overflowed device width by only 43px at `vw=288` — but because it's *fluid* (`max-width:100%`), it GREW with every widen: the loop ran `288→400→499→648→873`, never converging, and committed `873px → 0.33× scale`. The idempotency guard then locked that scale even after the image loaded and would have fit. **The widen loop's old termination assumption ("a width:100% element fills whatever viewport we set, so its remeasure is within slop → immediate break") is FALSE for an unloaded/`width:auto` image** — it overflows the viewport you give it, so the loop chases it to the cap. **Fix:** a `converged` flag (set only on the no-progress `want <= targetWidth+OVERFLOW_SLOP` break) + a post-loop guard: `if (!converged && targetWidth < 1200 && maxRight > targetWidth + OVERFLOW_SLOP)` → **revert the meta to `width=device-width, initial-scale=1`, leave `__tmLayoutVp` unset, report height at 1.0×, reveal** (Apple-Mail-like). Discriminator rationale: genuinely fixed-width content CONVERGES (once viewport ≥ its intrinsic width it fits → `maxRight <= targetWidth`); fluid content never does. The `targetWidth < 1200` gate preserves scale-to-fit for genuinely-wider-than-cap FIXED emails (clipping those would lose real content; fluid ones don't lose content — `html{overflow-x:clip}` hides the transient overflow and the loaded image's `max-width:100%` snaps it inside the device-width container). Only changes behavior for the runaway case (which always produced an unreadable sub-0.5× scale). Test: `EmailRenderPipelineTests.fitViewportRunawayGuard`. NB: this is the *width-arm* sibling of the `height:auto` logo-balloon fix (line above) — both are "a not-fully-constrained header image perturbs `fitViewportJS`", but that one was a loaded image ballooned by `height:auto`, this one a *deferred/unloaded* image ballooned by `width:auto`.
- **`measureMaxRight()` excludes not-yet-loaded images from the overflow scan — the UPSTREAM fix for the above runaway (2026-06-29, layer 2; the runaway guard is the downstream backstop, layer 1).** Root of the phantom overflow: `fitViewportJS` runs at first-layout/`didFinish`, BEFORE deferred images load (`deferredImageLoadJS` swaps `data-tmsrc`→`src` only AFTER first paint). An unloaded `width:auto` `<img>` has no reliable intrinsic size and balloons past its container, so `measureMaxRight` saw a 43px phantom overflow and widened. **Fix:** in `measureMaxRight`, before the rightmost-edge scan, hide (`display:none !important`) every `<img>` matching `hasAttribute('data-tmsrc') || hasAttribute('data-tmsrcset') || !complete`, scan, then RESTORE each image's original inline `display` value **and priority** (`getPropertyValue`/`getPropertyPriority` — so an `enforceMediaDisplayJS`-set `!important` survives). Hiding (not skipping the `<img>` node) is REQUIRED so the inline `<a>` wrapper collapses too — the culprit `getBoundingClientRect().right` is the `<a>`'s, driven by the img. **Scope discipline (do NOT loosen):** key on `data-tmsrc`/`!complete`, NEVER `naturalWidth===0` — deferred imgs report `complete===true` (no src) so `data-tmsrc` is the load-bearing check (the reverted-2026-06-16 `!complete`-only guard "skipped 0"), and `naturalWidth===0` would also hide a LOADED intrinsic-size-less SVG and wrongly drop its real width. Does NOT regress the `height:auto` logo case (that img is LOADED → not hidden) nor genuine desktop-width emails (their width comes from tables/fixed elements, not from unloaded imgs; loaded wide imgs are clamped by `img{max-width:100%}` so they never drive a widen). Measurement-time only, fully restored before return, before paint. Test: `EmailRenderPipelineTests.fitViewportHidesUnloadedImagesForMeasurement`. With layer 2, Scholar Inbox decides "no overflow → 1.0×" on the first measurement — the runaway guard never fires for it (kept as defense-in-depth for non-image fluid culprits).
- **Post-image-load WIDTH recheck + centered-culprit widen target — the flip side of hiding unloaded images (2026-07-04, FleetOptics delivery template).** Hiding deferred imgs during `measureMaxRight` (layer 2 above) means an email whose true width is IMAGE-DRIVEN under-widens: FleetOptics' centered 515px table measured 307px at vw=288 with its 12 remote imgs hidden → fit converged at 400 → imgs loaded → table re-expanded to 515 → ~115px clipped right behind `html{overflow-x:clip}` FOREVER (idempotency guard blocks re-entry; height had post-load re-report paths, width had none). Two coordinated fixes in `AutoSizingHTMLView.swift`: **(a) `postImageWidthRecheckJS`** (documentEnd, after monitor) arms load/error listeners on every img keyed like the measure-hide (`data-tmsrc`/`data-tmsrcset`/`!complete`); when the LAST one settles it re-measures maxRight vs `__tmLayoutVp||__tmDeviceWidth` (+8 slop) and posts ONE `{requestWidthRefit:true}` (`__tmWidthRefitRequested` one-shot — can't loop). Swift's `Coordinator.resetAndFit()` re-runs the fit through the SANCTIONED `viewportResetJS` path (ADR-IOS-039) in a SINGLE evaluateJavaScript turn (reset+fit in one JS turn = one WebKit layout/scale commit, no intermediate device-width paint of the already-revealed content). Imgs all loaded before fit ran need nothing (fit measured them un-hidden); imgs that never fire load/error keep old behavior. **(b) widen target includes the culprit's own width**: `want = max(ceil(max(maxRight, culpritWidth)), STANDARD_MIN)` where `culpritWidth` is measured INSIDE `measureMaxRight` while imgs are still hidden. Rationale: a `margin:auto`/`align=center` culprit RE-CENTERS on every widen, so `rect.right` closes only HALF the overflow per pass (515px table: 402→459→487→501 — exhausts MAX_PASSES still clipped, and the not-converged end state can trip the runaway guard into reverting a FIXED-width email to 1.0×). Culprit width is the exact one-pass viewport for centered content; left-anchored width ≤ rect.right (no-op), fluid width:100% == vw (never enters loop). Tests: `EmailRenderPipelineTests.fitViewportWidenTargetsCulpritWidth`, `.postImageWidthRecheckPolicy`.
- **Width-strip pass includes `hr` — OWA quoted-content separator (2026-07-07).** Outlook/OWA bakes a hard pixel width from the sender's desktop window into its quote separator (`<hr class="_qc_B" style="width: 1457.4px;">`). The pre-widen width-strip selector was `'div,p,section'`, so the `<hr>` survived, `measureMaxRight()` saw maxRight=1459 as the SOLE overflowing element, and the widen loop hit the 1200 cap → whole (otherwise narrow, fits-288pt) email at 0.24x (logmain.log 2026-07-07: `constrained 0 oversized elements` → `OVERFLOW CULPRIT: HR._qc_B w=1459`). Fix: selector is now `'div,p,section,hr'` — an `<hr>` is purely decorative, so a fixed width wider than the viewport carries no content and `width:auto` restores default fill-the-container behavior. The table exclusion rationale in the comment block is unchanged. Test: `EmailRenderPipelineTests.fitViewportStripsOversizedHrAndSkipsWiden` (selector-faithful `querySelectorAll` stub — parses the comma list, so the test fails on the pre-fix selector).
- **Rightmost-edge scans are CLIP-AWARE — content inside an author `overflow-x: auto/scroll/hidden/clip` ancestor cannot widen the page (2026-07-07).** A notification email (`__aliyun_email_body_block` markup, Tailwind-style inline styles) carried a 491px pricing table with nowrap cells INSIDE the sender's own `DIV.w-full overflow-auto` scroller (286px — the standard markdown-render horizontal-pan pattern). On the web that table pans inside its box and never touches page width, but both rightmost-edge scans measured every descendant blindly: `measureMaxRight()` saw maxRight=492 → widened the whole email to 493px → **0.58x shrink** for content that never visually overflowed (logmain.log 2026-07-07: `OVERFLOW CULPRIT: TABLE.w-max min-w-full … ancestor[0] DIV.w-full overflow-auto w=286`). Fix in BOTH scan sites (`fitViewportJS`'s `measureMaxRight` AND `postImageWidthRecheckJS`'s `check()` — they must stay in lockstep): when a candidate would BECOME the new max, walk its ancestors up to but EXCLUDING `document.body`; if any has computed `overflow-x` in {auto, scroll, hidden, clip}, SKIP it (the clipping ancestor's own box already participates in the scan). The walk stops BELOW body because our own wrapper puts `overflow-x: clip` on html/body — including body would make EVERY candidate look contained and kill widen detection entirely. Undefined/empty/`visible` overflowX = non-containing (harness stubs without an `overflowX` field must keep behaving as before). Only walking on monotonic max increases keeps the cost negligible. Post-fix, such emails render 1.0x and the table pans inside its own container — the sender's design. Tests: `fitViewportSkipsClippedAncestorOverflow`, `fitViewportWidensWithoutClippingAncestor` (regression guard: same overflow, no clip ancestor → still widens), `widthPipelineRecheckSkipsClippedOverflow`.
- **`img{max-width:100%}` cap is `!important` + inline-style aspect analog — author CSS resets must not defeat the responsive image cap (2026-07-07).** A webmail-composer email ("cafe note"/`cui` markup) fit at 1.0x but shipped a composer reset `#cafe-note-contents, #cafe-note-contents * { …; max-height: none; max-width: none; … }` (no !important) that out-specificity'd our plain `img { max-width: 100%; }` cap in `EmailHTMLWrapper`. Its banner `<img style="display:inline-block;border:0px solid;width:900px;height:274px" src="http://…">` laid out at the full 900px; the image is deferred/hidden during the initial fit (so fit stayed 1.0x), then `postImageWidthRecheckJS` saw the settled 900px and re-fit → widened to 910px → **0.34x shrink** (logmain.log 2026-07-07). Fix: `img { max-width: 100% !important; }` — the cap is a non-negotiable mobile-rendering invariant; an author reset that removes it always ends in a whole-email shrink, strictly worse for the author too. Companion rule `img[style*="width"][style*="height"] { height: auto !important; }` — the inline-STYLE analog of the existing `img[width] { height: auto; }` attribute rule — so a clamped inline-styled image doesn't keep its fixed inline pixel height (distortion); requiring BOTH substrings keeps the loose `[style*=]` match harmless (a false positive just gets aspect-correct `height:auto`, and a never-loading image collapses instead of leaving a fixed-height hole). `img[width] { height: auto; }` itself is deliberately UNCHANGED (Apple-survey logo hazard documented at the rule). Tests: `imgMaxWidthCapIsImportant`, `imgInlineStyleHeightAutoAnalog`.
- **Side gutter is a MIN "indent" that ABSORBS the email's own outer margin — `eatGutterMarginsJS` + dynamic SwiftUI padding (2026-06-30).** Our SwiftUI horizontal gutter (default 16pt) STACKED on an email's own outer inset (e.g. Scholar's 8px `.content-padding` cell padding) → ~24pt, over-inset once we stopped shrinking emails. The gutter is now a MINIMUM that absorbs the email's indent. **Mechanism — measure, don't fiddle (so it CANNOT clip or perturb layout):** `eatGutterMarginsJS` (documentEnd userscript) only MEASURES the email's own content inset and posts it via the `gutterAdjust` WKScriptMessage; the Coordinator sets `@Binding leadingPad/trailingPad`, and `AutoSizingHTMLView` applies `.padding(.leading/.trailing, …)` = `max(0, 16 − inset)`. Total indent = `max(16, emailInset)`: no inset → 16 (unchanged); 8px → pad 8 → total 16; indented >16 → pad 0 → its own inset. **Inset = MIN left/right inset among WIDE text leaves (width ≥ 60% of body = the main column)** — narrow elements (the injected `[CAUTION]` banner, a centered date) and structural full-width wrappers (no DIRECT text) are excluded so a single low-inset outlier can't defeat it (the failure of the earlier body-pull approach, which the `[CAUTION]` banner capped to ~1px). Clamp `[0,16]` ⇒ a negative inset (content overflowing) clamps to 0 ⇒ no reduction ⇒ **cannot harm other emails** (only ever removes our own double-count, never adds indent or pulls content). The earlier NEGATIVE-body-margin version was REPLACED (it was clip-bounded by the smallest inset → ineffective on most emails; `logmain.log 2026-06-30` showed Scholar pull=1px, several emails pull=0). Test: `EmailRenderPipelineTests.eatGutterMeasuresAndPostsReducedGutter`. `GUTTER=16` in the JS MUST match the SwiftUI `leadingPad/trailingPad` default 16.
- **`eatGutterMarginsJS` is a GLOBAL min-inset measurement and is structurally blind to a WHOLESALE per-region indent that also contains a full-width element — `normalizeIndentJS` per-region crop (2026-07-07, OWA/Outlook compose).** An Outlook/OWA-composed email styles EVERY main-content block with the OWA idiom `margin: 0px 0px 16px 40px` (margin-left:40px) — on a 288pt phone that's 14% of the width, stacked on our own 16pt gutter → ~56pt left inset vs 16pt right. The same email carries a full-width mailing-list footer paragraph at inset 0, so `eatGutterMarginsJS`'s MIN-inset measurement across wide text leaves is (correctly) 0 → it does nothing, and any GLOBAL body-pull is bounded the same way (`logmain.log` 2026-07-07: `emailInset L=0 R=0` while every content block carried `margin: 0px 0px 16px 40px`) — exactly why the negative-body-margin approach the gutter fix replaced (2026-06-30, bullet above) was ineffective in general. A global fix can never see past that single full-width outlier. **Fix: work PER-REGION instead of globally.** `normalizeIndentJS` (documentEnd userscript, injected BETWEEN `leftFix` and `eatMargins` so both downstream passes measure the post-crop layout) finds "indent carriers" — elements with `margin-left >= INDENT_MIN` (24px, clears the 16pt gutter and Gmail's ~4px quote indent) AND asymmetric vs `margin-right` (excludes `margin:auto`-centered content, whose used margins are equal) — keeps only the OUTERMOST ones (skip a carrier with a selected ancestor) whose subtree contains a wide (≥60% body width) text leaf, then applies a **DOMINANCE GUARD**: only crop when ≥60% of the body's wide text leaves live inside selected carriers. This is what makes it safe to run unconditionally — an email with ONE intentionally indented aside among normal paragraphs (real, meaningful indentation) keeps its aside; only emails whose main column is WHOLESALE indented (compose-tool chrome, not information) get cropped. **Crop is a SHIFT (subtract the tightest bounding min margin-left across selected carriers), not a flatten** — RELATIVE deltas are preserved (a 40/80 mix becomes 0/40), a uniform indent (the OWA case) collapses every carrier to 0 so the SwiftUI 16pt gutter remains the total indent, same as any un-indented email. Only `margin-left` is touched (`!important`), so the idiom's own `margin-bottom:16px` etc. survive; each cropped element is marked `data-tm-indentcrop`. **Exclusions:** UL/OL/LI (list geometry is `constrainLeftOverflowJS`'s job), BLOCKQUOTE (real quote semantics, owned by the quote-collapse pass), and any element — or the whole body — computed RTL (LTR-only scope). No idempotency machinery needed (runs once per document load; appearance-flip re-renders reload the document fresh, same as the other userscripts). Tests (`TabMailTests/Views/EmailRenderPipelineTests.swift`, new `runIndentCrop` JSContext harness mirroring `runLeftFix`): `indentCropCropsUniformOwaIndent`, `indentCropPreservesRelativeDeltas`, `indentCropDominanceGuardLeavesMinorityIndentAlone`, `indentCropExcludesCenteredContent`, `indentCropExcludesBlockquoteAndLists`, `indentCropCropsOutermostOnly`.
- **Dark-mode color handling: preserve saturated colors + textless decorations, dim only LIGHT LOW-SAT fills, keep bordered boxes distinct — `fixDarkModeColorsJS` (2026-06-30).** The old `coloredBg && bgL>80 → dim to a FIXED lum~80` normalized EVERY colored fill to the same luminance, which (a) flattened sender color-coding and (b) destroyed nesting contrast. Three scoped rules now: **(1) textless** decorative fills (accent/score bars, rule lines, swatches — `el.textContent` empty) keep their hue (skip the dim). **(2) saturated** colors (`bgS > 100`: score badges/chips/brand buttons like `#C14600` sat 193 / `#E57C4F` sat 150) keep their hue too — only LIGHT LOW-SAT tinted fills (cream sections sat 15-34, `[CAUTION]` banner sat 76) get dimmed; text contrast on a preserved fill is handled by the existing contrast safety net. **(3) chrome** (a visible border / border-radius / box-shadow = a deliberate card/callout) makes a box stay DISTINCT: a near-white chrome box darkens as a panel even when nested (instead of going transparent), and a colored chrome box dims to a DARKER target (lum ~50, not 80) so it doesn't collapse cream-on-cream into its dimmed section (the "Important box has a border but empty infill" report — card is `0x1C1C1E` lum 28, so it wasn't card-blend, it was section-blend). NEAR-WHITE panels: the OUTERMOST near-white surface (or any chrome box) gets a translucent-black overlay (`rgba(0,0,0,0.22)`, marked `data-tm-panel`) so it reads as a sunken panel darker than its surroundings; nested plain (no-chrome) near-whites stay transparent so overlays don't stack into mud on white-heavy receipts (Anthropic/Amazon). Tests: `darkModeDimScopedToLightLowSatTextFills`, `darkModeNearWhitePanelDarkening`. Border colors are never touched by this script.
- **`fixDarkModeColorsJS` PAGE-COLOR dominance regime — near-whites that ARE the page color go transparent, not panels (2026-07-07).** The near-white branch's own comment always named case (a) "a full-bleed wrapper that should DISAPPEAR", but the code treated every OUTERMOST near-white as case (b) (sunken panel, `rgba(0,0,0,0.22)` + `data-tm-panel`). A cloud-console notification email bakes `background-color: rgb(250,250,248)` inline on EVERY paragraph/heading — each is independently "outermost" → **103 `panel→darken` hits** → dark stripes behind every line of text (logmain.log 2026-07-07). Fix: a PAGE_DOMINANCE pre-pass (dominance-guard idiom shared with `normalizeIndentJS`): sum the rect areas of OUTERMOST chrome-less near-white surfaces; if they cover ≥60% of the body's area, `pageColorMode=true` → every CHROME-LESS near-white goes transparent, never `data-tm-panel`. CHROME boxes (border/radius/shadow = deliberate card) are excluded from the candidates AND keep the panel treatment in both regimes — this is what keeps the Scholar paper-card case safe. `pageColorMode=false` → behavior exactly as before. Tests: `darkModePageColorModeMakesChromelessBlocksTransparent`, `darkModeMinorityPanelStaysPanelized` (regression guard), `darkModeChromeBoxStaysPanelizedInPageColorMode`. NB: a dark-logo image treatment (native pixel pipeline, grayscale-selective inversion, force-light fallback) was prototyped alongside this on 2026-07-08 and REVERTED pre-commit for a future deep-planning pass — white plates looked worse in practice; do not re-add ad hoc.
- **Re-render the body on light↔dark appearance change (2026-06-29).** `fixDarkModeColorsJS` runs ONCE at load and bakes inline color overrides (`!important`) for the appearance AT LOAD TIME; the CSS `@media (prefers-color-scheme)` re-evaluates automatically on a flip but the JS inline overrides do NOT, so they become wrong/stale (load light→flip dark = fine-grained dark fixes never ran; load dark→flip light = forced light text persists on a light bg). Fix: `HTMLWebView` now reads `@Environment(\\.colorScheme)`; `Coordinator.loadedColorScheme` tracks the rendered appearance; `updateUIView`'s `else if loadedColorScheme != colorScheme` branch reloads via `wrapAndLoad` so every script re-runs fresh for the new appearance. Cheap (off-main wrap, WebKit-cached deferred images); `hasRevealed` stays true (html unchanged) so no "Loading…" placeholder — the opacity:0→reveal fade covers the brief re-render. Reload (not reversible-JS-reapply) chosen for robustness over the fragility of tracking + undoing every inline override.
- **Load flicker fix — `monitorHeightJS.report()` is GATED on `window.__tmFitDone`.** A widened email's frame jumped `1→881→466`: the documentEnd/RO report posts the *un-widened* scale-1.0 height (881) before `fit()` widens, then it snaps to the scaled height (466) — visible flicker. Fix: `report()` early-returns while `!window.__tmFitDone`; `fitViewportJS` sets `__tmFitDone = true` on EVERY exit (right after the idempotency guard, so even the `vw<100` skip opens it) and calls `window.__tmReportHeight()` at the no-overflow and post-widen exits so the FINAL height posts immediately (frame goes `1→466` directly). Liveness fallback: a one-shot `setTimeout(…, 700)` in monitorHeightJS force-opens the gate if `fit()` never runs (e.g. webView bounds<50) so the frame can't strand at its seed. NOT a polling interval (the `noPolling` test still forbids `setInterval`). Tests: `monitorGatesUntilFit`, `fitOpensGate`.
- **Residual widen "blink" = WebKit pageScale commit lag, not our sizing.** After the gate fix the SwiftUI frame is stable (frameH=466 throughout), but `ContentSizeKVO` shows the webview paints the widened layout once at `zoom=1.000` (content overflowing the frame) then commits `zoom 1.0→0.56` ~one frame later — a brief blink. Mitigation history: tried `initial-scale=(vw/targetWidth)` on the widen meta — **log-proven NOT to help** (WebKit ignores `initial-scale` on a RUNTIME meta change; first `ContentSizeKVO` after widen still `zoom=1.000`). **Actual fix = opacity hold:** `EmailHTMLWrapper` starts the doc at `html { opacity: 0; transition: opacity 0.07s linear; }`; `fitViewportJS` has a `reveal()` (`documentElement.style.setProperty('opacity','1','important')`) called at EVERY exit — synchronously on the no-widen/skip/idempotency paths (first paint already correctly scaled), and on the widen path via a **double `requestAnimationFrame`** so the reveal lands AFTER WebKit's async page-scale commit. The un-scaled widen frame is thus painted while still `opacity:0` and never seen; the 0.07s fade makes the reveal a deliberate fade-in. Failsafe: the monitorHeightJS 700ms liveness timer also force-sets opacity:1 so content can never strand invisible if `fit()` never runs. Tests: `wrapperStartsHidden`, `fitRevealsPostCommit`. Cost: emails don't progressively paint during load (hidden until `fit()` runs at ~didFinish) — acceptable for small email bodies. Keep `initial-scale` on the meta too (harmless, helps if a future WebKit honors it).
- **`fitViewportJS` overflow decision needs an `OVERFLOW_SLOP` (8px) — never compare `Math.ceil(maxRight)` to `vw` exactly.** WebKit reports fractional layout widths (a column measured `288.2` in a 288 viewport); `Math.ceil(288.2)=289 > 288` was a FALSE overflow that floored to `STANDARD_MIN=400` and shrank a perfectly-fitting email to 0.72x. Log-proven on a 107KB newsletter (`id=ZVELEG`, 2026-06-17): first fit `no overflow` at 288 + `onload overflowingDescendants: none` (genuinely fits), yet a SECOND fit at `onload` widened to 403 and visibly shrank it — the user saw it appear at full size then shrink seconds later ("slow and weird"). Fix: entry gate is `if (maxRight <= vw + OVERFLOW_SLOP) return false` (was `ceil(maxRight) <= vw`), and the widen-loop stable-break is `want <= targetWidth + OVERFLOW_SLOP` (was `<= targetWidth`). The loop slop ALSO stops a `width:100%` culprit (e.g. `TD.column-2`) creeping ~1px/pass (`400→401→402→403`) and burning all `MAX_PASSES` on a 15000px doc — now no-overflow emails skip the loop entirely (zero forced reflows) and genuine-widen emails converge in ~2 passes. Genuine desktop emails overflow by 50-130px (far past 8px) so they still widen; a <=8px overflow clips harmlessly under `html{overflow-x:hidden}` rather than over-shrinking the whole email. Tests: `fitViewportSubPixelSlop`, updated `fitViewportOnlyOnOverflow`/`fitViewportWidenTerminates`.
- **Reveal/size on FIRST body layout, not `didFinish` (the "still slow" blank).** `fit()` was driven only by `didFinish`, which for `loadHTMLString` waits on external images/subresources. Log-proven (`id=RM6UHL`, 107KB newsletter, 2026-06-17): `HeightDiag t100` shows `body scroll=15493` (fully laid out at ~100ms) but `innerH=1` (frame still 1px = invisible); the frame isn't sized until ~t800 because the `__tmFitDone` gate held all height posts until the `didFinish`-driven fit. Fix: `monitorHeightJS.report()`, while still gated, posts `{requestFit:true}` once (`__tmFitRequested` guard) the first time `document.body.scrollHeight > 1`; `handleHeightMessage` sees `requestFit` and calls `fit(webView)` immediately. So fit/reveal/sizing happen as soon as the WIDTH is known (~first layout) instead of after image load. `didFinish` still calls `fit()` (idempotent: widen path bails on `__tmLayoutVp`, no-overflow path re-reveals/re-posts same height — both harmless). Width comes from document structure (available at first layout); late images are capped by `img{max-width:100%}` so they don't trigger a late widen — so early fit picks the correct scale. Test: `monitorRequestsEarlyFit`. NOTE: in DEBUG builds the diagnostic scripts (`QuoteDetect` logs every line, `htmlDebugReportJS`, `heightDiagnosticJS` t100..t3000) add real main-thread overhead on big docs — production (DebugModeManager off) is faster than the logged repro suggests.
- **Reveal must wait for a PAINT cycle, not just layout (`revealAfterPaint` = double-rAF) — else big docs show an empty box.** After the early-fit (`requestFit`) change, `fit()`/reveal can run at the FIRST layout (`readyState=interactive`, ~100ms). On a small email that's fine, but on a 16003px Outlook newsletter (Vancouver Sun) the frame sized correctly AND opacity went to 1 while WebKit hadn't yet rasterized the visible content → "proper height but empty, content appears later". Fix: `reveal()` (sync opacity:1) is now only used on the idempotent/`vw<100` edge exits; the no-overflow and widen exits use `revealAfterPaint(tag)` = `requestAnimationFrame(requestAnimationFrame(reveal))`, which lands after the next compositor frame so the visible tiles are painted before un-hiding (offscreen tiles still paint lazily on scroll — unavoidable). This double-rAF also still covers the widen path's async page-scale commit (its original purpose). DIAGNOSTICS added (debug-gated): `reveal()` logs `[FitViewport] reveal opacity=1 [tag] readyState=… scrollH=…`; Swift logs `[FitReq id=…] requestFit received → fit()`. If the next capture shows reveal firing while content is still painting (double-rAF insufficient for a pathologically heavy doc), the fallback is to reveal on `readyState==='complete'` for very large docs (trades a bit of speed). Tests: `fitRevealsPostCommit` (asserts both exits use `revealAfterPaint`).
- **Render-blocking external `<link rel="stylesheet">` (e.g. Google Fonts) = empty-box-for-seconds; STRIP it in `EmailHTMLWrapper.wrapHTML`.** ROOT CAUSE of the "Vancouver Sun renders slowly / proper height but empty, appears later" report. Timestamped trace (logmain.log 2026-06-17, 107KB newsletter): frame sized at +196ms, but the **first compositor frame (`first rAF`) didn't fire until +2771ms** ≈ `readyState=complete` (+2766ms). WebKit defers the first paint until all render-blocking external stylesheets load, and this email's `<link href="fonts.googleapis.com/css2?family=Playfair+Display…">` took ~2.6s; the rAF-based opacity reveal was starved the entire time (`revealAfterPaint rAF2 fired` at +2798ms). The link survived `unwrapFullHTMLDocument` because it was hidden in an `<!--[if !mso]><!-->…<!--<![endif]-->` conditional comment. Fix: regex-strip `<link … rel=stylesheet …>` (any ordering, anywhere) right after the `loading="lazy"` strip in `wrapHTML`; `<style>` blocks are preserved. Email paints immediately with fallback fonts (layout is inline). Also a privacy win (external CSS = remote-tracking vector, like remote images). Test: `wrapperStripsExternalStylesheets`. KEY LESSON: WebKit `loadHTMLString` does NOT paint progressively — first paint waits for `readyState=complete`, which is gated on external subresources; so any slow/render-blocking remote resource = blank email until it resolves. The `[Ttrace +Nms]` / `first rAF` / `fonts.ready` / `[FrameApply +Nms]` timing diagnostics (debug-gated) are what pinned this — keep them.
- **REAL root cause of "big email renders slow / sized-but-empty box": remote `<img>`s block WebKit's first paint — DEFER them.** WebKit (`loadHTMLString`) holds the first compositor frame until `readyState=complete`, which waits on EVERY pending subresource. Vancouver Sun newsletter: `IMAGE AUDIT total=28 pending=28`, `first rAF`==`readyState=complete`==+2732ms → 2.7s blank even though the frame was sized at +196ms. (The earlier render-blocking-CSS theory was WRONG — `unwrapFullHTMLDocument` already strips the `<head>` so the Google Fonts `<link>` never reached the doc; the stylesheet strip logged `removedChars=0`. Keep the strip anyway: harmless + covers body-level/`.eml` links + privacy.) **Fix = DEFER remote images (auto-load after first paint).** `EmailHTMLWrapper.wrapHTML` rewrites remote `http(s)` `<img src/srcset>` → `data-tmsrc/data-tmsrcset` (cid:/data:/local-scheme untouched) so the initial document has zero pending image loads → `complete` fires at ~DOMContentLoaded → text/layout paints immediately. `deferredImageLoadJS` (atDocumentEnd WKUserScript, NOT debug-gated) swaps the real URLs back in AFTER the first paint (double-rAF post-`load` + 1500ms idempotent failsafe) so images stream in and the frame grows via the ResizeObserver. Matches Apple Mail's "text first, images after". **REJECTED ALTERNATIVE — block-with-banner (TB-style "remote content blocked, tap to load"): implemented then smoke-tested 2026-06-17 and REVERTED — it broke too many messages (layout depends on images loading). Do not re-attempt without a much more selective heuristic.** Tracking pixels still fire when the deferred swap runs (user accepted "minor tracking" for now). Tests: `wrapperDefersRemoteImages`, `wrapperKeepsLocalImages`, `deferredImageSwapScript`. **Debugging aids that cracked this were REMOVED 2026-06-17 once the cause was found (recover from git history if a render-timing bug recurs):** a `[RENDERER BUILD] rev=…` marker, `[RENDERER]` strip/defer logs, a `[Ttrace +Nms]` lifecycle timeline (`window.__tmT` at `documentStart`/`first rAF`/`fonts.ready`/`readyState`/fit/reveal) + Swift `[FitReq/FrameApply +Nms]` (a per-Coordinator `loadStartTime`/`tms()`). They pinpointed that `first rAF`==`readyState=complete`. KEY LESSON (keep this): WebKit `loadHTMLString` does NOT paint progressively — first paint waits for `readyState=complete` (all subresources), so ANY slow remote subresource = blank email until it resolves. To re-debug render timing, reinstate a `performance.now()` `__tmT` harness in the `.atDocumentStart` id-stamp script + log `first rAF`/`fonts.ready`/`readyState`.
- **"Inner content scrolls, not the page" + "1px double-scroll" — TWO distinct scroll bugs from the deferred-image render, both FIXED 2026-06-25 (confirmed on-device).** Symptom: an email renders shifted/clipped ~13px inside its card (the body content scrolls within the webview instead of the page scrolling), AND a ~1px dead-zone the user must drag through before the page moves. Height-change was NOT the cause — `logmain.log id=6HS8GR` shows `contentH=1658` STABLE the whole time while the `TABLE` body-child's `getBoundingClientRect().top` slid `0 → −13 → −1` with `body.rectTop` pinned at `0` (= an internal scroll offset on `body`, not the page/UIScrollView). **Root cause A (the slide): `overflow-x: hidden` on `html`/`body` in `EmailHTMLWrapper`.** Per CSS Overflow L3, when one axis is a scrolling value (hidden/scroll/auto) and the other is `visible`, the visible axis computes to **`auto`** — so `overflow-x: hidden` silently made html/body **Y-scroll containers**, and the late `deferredImageLoadJS` swap reflow left a stray `body.scrollTop` that WebKit doesn't re-clamp (oscillates `13 → 1` toward the `scrollHeight−clientHeight=1` clamp). **WebKit implements NO scroll anchoring / `overflow-anchor`** (bug 171099, still open) so that property is a no-op here — removing the scroll container is the only fix. **Fix A: `overflow-x: clip`** (NOT a scrolling value → no promotion → html/body never become scroll containers → the stray offset is structurally impossible). Belt-and-suspenders: `monitorHeightJS.pinBodyScroll()` resets ONLY `document.body.scrollTop` on image load (NEVER `documentElement`/`scrollingElement` — those mirror the native UIScrollView = pinch-zoom pan; `<!DOCTYPE html>` standards mode makes documentElement the viewport scroller, so body.scrollTop is always a safe inner reset). **Root cause B (the 1px double-scroll): the native WKWebView `UIScrollView` held ~1px of scrollable range** — `body.scrollHeight=1659` vs `clientHeight=1658` (a `SPAN` overflowing body by `+1px`), while the frame is `ceil(rect)=1658`, so the inner scroll view ate 1px before the gesture reached the parent SwiftUI ScrollView. **Fix B: zoom-aware inner-scroll lock** — `webView.scrollView.isScrollEnabled = false` (locks inner scroll at 1:1 so vertical drags pass straight to the parent) + a KVO on `scrollView.zoomScale` that re-enables it only while pinch-zoomed (`> 1.001`) so the user can pan magnified content. **`isScrollEnabled = false` does NOT disable pinch-zoom** (separate gesture recognizer — WebKit bug 234584); and **do NOT replace `scrollView.delegate`** to detect zoom (breaks WebKit's internal `viewForZooming` → kills zoom) — observe `zoomScale` via KVO instead. Tests: `EmailHTMLWrapperScopeTests.htmlBodyUseOverflowClipNotHidden` + `monitorHeightJsPinsBodyScrollOnly`. CAVEAT: WebKit `overflow: clip` is occasionally reported flaky — if horizontal clipping ever regresses (wide table / long URL email), the `pinBodyScroll` reset is the fallback; revisit before reverting to `hidden`.
- **Follow-on regression from the `clip` switch: "Show invite details" pill clipped at the bottom — FIXED 2026-06-26 with `body { display: flow-root }`.** The `overflow-x: hidden → clip` change above silently dropped a side effect: `overflow: hidden` establishes a **block formatting context**, `overflow: clip` does NOT (clip is explicitly excluded from BFC triggers). With body no longer a BFC, a calendar invite's body-level `.tm-ics-collapsible` wrapper (appended by `BodyRenderer` with `margin-top:12px`, then re-wrapped by `collapseICSJS` into a `.tm-quote-wrapper` carrying `margin-top:12px` + `margin-bottom:20px`) had its collapsing margins **escape the body** (margin-collapse-through, since `body{margin:0;padding:0}` and html is also `clip`). `body.scrollHeight`/`getBoundingClientRect().height` (the height pipeline in `monitorHeightJS`) exclude those escaped margins, so the webview frame was sized ~12px too short and the bottom of the "Show invite details" toggle was cut off (visible in calendar-invite emails where the wrapper is the first/only or last body child). **Fix: add `display: flow-root` to the `body` rule in `EmailHTMLWrapper`** — restores the BFC that `hidden` used to provide (margins contained → counted in the measured height) WITHOUT being a scrolling value, so it can't re-promote overflow-y or recreate the inner-scroll container that the `clip` fix removed. This is the strictly-correct restoration of pre-d242454 margin behavior; `flow-root` is the canonical modern BFC trigger (Safari 13+). Did NOT touch the `clip` rules. Test: `EmailHTMLWrapperScopeTests.bodyEstablishesBlockFormattingContext`. LESSON: when replacing `overflow:hidden` with `clip` anywhere, remember `clip` loses the BFC — re-add `display:flow-root` if margin-containment / float-containment was being relied on.
- Distinct latent bug still open (another agent): stale-scale `frameH` flush flip (939↔1368) for legitimately-widened emails via ScrollFreezeGate/HeightSeedCache.
- Regression tests: `EmailRenderPipelineTests` (JS pattern asserts incl. idempotency guard) + `ScrollFreezeGateTests` + `HeightSeedCacheTests`.

### Swift Gotchas
- `ActionTag.none` must be fully qualified in `ActionTag?` contexts — bare `.none` resolves to `Optional.none` (nil)
- **`\u{XXXX}` is NOT a valid escape in an ICU regex pattern — use `\x{XXXX}` (or a literal char).** A Swift *raw* string `#"...\u{2014}..."#` passes the literal bytes `\u{2014}` to `String.replacingOccurrences(of:options:.regularExpression)` (ICU); ICU's only code-point escapes are `\uHHHH` (exactly 4 hex, no braces), `\x{H...}`, `\UHHHHHHHH`. So `\u{...}` matches nothing and the replace is a **silent no-op** — no error, just doesn't fire. This is why `AIService.normalizeUnicode` (AIHelpers.swift) had **never** normalized dashes, smart quotes, or special spaces (only NFKC + the plain-string `\u{3010}`/`\u{2026}` replaces worked; no test asserted the regex passes, so it went unnoticed). Fixed 2026-06-28 by switching all three character classes to `\x{...}`; verified with standalone `swift`. If you write a regex character class of code points, use `\x{...}` and add an assertion that the substitution actually changes the input.
- **Dash normalization is line-start-only (parity with TB `normalizeUnicode`).** `normalizeUnicode` converts dashes (en/em/figure/horizontal-bar/minus) to ASCII `-` **only** at the start of a line — regex `(^|\n)([ \t]*)[\x{2010}…\x{2212}]` → `$1$2-` (indentation preserved). Reason: LLMs emit en/em-dash list bullets (`– item`) that the summary/action/KB parsers need as `- item`; an **inline** em-dash (prose punctuation, ranges like `10–20`) is deliberately preserved so real typography survives. Mirrors `tabmail-thunderbird/agent/modules/utils.js`. **Compose/reply text is intentionally NOT run through `normalizeUnicode` on iOS** (see `AIReply`): unlike TB there is no inline-autocomplete diff to keep ASCII-clean, and email is sent as HTML, so the model's real typography should reach the recipient.

### Folder.== must include every UI-visible field
- `Folder` overrides `==` (Models/Folder.swift) to compare *visible* fields, because SwiftUI's ForEach diff uses Equatable to decide whether to re-render a row even after the underlying `@State`/`@Observable` array reassignment.
- If a UI-visible field is omitted, the row stays cached with stale data even though the DB write succeeded. This bit us on `role` (Settings → Account Detail folder/role rows didn't refresh after re-assignment or "No Role"). Tests: `FolderEqualityTests.differentRole`.
- Rule: when adding any field that the UI reads off `Folder`, add it to `==`. Sync-only fields (cursors, `lastKnownUidNext`) stay out.

### IMAP Folder Role Detection & Dedup (iCloud "Trash" + "Deleted Messages")
- `IMAPProvider.mapRole(attributes:name:)` returns a single role per folder via SPECIAL-USE first, then a name-based fallback. Both "Trash" and "Deleted Messages" match the trash heuristic — without dedup, iCloud accounts ended up with two `.trash` folders (and a confusing/empty unified Trash view).
- `IMAPProvider.dedupRoles(_:)` runs at the end of `fetchFolders()` and demotes losers to `.custom`. Tiebreak: SPECIAL-USE flag first, then `canonicalNameRank` (the position in the canonical-name list — lower wins), then shorter name.
- `fullSync` deliberately does NOT update `role` on existing folders (it only refreshes `name`/`totalCount`/`uidNext`). This protects user manual reassignments — but it also means existing duplicate state needs a one-shot migration.
- Migration `v48_dedupFolderRoles` in `AppDatabase.swift` mirrors the same canonical-name preference (no SPECIAL-USE info on disk) and runs once to heal pre-fix accounts.
- UI: `AccountDetailView`'s "Folder Roles" section shows ALL folders for a role (joined) plus a `exclamationmark.triangle` warning icon when >1 — surfaces edge cases the migration didn't catch.

### Two-Tier Sync (ADR-IOS-009)
- **Delta sync** (every 60s): lightweight check for changes since last sync
  - Gmail: `history.list` API with `lastHistoryId` cursor → returns adds/deletes/label changes
  - IMAP: `STATUS` command per folder → compares `uidNext` + `messageCount` with cached values
- **Full sync** (every 10 min): safety net, self-healing — runs `fullSync()` as before
- Delta sync skips unchanged folders entirely (IMAP) or processes only affected messages (Gmail)
- If delta fails (e.g., Gmail 404 expired cursor), falls through to full sync automatically
- `Account.lastFullSyncAt` tracks when last full sync ran (delta syncs don't update it)
- `Folder.lastKnownUidNext` caches IMAP uidNext for change detection
- `GmailProvider.fetchMessageDetails(ids:)` returns headers + labelIds for folder assignment during delta sync
- `IMAPProvider.folderStatus(path:)` returns quick STATUS without SELECT

### `syncStartup` Budget Discipline (v50/v51 lesson)
- **`syncStartup` is the shared entry point** for FG return, BGAppRefresh, silent push, and BGProcessing. Any blocking work before the `syncTask` spawns is paid by ALL four paths.
- **BGAppRefresh (~25s) and silent push (~30s) have iOS-imposed budgets.** Pre-sync work eats directly into message-fetching/AI-processing time. A 3s pre-sync scan = 10% of push budget burned before a single byte is fetched. When NSE classification or delta sync gets cut off mid-flight, passive "Inbox updated" notifications appear instead of the AI-classified ones.
- **Every pre-`syncTask` step must be cheap at steady state** (<10ms each). Currently: NSE merge (0ms if unchanged), `cancelAllInFlight` (parallel, ~1-5ms), `recoverIncompleteHeaders` (partial-index seek, ~ms). Self-heal 2b/2c and repopulate are detached post-spawn.
- **Adding DB reads to the pre-sync path requires a purpose-built index.** Test with `EXPLAIN QUERY PLAN` — if you see `SCAN messageHeader`, `ANY(...)`, or `USE TEMP B-TREE FOR ORDER BY`, fix before landing. See ADR-IOS-029.
- **Prefer partial indexes for "drain to empty" queries** (crash recovery, orphan repair, queue repopulate). At steady state the index has ~0 rows, seek is free regardless of stats.
- **Incident history**: `BackfillEmbeddingQueue.repopulate` (v50 fix) burned 1.4-4.2s; `recoverIncompleteHeaders` (v51 fix) burned 2-2.6s. Both for 0-row results. Both were planner confusion from overlapping indexes + stale stats. Partial indexes + dropping the superseded full indexes eliminated both.
- **Future verification**: a seeded 100K-row in-memory harness with EXPLAIN + wall-clock assertions per hot query (planned). Until that exists, manual `EXPLAIN QUERY PLAN` probe on any new pre-sync query.

### Stale Message Detection (syncMessages)
- If `messages.count < limit`, we fetched ALL messages in the folder — any local message not in the remote set is stale
- If `messages.count == limit`, only delete local messages with `date >= oldestFetchedDate` (within fetch window)
- Prevents archived/deleted messages from persisting locally in IMAP accounts

### Benign Sync Log Noise — DraftUpsertSkip + Gmail 429 (diagnosed 2026-06-09)
- `[Sync] DraftUpsertSkip: preserving local content for … (folder=sent/drafts)` fires for EVERY existing row in drafts/sent on every full sync, for ALL providers (IMAP UIDs, Graph AQMk… ids, Gmail hex ids). It is the intentional guard at `SyncEngineFullSync.swift:691` against the "time snaps back" bug (server drafts.list/summary cache lags a local push, so existing-row metadata is never overwritten in those folders). High volume ≠ problem.
- `[Gmail] Rate limited (429) … "Too many concurrent requests for user"` comes from the UNBOUNDED `withThrowingTaskGroup` header fetch (`GmailProvider.swift:149` — one `messages.get?format=metadata` task per ref). `performHTTPRequestWithRetry` (HTTPClient.swift) retries 3× with 1s/2s/4s backoff; observed hits all recover on attempt 1. If retries DID exhaust, `request()` throws → the throwing task group aborts the whole folder fetch → that folder's sync cycle fails and retries next poll. A partial header list can never reach the stale-delete path, so 429s cannot cause message deletion.

### Transient provider HTTP errors must NOT surface as a sync failure — `SyncEngine.isTransientError` (2026-06-17)
- **Symptom**: a momentary Microsoft Graph `503` on `GET /v1.0/me` during connect flipped the inbox sync-status subtitle to the failed state. Log chain: `[Exchange] HTTP 503 …/me` → `[AccountManager] Connect failed … (non-auth)` → `[Sync] Error: networkError(underlying: HTTPError.networkError(statusCode: 503))` → `[SyncPhaseMut] lastSyncFailed = true`.
- **Root cause**: the surfacing paths reused `SyncEngine.isConnectionError` to decide whether to suppress. But that predicate **deliberately** returns `false` for `ProviderError.networkError` (HTTP 4xx/5xx) — its job is "should we *reconnect*?", and a 503 means the request completed, so the connection is fine (see its doc comment). So a transient 503 was treated as a hard sync failure.
- **Fix**: added `SyncEngine.isTransientError(_:)` (sibling of `isConnectionError`, `SyncEngine.swift`) = `ProviderError.networkError` wrapping HTTP **5xx or 429** (matches both the `HTTPError.networkError(statusCode:)` enum AND the `NSError(domain:"Gmail"/"Exchange", code:)` shape, mirroring `isHttpGoneStatus`). It is intentionally **narrow (HTTP-only)** — it does NOT include connection errors, so offline behavior (which still flips the failed indicator) is unchanged.
  - `InboxViewModel.performSync` catch: a `catch where isTransientError` arm leaves `lastSyncFailed` untouched and shows no error banner (the reported path — one account's transient throw was aborting the whole multi-account refresh).
  - `AccountManager.foregroundSyncOne` catch: transient → `SyncResult(failed: false)` so the foreground poll doesn't flip the subtitle either. (`foregroundSyncAll` already masked single-account failures via its all-accounts-failed aggregation; this also covers a full-Graph-outage where every account 503s.)
- Tests: `IsTransientErrorTests` in `ErrorClassificationTests.swift`. NOTE the layering — `isConnectionError` (transport/reconnect) vs `isTransientError` (retryable HTTP, don't-surface) vs `isHttpGoneStatus`/`isMessageNotFoundError` (permanent 404/410, drop-the-op) are three DISTINCT classifiers; don't collapse them.

### Raw NIO error types must be added to `isConnectionError` by NSError DOMAIN, not substring (2026-07-04)
- **Leak class**: SwiftMail occasionally lets raw Swift NIO transport errors escape its `IMAPError` wrapping. Their `localizedDescription` bridges to the useless "The operation couldn't be completed. (NIOCore.<Type> error N.)" form, and if `SyncEngine.isConnectionError` doesn't classify them, `InboxViewModel.performSync`'s catch (`InboxViewModel.swift:~1535`) publishes that text verbatim to `viewModel.error` → the (debug-gated, `InboxView.swift:198`) inbox banner, and flips `lastSyncFailed` as a "real" failure instead of a connection blip.
- **Instances so far**: `NIOCore.ChannelError` (fixed earlier, `SyncEngine.swift` domain check) and `NIOCore.IOError` (raw syscall errno failure; banner showed "(NIOCore.IOError error 1.)" during a foreground poll — fixed 2026-07-04, same pattern).
- **Why domain check**: an `IOError` description embeds arbitrary `strerror` text ("Operation not permitted (errno: 1)", etc.) — unenumerable by substrings; NIO structs always bridge with NSError `code 1`, `domain = "NIOCore.IOError"`. Substring `"NIOCore.IOError"` also added for the already-bridged-to-string form.
- **If another NIO type leaks** (`NIOPosix.*`, `NIOSSL.*`…): add its NSError domain check + bridged-string substring to `isConnectionError` and the mirrored test pair in `IsConnectionErrorTests` (`ErrorClassificationTests.swift`), following the ChannelError/IOError precedent.
- The banner path uses raw `error.localizedDescription`, NOT `Error.userFacingDescription` (`Helpers/ErrorFormatting.swift`, which strips the "(Domain error N.)" tail in production) — classification is the correctness layer here, not formatting.

### ICS Calendar Import — Invisible SFSafariViewController Hack
- **Problem**: iOS only shows the native "Add to Calendar" dialog (with attendees, RSVP, calendar picker) when Safari navigates to a `text/calendar` MIME type. Third-party apps can't trigger this natively.
- **Solution**: Start a tiny localhost `NWListener` HTTP server serving the ICS data with `Content-Type: text/calendar`, then open it via `SFSafariViewController` configured as an invisible sub-pixel sheet.
- **Key trick — invisible sheet**: Use a custom sheet detent of `0.01` points (sub-pixel, completely invisible). Set `largestUndimmedDetentIdentifier` to match the custom detent's identifier — this prevents background dimming.
- **Shadow removal**: Walk Safari's view hierarchy up from `safari.view.superview` looking for `UIDropShadowView` (internal UIKit class), set `layer.shadowOpacity = 0` to remove the thin line at the sheet edge.
- **Why this works**: iOS intercepts the `text/calendar` MIME type in Safari and presents the native calendar dialog *on top of* the Safari sheet. The calendar dialog lives inside Safari's view hierarchy, so the invisible Safari stays alive underneath.
- **Dismissal**: No auto-dismiss needed since the sheet is invisible. Tear down on: (1) re-tap on ICS attachment, (2) navigating away from message (`onDisappear`), (3) scene entering background.
- **Strong reference required**: `activeSafari` must be a strong (not `weak`) reference — `weak` causes premature deallocation after teardown, making subsequent presentations fail because `topViewController()` returns the zombie Safari.
- **Re-presentation**: Always teardown first, then present after a delay (0.3s if had active session, 0.05s otherwise) to let UIKit process the dismiss animation.
- **Port reuse**: Server tries port 18942 first, falls back to `.any` if in use (TIME_WAIT from previous session).
- **Reusable pattern**: This localhost-server + invisible-SFSafariViewController trick can be adapted for any iOS feature that requires Safari to trigger native system behavior (e.g., `.pkpass` wallet passes, `.mobileconfig` profiles, other MIME-triggered system dialogs).
- **App Store safety**: No private APIs used — just cosmetic `layer.shadowOpacity` changes and standard sheet detent configuration.
- **Implementation**: `ICSCalendarImporter.swift` — `NWListener` server + `SFSafariViewController` + 0.01pt detent
- **ICS is sanitized before it reaches the OS (ADR-IOS-052, 2026-07-04)**: `presentCalendarImport(icsData:)`'s first statement is `let icsData = ICSSanitizer.sanitize(icsData)` (`Shared/ICS/ICSSanitizer.swift`). This is the single choke point — the `icsText:` overload and the demo-mode branch both funnel through it. It strips pathological bloat (a real third-party webmail invite carried a ~79 KB `X-ALT-DESC` that wedged iOS↔Google CalDAV sync: 85 KB → 2.6 KB), repairs RFC-invalid DISPLAY alarms, drops EMAIL/PROCEDURE alarms, size-gates/truncates over-long properties, and re-folds to ≤75-octet CRLF — WITHOUT touching event semantics (METHOD/ORGANIZER/ATTENDEE/recurrence/VTIMEZONE). Total + idempotent. Stored copies (`BodyAssetStore`, share sheet, `MessageBody.icsText`) stay byte-identical full originals. Any new caller that presents ICS must route through here (or call the sanitizer). See `PLAN_ICS_SANITIZER.md`.

### Attachment Support
- `AttachmentInfo` (filename, contentType, section, size) stored as JSON in `MessageBody.attachmentsJSON`
- IMAP: `IMAPProvider.fetchAttachment` fetches by MIME section via `server.fetchPart(section:of:)`
- Gmail: `GmailProvider.fetchAttachment` fetches by `attachmentId` via REST API (`/messages/{id}/attachments/{attachmentId}`). `AttachmentInfo.section` stores the `attachmentId` for Gmail.
- Gmail attachment metadata extracted in `fetchMessage` via `extractAttachments(from:)` — recursively walks the `GmailPart` tree for parts with non-empty `filename` and `body.attachmentId`
- **Walk from the TOP-LEVEL `payload`, not just `payload.parts`** — a single-part message (e.g. a Google **DMARC aggregate report** = one `application/zip`, no text body, no `parts`) carries its attachment on the payload node itself. `fetchMessage` builds a synthetic root `GmailPart` from `msg.payload` (mimeType/filename/headers/body/parts) and passes that to `extractAttachments`, mirroring the shared NSE parser `GmailParse.walkParts` (which is why the inbox `hasAttachments` paperclip was already correct via `GmailParse.parseMessage`). `GmailPayload` gained a `filename` field (with a defaulted memberwise init) for this. Regression test: `GmailProviderMockTests.singlePartAttachmentOnlyMessage`.
  - **Why it mattered (infinite-loop bug, fixed 2026-06):** before the fix, such a message came back with empty body AND `attachments=0` → `BodyFetchProcessor` confirmed it empty (`bodyComplete=1`, FTS body empty) → the AI **reply** job (`ActiveAIQueue`, `WHERE isInInbox=1 AND bodyComplete=1 AND cachedReply IS NULL`) re-enqueued forever, dropping at the "No FTS body" guard each time (`maxRetries:.max`). The no-reply filter never ran (reply dropped before reaching `processReply`), so the inbox kept showing the reply as pending. Root-cause fix = surface the attachment so it routes through the `[attachment]` body path instead of confirmed-empty. (`cachedReply=''` on confirmed-empty was the considered loop-stopper alternative; not needed once the parse is fixed.)
- `Section` type in SwiftMail: construct with `Section("1.2")` (dot-separated string) or `Section([1, 2])` (array)
- QuickLook preview: write to temp file → `.quickLookPreview($previewURL)`
- Share/save: `ShareLink(item: fileURL)` on downloaded attachment rows in `AttachmentListView`
- Downloaded files cached in `downloadedFiles: [String: URL]` — re-tap shows QuickLook without re-download
- **Attachment preview "blink/reload" — file QuickLook is now IMPERATIVE (ADR-IOS-045).** Symptom: tapping a file attachment (pdf/xlsx) opened a preview that blinked/reloaded during background sync. ROOT CAUSE: the SwiftUI `.quickLookPreview` modifier was hosted INSIDE the message-detail `List` row (`AttachmentListView` → `MessageCardView` → `List`), so any re-render of that subtree reconfigured the preview and the **out-of-process QuickLook extension** got interrupted/relaunched (device log `logmain.log` 2026-06-24: `Connection to appex interrupted` + repeated `QLOverlayDefaultActionBut…` re-layouts). Reproduced for BOTH a pdf and an xlsx (different QL extensions → common cause is the host, not the file). FIX = present file previews **imperatively** via `AttachmentQuickLook` (enum in `AttachmentListView.swift`): a `QLPreviewController` presented from the top view controller, detached from the SwiftUI tree (same pattern as `ICSCalendarImporter`) — no re-render can touch it. `NSURL` is the `QLPreviewItem`; dismissal via `QLPreviewControllerDelegate.previewControllerDidDismiss`; controller retained in a `@MainActor` static (its dataSource/delegate are weak); delegate is `@unchecked Sendable` (stateless, mirrors `SafariDelegate`). The **`.eml` preview stays a SwiftUI `.sheet`** (`EmlAttachmentPreview`, `.presentationDetents([.large])`) — it's an in-process SwiftUI view, not an out-of-process QL extension, so it doesn't churn; migrate it the same way only if it ever blinks.
- **PreviewFreezeGate is now defense-in-depth, NOT the correctness path for QuickLook.** `PreviewFreezeGate.shared` (`SyncStatusSubtitle.swift`, `@MainActor @Observable`) is still raised around any preview (file via `AttachmentQuickLook`, `.eml` via the sheet) to quiet background sync — but since file QL is detached from the tree, correctness no longer depends on it. It still matters for the `.eml` sheet (still in the row) and for CPU/quiet-UI. `begin()` raised before present, `end()` on dismissal; `end()` posts `.previewFreezeReleased`. Consumers buffer-and-flush while frozen: (1) `SyncStatusObservationModifier.refresh` skips (holds env sync-status steady); (2) `MessageDetailViewModel` buffers `.messageDataDidChange` ids in `pendingRefreshIds`; (3) `MessageCardView` buffers `.inboxDataDidChange` → `loadUserLabels()` via the pure `PreviewGatedReload` value type (`request(isFrozen:)`/`release()`), extracted so it's unit-testable. Tests: `PreviewFreezeGateTests` + `PreviewGatedReloadTests` in `EmailRenderPipelineTests.swift` (next to `ScrollFreezeGateTests`). NOTE: the freeze-gate consumer chain was a whack-a-mole of re-render *sources* and the device log proved it insufficient on its own (gate held, zero data-change notifications, yet the preview still cycled) — which is why file QL was moved out of the tree entirely. Lesson: SwiftUI modal presentation (`.sheet`/`.quickLookPreview`) hosted in a `List`/`ForEach` row is a fragile anti-pattern; detach it, don't gate around it.

### Compose AI Suggestion — chat pill edits suggestion in place when bubble is visible
- Reply suggestion bubble (`ComposeView.swift:307-335`) and the body TextEditor are mutually exclusive — while the bubble is up, `messageBody` is empty.
- Chat pill ("Edit Draft") routes by `showingSuggestion`:
  - **Bubble visible:** input = `currentSuggestion`, output writes back to `currentSuggestion` AND `messageHeader.cachedReply` (DB) so reopens show the edited version. Bubble stays up.
  - **After Use Reply / Dismiss:** input/output is `messageBody` (existing flow, unchanged).
- `prepopulate()` re-reads `cachedReply` fresh from DB so prior edits survive in-memory staleness of caller's `MessageHeader` snapshot.
- Recompute (`ComposeView.swift:825`) still overwrites `cachedReply` — by design, "give me a fresh AI draft" discards edits.
- Subject / recipient deltas always go to compose state, regardless of whether bubble is up.

### Compose body — caret-aware scroll, NO input gating
- Compose body uses `TextEditor` with `.scrollDisabled(true)` inside the parent SwiftUI `ScrollView`. UIKit propagates scroll-to-cursor via `scrollRectToVisible` on the enclosing UIScrollView, passing the **whole TextEditor frame** (often hundreds of pt) — so we swap the scroll view's class to `CaretAwareUIScrollView` (`ComposeView.swift`) which substitutes the caret rect, no-ops if already visible, and animates with a `UIViewPropertyAnimator`.
- **DO NOT gate text input during scroll.** A previous version (commit 755fe97, removed) class-swapped the focused UITextView to a `GatedUITextView` and dropped `insertText`/`deleteBackward`/`paste` for 500 ms after every `scrollRectToVisible`. UIKit fires that on every line wrap / Enter / keyboard frame change during normal typing — so a fast typist lost 4-6 keystrokes whenever the body scrolled. There are no real "accidental keystrokes" to drop (UIKit doesn't pre-queue keypresses); the gate is purely destructive.
- If you ever feel the urge to "block input briefly while the UI settles" in compose, stop and re-read this note.

### Compose Attachments
- `DraftAttachment` (filename, mimeType, data) in `DraftMessage.swift`
- ComposeView: bottom-right `Menu` with `PhotosPicker`, file importer (`.item`), camera (`CameraPickerView`)
- IMAP send: `DraftAttachment` → SwiftMail `Attachment(filename:mimeType:data:)` → `Email(... attachments:)` — SwiftMail `constructContent()` handles MIME multipart
- Gmail send: `buildMIMEMessage(draft:)` produces `multipart/mixed` with base64 attachment parts, base64url-encoded for `messages/send` API
- `CameraPickerView.swift`: `UIImagePickerController` wrapper via `UIViewControllerRepresentable`
- `NSCameraUsageDescription` in `Info.plist` for camera access

### SwiftMail Types
- `Section` — top-level struct (NOT `MessagePart.Section`)
- `UID(_ value: UInt32)` — construct UID from UInt32
- `UIDSet(UID(...))` — construct from single UID
- `Mailbox.Selection` — returned by `server.selectMailbox()`
- `Mailbox.Info.Attributes` — folder attributes (`.inbox`, `.sent`, etc.)

### Outgoing email header encoding — RFC 2047 (mojibake fix, 2026-06-21)
- **Header fields MUST be 7-bit ASCII.** Non-ASCII Subject/display-name text must be wrapped in an RFC 2047 encoded-word (`=?UTF-8?B?…?=`). Injecting raw 8-bit UTF-8 into a header (the old behavior) lets downstream agents charset-guess and mojibake it (Korean `가` → `ÃªÂ°Â…` = double UTF-8-as-Latin-1). Bodies were never affected — they carry `charset=UTF-8` + Content-Transfer-Encoding; headers have no mechanism except RFC 2047.
- **Encoder lives in TWO places (cannot share — SwiftMail is a remote SPM pin, not a local path):** iOS app `TabMail/Helpers/RFC2047.swift` (`RFC2047.encodeHeaderValue`, used by `GmailProvider.buildRFC822`/`buildMIMEMessage`); SwiftMail fork `String.rfc2047EncodedHeader()` (used by `Email+Content.writeHeaders` Subject + `EmailAddress.headerString()` for From/To/Cc names, + `EMLSerializer`). Keep the two byte-for-byte identical. ≤45 UTF-8 bytes/word (≤75-octet word), split on char boundaries, folded with `CRLF SPACE`.
- **Gmail API decode is UNDOCUMENTED** — the REST reference does not say whether `payload.headers[].value` is RFC 2047-decoded. So `GmailParse.parseMessage` decodes the Subject **defensively** via `RFC5322Parse.decodeRFC2047` (idempotent no-op when there's no `=?…?=`). This guarantees our own encoded sent subjects (and any encoded incoming subject) render clean instead of literal `=?UTF-8?B?…?=`. Note: `GmailParse` deliberately does NOT decode From/To/Cc *names* (the `parseFromHeader` test asserts the raw encoded displayName) — out of scope; subject-only.
- **`RFC5322Parse.decodeRFC2047` merges adjacent encoded-words** (drops fold whitespace between two `=?…?=`, preserves whitespace around plain text) — required so folded long subjects decode without a stray `\r\n `. SwiftMail's `decodeMIMEHeader()` already did this.
- **Exchange/Outlook is unaffected** — `ExchangeProvider` sends `message["subject"]` as a structured JSON field to Graph `/sendMail`; Graph builds the MIME and encodes the header server-side. Never hand-builds a header.
- **Fork changes need push + Package.resolved re-pin to reach IMAP/iCloud builds.** The iOS GmailProvider fix is app code → live on next build.

### Hybrid FTS5 + Vector Search (Local)
- **Separate SQLite database** (`fts.db` in Application Support) using GRDB — separate from main `tabmail.sqlite`
- **FTS5 schema**: 7 columns matching TB — `messages_fts_YYYY` (msgId, subject, from_, to_, cc, bcc, body) year-sharded
- **BM25 weights**: msgId=0, subject=5, from_=3, to_=2, cc=1, bcc=1, body=1
- **UNION ALL search**: single query across all year shards (not per-year loop). Date filtering in SQL.
- **Synonym expansion**: ~65 email synonym groups (ported from Rust `synonyms.rs`)
- **Query parser**: field aliases (`from:` → `from_:`), auto-wildcards for tokens ≥4 chars, OR groups for synonyms
- **Vector search**: sqlite-vec KNN (v0.1.7-alpha.2), embeddings in `messages_vec` virtual table
- **sqlite-vec safety**: ALL `messages_vec` access MUST go through the writer connection (`writeWithoutTransaction` for reads). Reader pool connections create vec0 vtab instances that crash in `vec0_free_resources` on disconnect. No `sqlite3_auto_extension` — registered per-connection in SearchIndex only.
- **Hybrid merge**: 70% vector / 30% keyword scoring, min score 0.1, 4× candidate multiplier
- **FTS-only fallback**: when vec table empty (embedding rebuild), uses `searchFTSOnly()` — sorts by dateMs DESC, rank ASC
- **Date-range scan**: `query="*"` with date params → `scanByDateRange()` (no FTS MATCH, just date filter on message_meta)
- **Column-scope filter-first**: `from:"email"` queries pre-filter vec candidates to matching rowids
- **Indexing pipeline**: headers indexed synchronously (`await indexHeadersForFTS`) — no `Task.detached`, eliminating body race condition. Body + embedding on fetchBody (AccountManager hook).
- **Bulk index**: runs after sync if FTS index is sparse (first-launch catch-up). Phase 1: headers, Phase 2: body backfill from GRDB.
- **Background embedding rebuild**: generates embeddings for messages with bodies but no embeddings, low priority
- **Model conversion**: `Scripts/convert_model.py` → `AllMiniLML6v2.mlmodelc` + `tokenizer.json` in Resources
- **Graceful degradation**: if model/tokenizer not in bundle, falls back to FTS-only (keyword search)

### Device Prompt Sync & AI Cache Probe
- `DeviceSyncService` — always-on WebSocket to Cloudflare DO relay (`sync-dev.tabmail.ai`)
- Auto-connect in `RootView.task`, reconnect in `.active`, disconnect in `.background`
- **Text fields (composition, action, kb)**: State-based delta merge with **peer-received base** as common ancestor. Steps: (1) epoch-zero → skip, (2) stale → skip, (3) first sync (no peer base) → LWW, (4) fast-forward (no local changes) → accept incoming, (5) both changed → 3-way bullet merge using `peer_base` as ancestor. **Critical**: `peer_base = incoming` (what peer sent), NOT merged result. Peer base stored in UserDefaults (`device_peer_base:*` / `device_peer_base_ts:*` keys). One-time migration from old `syncBase*` keys.
- **Templates**: per-template CRDT merge by id (newer `updatedAt` wins per template)
- **DisabledReminders**: per-hash CRDT merge (newer `ts` wins per hash)
- **Epoch-zero protection**: defaults and resets get epoch-zero timestamps — never overwrite customized content
- **Virgin device detection**: all timestamps epoch-zero → skip broadcast, probe peers instead
- `isSyncApplying` flag prevents echo loops when applying incoming sync
- 500ms debounce on outgoing broadcasts to prevent flooding during typing
- **AI cache probe**: before LLM, ask connected peers for cached results via `probeAICache(keys:)` (2s timeout, always probes)
- Probe key = RFC 2822 Message-ID header without angle brackets (device-independent, matches TB's `headerMessageId`)
- `rfc822MessageId` field on `MessageHeader` — populated from IMAP ENVELOPE or Gmail `Message-Id` header
- TB IDB keys: `summary:<accountId>:<folderPath>:<cleanHeaderMessageId>` — iOS probe handler searches by `rfc822MessageId.contains(key)`
- Date encoding: `.iso8601` for cross-platform template sync (TB sends ISO strings, not timestamps)
- Backend SSE: `BackendClient.extractSSEFinalData()` parses `event: final` from `text/event-stream` responses

### Cross-Instance Action Tag Sync (ADR-IOS-036, supersedes ADR-IOS-004 "First Compute Wins")
- Action tags are **local-only** on iOS. No `tm_*` IMAP keyword, Gmail label, or Exchange category is written or read. `MessageHeader.actionTag` is driven by `MessageAICache.restoreIfCached` + AIService classification only.
- Cross-device parity on overlapping inboxes uses **Device Sync** (`DeviceSyncService.probeAICache`): iOS probes peers on cache miss before running the LLM. Peers that have the classification respond with `AICacheResult(summary, action, reply)`.
- Async cross-device pickup (device A offline when device B processes) is intentionally **not** covered — device B runs the LLM independently. Cost = one duplicate LLM call per miss. Accepted tradeoff.
- User manual overrides (long-press menu → pick action) go through `AccountManagerAI.setManualTag`: optimistic local `MessageHeader.actionTag` + `MessageAICache` write, plus a PendingOperation(.setTag) that drains to a no-op (legacy queue rows flush cleanly).
- Legacy `tm_*` keywords/labels/categories still present on user servers from prior versions are **not scrubbed** (too risky); they decay naturally as messages turn over. `UserLabelStore.shouldExcludeLabel` (matches `tm_` prefix) keeps them out of user-visible label chips.

### AI Summary & Action Pipeline

- `AIService` actor — orchestrates summary generation + action classification via backend API
- `BackendClient.sendCompletions()` — calls `POST /completions/chat` with `X-Client-Type: ios`
- **Summary flow**: system message with `content: "system_prompt_summary"` + template variables (user_name, subject, from_sender, body, email_date, etc.) → backend expands into multi-message prompt → returns structured text
- **Action flow**: system message with `content: "system_prompt_action"` + variables → 3 parallel calls → mode (most common action wins)
- **Variable encoding**: `CompletionsMessage` uses dynamic coding keys to flatten vars into top-level JSON keys alongside role/content
- **Response parsing**: summary uses section-based text parsing (Todos/Two-line summary/Reminder); action uses JSON parsing (`{"action": "...", "justification": "..."}`)
- **Cross-instance dedup now via Device Sync probe** (ADR-IOS-036, supersedes ADR-IOS-004 "First Compute Wins"): `DeviceSyncService.probeAICache` queries connected peers before running the action LLM; hit = adopt, miss = compute.
- `AccountManager.processAIForAccount()`: gathers `AIMessageSnapshot`s on main actor, dispatches to AIService, updates `MessageHeader.actionTag` + `MessageAICache` locally. No provider-side tag write.
- `EmailFilter.isNoReply()` / `.hasUnsubscribeLink()` — lightweight heuristics for LLM prompt context. `hasUnsubscribe` is NOT used in reply skip (too aggressive for mailing lists) — only `isNoReply` drives `skipCachedReply`.
- `MessageHeader` summary fields: `summaryBlurb`, `summaryTodos`, `reminderDate`, `reminderTime`, `reminderContent`
- Provider `setActionTag` methods exist as no-op stubs (signature kept so legacy PendingOperation(.setTag) drain flushes cleanly). ADR-IOS-036.

### Background AI Processing (Tier 3)
- **BGProcessingTask** (`ai.tabmail.ai-processing`) — long-running background task for AI processing (up to ~10 min)
- Requires `processing` in `UIBackgroundModes` and task ID in `BGTaskSchedulerPermittedIdentifiers`
- Registered in `TabMailApp.init`, handled in `SyncScheduler.handleBackgroundAIProcessing()`
- Scheduled on app background in `RootView.onChange(of: scenePhase)`, 5 min earliest begin date
- Flow: WiFi check → poll (sync) → processMessagesForAccount per active account → embeddings → badge update
- **`beginBackgroundTask`** protects in-flight AI calls in both `processMessagesForAccount` (queue path) and `processMessage` (priority path) — ~30s grace period when app backgrounds

### Swift 6 BGTask Isolation Pattern (ADR-IOS-020) — CRITICAL
- **BGTask expiration handlers run on arbitrary Apple threads** — NOT the main thread
- In Swift 6, ALL locals in `@MainActor` methods are main-actor-isolated — accessing them from `@Sendable` expiration closures causes `EXC_BREAKPOINT` at runtime, even for `Sendable` types like `Mutex<Bool>`
- **`nonisolated(unsafe)` on locals does NOT help** — only works on stored properties (SE-0412)
- **Required pattern for BGTask handlers in `@MainActor` classes:**
  1. Handler method must be `nonisolated func` (strips isolation from locals)
  2. Shared state via `BGTaskContext` class (`@unchecked Sendable` + `NSLock`)
  3. `@preconcurrency import BackgroundTasks` (ObjC types lack Sendable annotations)
  4. Set `task.expirationHandler` BEFORE creating `Task { @MainActor in }` (use-then-send ordering for SE-0414)
  5. Only the `Task { @MainActor in }` body calls `task.setTaskCompleted` — expiration handler only sets flag + cancels task
- **`CheckedContinuation` double-resume guard**: `NWPathMonitor.pathUpdateHandler` can fire multiple times racing with `cancel()`. Always guard with `Mutex<Bool>` before `continuation.resume()`
- **`requestBackgroundGracePeriod`**: expiration handler must not access `@MainActor` state — use a safety-timeout `Task { @MainActor in }` to call `endBackgroundTask` instead

### SSE Streaming & Tool Execution Loop
- `BackendClient.sendCompletionsWithTools()` — full multi-turn tool execution loop matching TB's `sendChatCompletions()` + `onToolExecution` pattern
- Parses SSE events: `keepalive`, `tool_started`, `tool_completed`, `tool_failed`, `final`, `error`
- When `final` contains `tool_calls`: executes tools via `ToolRegistry`, appends results to `conversation_state.harmony_messages`, sends next round
- `CompletionsRequest` now includes optional `conversation_state` for multi-turn continuation
- Max 10 tool rounds before forcing a final response with `disable_tools: true`
- `AIService.sendWithTools()` — entry point for tool-enabled completions (used by future chat/reply features)
- Existing summary/action pipeline unchanged (still uses `sendCompletions` — single-shot, no tools)

### Thread (ThreadGroup) Actions — tag-tap dispatch & grouped undo
- A collapsed thread row's badge shows `group.threadTag` = the **highest-priority** tag across all members (`reply`＞`none`＞`archive`＞`delete` by `ActionTag.sortOrder`, computed in `ThreadGroupBuilder.buildDisplayGroups`). This can differ from the representative/head message's own `actionTag`.
- **`InboxView.executeTaggedAction` MUST act on `group.threadTag` for a collapsed thread, NOT `snapshot.actionTag`.** The `snapshot` arg is `group.representative` (the most-recent/head message); using its tag was a bug — tapping a surfaced "reply" badge archived the whole thread when the head happened to be tagged archive. Single messages and expanded-thread child rows still use `snapshot.actionTag`. The `.reply` case targets `snapshot.id` (the head = most recent), which is the intended reply target.
- **All multi-message thread actions push ONE grouped `UndoableAction`** carrying every member, so the whole thread restores in a single undo: `archiveThread` / `deleteThread` / `moveThread` in `InboxViewModel`. `UndoService.undo` + `AccountManagerActions.undoDestructiveAction` restore all members in one write txn + one move-back op. When adding a new whole-thread action, mirror this pattern — never loop a per-message action (that yields N undo entries). Move was the last gap; fixed via `moveThread` + `moveThreadGroup` sheet state (collapsed-thread Move swipe). Expanded-thread child actions stay per-message by design.
- **`InboxView.body` is at the SwiftUI type-checker limit.** Adding another `.sheet`/`.fullScreenCover` modifier triggers "unable to type-check this expression in reasonable time" (error points at an *unrelated* existing modifier). Fold new sheet content into the existing sheet via a `@ViewBuilder` computed property (e.g. `moveSheetContent`) and extract closures into private methods — don't append more top-level modifiers to `body`.
- **`expandedThreads` is pruned in `rebuildDisplayGroups()` (2026-06-09).** Only the head-row swipe/chip paths call `evictAndRebuild` (which removes the group id); every OTHER removal pathway (expanded-child swipe/chip, detail-view archive/delete via `.messageDismissedFromDetail`, agent tools, sync from another device) shrinks the thread without touching `expandedThreads`. A stale id on a group that dropped to `memberCount == 1` permanently painted `Color(.tertiarySystemFill)` listRowBackground (reads as a stuck selection highlight) + hidden separators on the remaining row, with no chevron to clear it (`MessageRowView` renders the toggle only for `isThread`). The prune intersects `expandedThreads` with the rebuilt multi-member group ids — the rebuild choke point covers all pathways. Rule: any UI state keyed by ThreadGroup id must be pruned/validated in `rebuildDisplayGroups`, not at individual call sites. Tests: `InboxViewModelThreadEvictTests` (`childRemovalPrunesExpandedThreads`, `threadRemovalPrunesExpandedThreads`, `survivingThreadKeepsExpansion`).

### Manual Tag Teaching (Long-Press Context Menu)
- Long-press on message rows (both normal and triage view) shows "Tag as Reply/Archive/Delete" + "Remove Tag" context menu
- `InboxView.tagContextMenu(for:)` builds the menu; `InboxViewModel.applyManualTag()` dispatches to `AccountManager.applyManualTag()`
- `AccountManager.applyManualTag()` handles the full flow: optimistic UI → IMAP/Gmail write → AI cache update → fire-and-forget auto-prompt update
- **Auto-prompt refinement**: `AIService.autoUpdateUserPromptOnTag()` sends email metadata + summary + original action + user's correction to backend (`system_prompt_action_refine`), gets an ADD/DEL patch, applies it via `ActionPatchApplier` to `PromptStore.rawAction`
- `ActionPatchApplier` — port of TB's `patchApplier.js`: parses multi-operation patches, finds section headers, handles duplicate detection, case-insensitive DEL matching
- Self-sent emails are blocked from manual tagging (matches TB's `isInternalSender` check)
- Updated user_action.md auto-syncs to TB via Device Sync (PromptStore setter triggers `DeviceSyncService.debouncedBroadcast`)

### Tool Registry (Scaffold)
- `ToolRegistry.swift` — `AgentTool` protocol + `ToolRegistry` actor (central registry matching TB's `core.js`)
- Tools registered at startup, looked up by name during tool execution loop
- Types defined: `CompletionsSSEEvent`, `ToolStatusEvent`, `CompletionsFinalEvent`, `CompletionsToolCall`, `ConversationState`, `HarmonyMessage`, `ToolTrace`, `ToolResult`
- No tool implementations yet — scaffold only. Tools added incrementally (memory_search → email tools → calendar/contacts)

### Agent Chat (ADR-IOS-022, ADR-IOS-023)
- **Mobile-native UX (ADR-IOS-023)**: iOS departs from TB's "infinite chat" UI while keeping backend architecture identical:
  - **No welcome-back/greeting bubbles** — reminders shown as top-of-chat cards instead
  - **Session history with swipe navigation** — past K sessions (configurable, default 10) shown as horizontally swipeable pages in TabView(.page). Rightmost = current/newest. Resuming an old session swaps the conversation history sent to the API.
  - **Multi-turn within session** — `sessionTurns` sent as `history` to API. When resuming an old session, that session's persisted turns become the history.
  - **Nudges become reminder cards** — urgent reminders get accent highlighting, tap to expand, dismiss to snooze
- **ChatStore** (`Services/AI/ChatStore.swift`) — `actor` with GRDB-backed `chatTurn` table. Matches TB's `persistentChatStore.js`.
  - `ChatTurn` model: id, timestamp (epoch ms), role, content, userMessage, type, chars, renderedContent, sessionId
  - Budget enforcement: 50 exchanges max (100 turns), 25K chars max, FIFO eviction in atomic write transaction
  - Turns still persisted for Settings > Chat History view, just not loaded back into live chat
- **AIChat** (`Services/AI/AIChat.swift`) — `AIService` extension for agent chat via completions API
  - Builds: system message (`system_prompt_agent`) + user message (`chat_converse`). History is current session's turns (multi-turn).
  - KB + reminders refreshed per turn (matches TB parity)
  - Sends via `sendWithTools()` — server-side tools auto-execute in backend. Client-side tools registered in ToolRegistry.
  - `chatUserName()` queries GRDB directly for primary account display name (no MainActor dependency)
- **DynamicIslandChatButton** — chat pill UI. On expand: loads reminder cards (InboxView only), registers email context ID. Persists turns on send/receive. `enrichedText` built inside Task block (after awaiting ID registration) to prevent race.
- **ChatHistoryView** (`Views/Settings/ChatHistoryView.swift`) — searchable history in Settings > AI > History > Chat History. Pairs user+assistant turns into exchanges. Debounced GRDB search, expand/collapse on tap, debug stats in toolbar menu.
- **ChatIdTranslator** (`Services/AI/ChatIdTranslator.swift`) — in-memory actor mapping numeric IDs ↔ real MessageHeader.id. Matches TB's `idTranslator.js`. `processResponseForDisplay()` resolves `[Email](N)` patterns to tappable pills with subject text. Cleared when chat history is cleared.
- **Client-Side Tools** (registered in `ToolRegistry` at app startup via `TabMailApp.init()`):
  - `InboxReadTool` (`Services/AI/Tools/InboxReadTool.swift`) — queries GRDB inbox, assigns numeric IDs via ChatIdTranslator, formats matching TB's `formatMailList`. Page size 10.
  - `MemorySearchTool` (`Services/AI/Tools/MemorySearchTool.swift`) — per-turn hybrid FTS5 + sqlite-vec search via `MemoryIndex` (memory.db sidecar). Role-tagged output (USER/AGENT). Page size 5. See ADR-IOS-034.
  - `MemoryReadTool` (`Services/AI/Tools/MemoryReadTool.swift`) — returns session-bounded context window (±N turns from matched timestamp, clamped to matched turn's session). TB parity.
  - Tool definitions live in the backend repo (`src/tools/ios/inbox_read-v1.0.0.json`, `memory_search-v1.0.0.json`)
- **MarkdownChatText** (`Views/Agent/MarkdownChatText.swift`) — SwiftUI view rendering markdown + email pills + reminder cards. Uses `AttributedString(markdown:)` with `inlineOnlyPreservingWhitespace`. Pre-processes `[Email](N)` → `[📧 Subject](tabmail://email/N)` for tappable links. Handles URL taps via NotificationCenter (`.emailPillTapped`).
- **Backend templates** (in the backend repo, `src/prompts/ios/`):
  - `system_prompt_agent-v1.0.0.md` — full iOS agent system prompt (identical to TB v1.2.0 for tool sections, with iOS-specific intro and mobile response formatting)
  - `chat_converse_user_message-v1.0.0.md` → aliased to `chat_converse` in registry (security reminder + `[timestamp] user_message`)
  - `chat_converse_history-v1.0.0.md` — prior session history wrapper (background memory only)
  - `chat_converse_reminders-v1.0.0.md` — reminders injection (currently unused, iOS sends empty string)
- **KB Refinement** (`Services/AI/KBRefinementService.swift`) — `actor` matching TB's `knowledgebase.js`:
  - Triggered on chat session expiry (idle > 30s) with session turns via fire-and-forget Task
  - Sends `system_prompt_kb_refine` + current KB + chat history to backend orchestrator
  - Backend returns `refined_kb` (multi-step: chat summarize → reminder extract → KB refine → trim)
  - Persists refined KB to `PromptStore.shared.rawKB` (triggers Device Sync broadcast + reminder re-parse)
  - Guards: privacy opt-out check, minimum 3 exchanges, serialized execution (drops concurrent calls)
  - Compose mode sessions excluded (no KB value from draft edits)
  - `CompletionsResponse.refined_kb` field decodes the backend orchestrator's response
- **GRDB migration**: v6 (`v6_createChatTurn`) adds `chatTurn` table with timestamp index; v7 adds `renderedContent` column; v11 adds `sessionId` column + index; v12 creates `chatIdMapping` table (numericId PK, realId UNIQUE) for persisting ChatIdTranslator mappings across app restarts
- **ChatIdTranslator persistence**: ID mappings (numeric ↔ real) persisted to GRDB `chatIdMapping` table. Lazy-loaded on first `toNumericId()` call. New mappings written on creation, deleted on eviction/sweep/clear. Ensures `[Email](N)` pill references in old chat sessions remain resolvable
- **Event pill detail must carry notes AND attendees at every cache site**: the `[📅 Title](tabmail://event/N)` popover (`EventPillPopover` in `MarkdownChatText.swift`) renders title/time/recurrence/availability/location/**notes**/attendees/tz/calendar. `EventPillDetail.notes` ← `GCalEvent.description` (Zoom/Meet links live here). Every `cacheEventDetail` call site MUST pass real `notes` + `attendees` — NEVER hardcode `attendees: []` when the data is available. Sites: `CalendarEventCreateTool` (from tool args via `CalendarToolHelpers.eventPillAttendees(fromArguments:)` + `description` arg), both `CalendarEventReadTool` direct-lookup paths and `cacheEventDetailsForPills` (from the `GCalEvent` via `eventPillAttendees(from:)` + `event.description`). `resolveEventDetail`'s live re-fetch also reads `event.description`. Hardcoding empty attendees in the create/read cache is a bug: the in-memory cache HIT short-circuits the live re-fetch, so the pill shows empty until the entry is evicted. (Fixed 2026-05-27; regression tests in `ToolCachingTests` + `CalendarToolHelpersTests`.) Free/busy-reader events intentionally keep `notes: nil` / `attendees: []` (server strips them).
- **Factory reset**: `DELETE FROM chatTurn` included in SettingsView nuke

### Screen Keep-Awake (chat pill)
- `Theme/ScreenKeepAwake.swift` — reference-counted wrapper around `UIApplication.isIdleTimerDisabled` + `.keepScreenAwake(while:)` view modifier. The idle-timer flag is a single global, so holders are counted; the modifier tracks its own held state (`@State holding`) so acquire/release fire exactly once per transition regardless of `onChange` vs `onDisappear` teardown ordering.
- Applied once in `DynamicIslandChatButton.swift` on the pill root: `isExpanded || isWorking || ActiveAgentTracker.shared.anyWorking || speechRecognizer.isRecording` (same scope as the wand-glow indicator). Covers all three host screens (Inbox/Compose/MessageDetail) with no per-screen wiring.
- No `scenePhase` handling needed — iOS only honors `isIdleTimerDisabled` while the app is foreground; the hold resumes automatically on return. Reuse `.keepScreenAwake(while:)` for any future keep-awake need (never set `isIdleTimerDisabled` directly).

### Agent Compose FIFO Queue (ADR-IOS-030)
- **Problem solved**: Agent compose tools (`email_compose`, `email_reply`, `email_forward`) used to write directly to `AgentToolRouter.pendingCompose` (single slot). If a compose window was already presented (manual or prior agent), the second request was silently dropped because SwiftUI cannot stack two `fullScreenCover`s from one source view. The LLM still got a success string back.
- **Fix**: In-memory FIFO queue on `AgentToolRouter`. Tools call `enqueueCompose(_:)` (synchronous, fire-and-forget). Tools' return strings are unchanged.
- **Manual compose blocks the queue**: Tracking lives inside `ComposeView` itself via `.onAppear { composePresentationDidBegin() }` / `.onDisappear { composePresentationDidEnd() }`. Counts every presentation path automatically — manual New, contact, reply, replyAll, forward, agent compose, agent draft. No per-cover-site instrumentation. `DraftComposePresenter` carries the same hook on its body root to close the loading-window race before the inner `ComposeView` renders.
- **Dispatch guard has 4 conditions**: `!awaitingAppear`, `pendingCompose == nil`, `presentationCount == 0`, queue non-empty. `awaitingAppear` is the race-window guard — set to `true` at dispatch, cleared in `composePresentationDidBegin`. Without it, a second `enqueueCompose` arriving between "view nils `pendingCompose` after capture" and "ComposeView's `onAppear` fires" would overwrite the in-flight request via `fullScreenCover(item:)`'s undefined behavior when the binding swaps non-nil identifiables mid-presentation.
- **Decrement-to-zero triggers next dispatch**: `composePresentationDidEnd` clamps at 0 and only calls `dispatchNextIfIdle` when count returns to 0, so the next request appears immediately after the previous window's dismiss animation completes.
- **Lifecycle hooks safe against ComposeView's internal modals**: Apple Forums thread 655338 confirms parent view's `onAppear`/`onDisappear` do NOT fire when parent presents `.alert`, `.popover`, `.photosPicker`, `.fileImporter`, `.sheet`, or `.fullScreenCover`. ComposeView contains all of these (camera fullScreenCover at line 541, alerts, photo picker, etc.) — count does not drift.
- **In-memory only**: Lost on app kill (acceptable — agent compose is session UI intent, not durable user action).
- **No cancellation needed**: User can't tap Stop while a compose `fullScreenCover` is up (chat surface is hidden behind it).
- **`@ObservationIgnored` on private fields**: `composeQueue`, `presentationCount`, `awaitingAppear` are marked `@ObservationIgnored` so they don't create false observation dependencies. Only `pendingCompose` is observed (by view `onChange` handlers).
- **Known limitation**: If neither InboxView nor MessageDetailView is alive when the queue dispatches, `pendingCompose` stalls. Recoverable — opening any compose window clears `awaitingAppear` and resumes draining on the next dismiss.

### Outgoing Threading — reply/forward stay in-thread on every provider (ADR-IOS-043, 2026-06-23)
- **Single source of truth: `ThreadUtils.outgoingThreadHeaders(...)`** builds the outgoing `inReplyTo` + full normalized/deduped `References` chain (`parent.references ++ parent.rfc822MessageId`) + Gmail `threadId`. Pure scalar core + a `MessageHeader` adapter; `ComposeView.send` is the ONE callsite (covers manual AND agent `email_reply`/`email_forward`, which open the same ComposeView).
- **Reply and forward share ONE path** (no `isForward` branch). Per Gmail convention (web-verified) a forward stays under the source conversation on the sender's side, so it gets the same threading headers as a reply. Forward-specific bits (empty To, `Fwd:` subject, quote) stay in ComposeView.
- **The bug this fixed:** `ComposeView.send` set only `inReplyTo` and never built `references` (empty `References` on every send), AND Gmail's REST `send` omitted `threadId`. Gmail's `users.messages.send` does NOT thread by headers alone — it needs `threadId` + RFC `References`/`In-Reply-To` + matching `Subject`. Result: Gmail web filed every iOS reply as a NEW conversation. (TB is unaffected — it sends Gmail via SMTP, header-threaded. Functional parity, not implementation parity.)
- **Gmail `threadId` is guarded twice** (in the builder): parent account == sending account, AND `normalizeSubject(sendSubject) == normalizeSubject(parentSubject)`. A failed guard → `nil` → a fresh thread (correct for cross-account / changed-subject). Preconditions, not a fallback (ADR-003).
- **Plumbing:** `threadId` added to `DraftMessage` + persisted on `OutboxMessage` (**v60** migration, so a post-relaunch drain re-sends with the same binding). `Draft` GRDB record UNCHANGED — derived from the resolved `replyTo` at send, like `inReplyTo` always was.
- **Provider responsibilities:** Gmail send includes `threadId` (`GmailProvider.buildSendBody`, extracted for tests). Exchange (`internetMessageHeaders`) + IMAP/SMTP (`buildEmail`) already emit In-Reply-To/References — NO code change, they just receive a populated chain now. `ExchangeProvider.buildGraphSendPayload` is `nonisolated` (pure, so unit tests can call it synchronously without crossing the actor boundary — `[String:Any]` is non-Sendable).
- Tests: `OutgoingThreadHeadersTests`, `GmailSendBodyTests`, `ExchangeSendPayloadTests`, `DatabaseOutboxTests` (v60 + round-trip), existing `IMAPProviderBuildEmailTests`. Plan: `PLAN_THREAD_FIX.md`.

### Outbox — Persistent Offline Send Queue (ADR-IOS-019)
- `OutboxMessage` GRDB model in `outboxMessage` table — stores full draft (to/cc/bcc/subject/body/isHTML/inReplyTo/references/threadId) with status (queued/sending/failed) and `sentAt` timestamp
- Attachments stored on disk under `Application Support/TabMail/outbox_attachments/{id}/` — NOT in DB blob. Files use index prefix for ordering, `.meta` sidecar for MIME type
- `AccountManager.queueSend()` — throws on failure. Persists to GRDB + disk, fires `drainOutbox()` async. ComposeView only dismisses on success; shows error if persistence fails
- `AccountManager.drainOutbox()` — only drains `.queued` messages (not `.failed`). Mirrors `drainPendingQueue()` pattern: isDrainingOutbox guard, NetworkMonitor gate, FIFO by createdAt. All DB writes use `do/catch` with 3 retries (never `try?`)
- **Message-ID pre-generation**: Before SMTP send, a stable RFC822 Message-ID is generated (`sentMessageId` column) and injected into the draft. Both SMTP send and IMAP Sent append use the same ID. SwiftMail fork's `constructContent` respects pre-set `Message-Id` in `additionalHeaders`.
- **Send success path**: provider.send() → stamp `sentAt` → IMAP APPEND to Sent folder (dedup by Message-ID SEARCH) → stamp `appendedToSent` → delete from DB → delete attachments from disk. Gmail/Exchange auto-save to Sent (no-op append). IMAP requires explicit APPEND.
- **Persistent Sent append**: If IMAP APPEND fails, outbox message stays with `sentAt != nil`, `appendedToSent == false`. `drainPendingSentAppends()` retries on next drain. Message only finalized when both send AND append succeed.
- **Send failure path**: retryCount < 3 keeps as `queued` (auto-retry next drain). retryCount >= 3 marks `failed` (user must tap Retry, which resets retryCount to 0)
- **Crash recovery**: `reconcileOutbox()` — sentAt!=nil + appendedToSent → delete (fully done). sentAt!=nil + !appendedToSent → keep for append retry. sentAt==nil + status=sending → reset to queued. Also cleans orphaned attachment dirs
- Drain triggers: NetworkMonitor reconnect, app launch (reconcileOutbox), after queueSend, SyncScheduler foreground polling + after each poll
- `toDraftMessage()` and `loadAttachments()` throw — prevents sending email with missing/corrupted attachments
- Discard refused for `sending` messages (UI hides button + backend guard). Discard uses atomic single-write-transaction fetch+delete
- UI: `NavigationStore.outboxMessages` via GRDB `ValueObservation`. `OutboxView` with retry (swipe left, only for failed) and discard (swipe right + confirmation, hidden for sending). Sidebar shows "Outbox" in unified section + per-account sections with count badge (red if failures)
- v3 migration adds the table; v4 adds `sentAt` column; v18 adds `sentMessageId` + `appendedToSent` columns. FK on accountId→account with CASCADE
- **Core philosophy**: never drop a message, never `try?` on state transitions, `sentAt` before delete, prefer double-send over drop, no auto-discard ever. See CLAUDE.md "Outbox Reliability Rules"

---

### Proactive Local Notifications (ADR-IOS-026)
- **Port of TB's `proactiveCheckin.js`** — two deterministic triggers, no LLM calls
- `ProactiveNotifyService` actor — singleton, called from `AccountManagerAI` (after AI drain) and `RootView` (foreground return)
- **Trigger 1 (`new_reminder`)**: after message processing, filters for reply-tagged reminders within window, debounced 1s, fires immediately via `UNNotificationRequest(trigger: nil)`
- **Trigger 2 (`due_approaching`)**: `UNCalendarNotificationTrigger` scheduled N minutes before due. Reschedules on every reminder list change. Works even when app is killed.
- **`ReachedOutStore`** — UserDefaults dedup keyed by `"{reminderHash}:{triggerType}"`. Prune splits on LAST colon (hashes contain colons like `m:msgId`).
- **`NotificationDelegate`** — separate class from `AppDelegate` (Swift 6 `@MainActor` isolation). Uses `@preconcurrency UNUserNotificationCenterDelegate`. Returns `[.banner, .sound, .list]` for foreground display.
- **Foreground return**: syncs `deliveredNotifications()` → `ReachedOutStore` (covers calendar triggers that fired while app was killed), then checks for overdue reminders
- **Rate limiting**: 60s minimum between immediate notifications
- **Settings**: toggle (`proactive.notify.enabled`), window days (default 7), advance minutes (default 30) — in TabMailSettingsView "Notifications" section. **Default ON.**
- **Default-ON migration**: TabMailApp init runs a one-shot migration keyed `didMigrateProactiveNotifyOnByDefault_v1` that writes `true` to the enabled key for all existing users. Reason: an earlier `.onReceive(UserDefaults.didChangeNotification)` handler in TabMailSettingsView used `bool(forKey:)` which returns false for missing keys, silently flipping the toggle off on the next UserDefaults change. Fix: that handler now treats missing key as `true` to match the AppStorage default.
- **Reminders panel warning**: `RemindersNotificationWarning` (in `ReminderTopCard.swift`) renders above the reminder cards in the chat pill when either the in-app toggle is off OR `UNUserNotificationCenter` authorization is `denied`/`notDetermined`. Uses `scenePhase` to refresh on foreground.
- **Deep link**: `.proactiveNotificationTapped` posted on tap — observer not yet wired (follow-up task)
- **Debug**: `#if DEBUG` test notification button in settings (fires in 5s)

---

### App Icon Badge Routine (NSE counter + main-app recount)

- **Two mechanisms**: (1) NSE best-effort incremental counter in App Group key `nse.unreadBadge` (`NSEBadge.badgeCountKey`), attached as an *absolute* value via `c.badge` on each delivered push; (2) main app's `UnreadCountManager.updateBadge()` — authoritative `SUM(folder.unreadCount) WHERE role=inbox`, calls `setBadgeCount` and **overwrites the mirror**. The counter only has to stay reasonable between main-app wakes; every recount self-heals drift.
- **All per-delivery badge values MUST go through `NSEBadge.badgeForDelivery`** (`Shared/Persistence/NSEBadge.swift`, compiled into app + NSE) — never raw increments. Gates:
  - **Dedup key = `NSEBadge.countedId` (rfc822-preferred), NOT the provider `messageId`.** The composite is `"\(accountId):rfc:\(normalizedRfc822)"` when an rfc822 Message-ID exists, else `"\(accountId):\(messageId)"`. **Why this matters (fixed 2026-06-15):** the NSE keys IMAP off the RFC 5322 Message-ID carried in the push payload, while main-app IMAP sync may store the *UID* as `messageId` (see the `WHERE accountId=? AND messageId=?` lookup in `mergeNSEStagingData`). A `messageId`-only key therefore silently NEVER matched for IMAP/iCloud — so the main-app-overlap dedup below would no-op for those accounts. rfc822 matches across both processes for all providers. All 3 NSE `badgeForDelivery` call sites pass `msg.rfc822MessageId`. Normalize with `EmailFilter.normalizeMessageId` on both sides.
  - **Gate 1 (duplicate pushes)**: per-`countedId` idempotency arbitrated by atomic `INSERT OR IGNORE` into `nse_badge_counted` in the shared staging DB. Deliberately a separate table (NOT a column on `nse_processed_message` — those rows get `INSERT OR REPLACE`d by re-runs and deleted by the merge). Created lazily by `NSEBadge`; 7-day retention pruned opportunistically.
  - **Gate 2 (main-app overlap, AI lease)**: when the main app holds a *fresh* `AIOwnershipLease` on the message (looked up by provider `messageId` — the lease key is unchanged), the NSE delivers the current counter without bumping. Narrow — only catches the case where AI is actively processing that exact message.
  - **Main-app authoritative recording (the real overlap fix, 2026-06-15)**: `UnreadCountManager.updateBadge()` → `recordRecentUnreadForNSE()` writes the recent unread inbox messages' `countedId`s into `nse_badge_counted` via `NSEBadge.markCounted`. So a message the awake main app already counted → row exists → NSE's Gate 1 doesn't bump → **no "+2 for one email"**, regardless of AI-lease state. Order-independent (whoever counts first wins the `INSERT OR IGNORE`). Bounded to `SyncConfig.nseBadgeDedupRecentLimit` (200) most-recent-by-date — only recently-arrived mail can race a concurrent NSE delivery. This generalizes Gate 2 WITHOUT a liveness heartbeat (which would risk a persistent under-count if the app backgrounds right after its heartbeat). **`recordRecentUnreadForNSE` is gated on `UIApplication.shared.applicationState == .active`** (ADR-IOS-041, fixed 2026-06-15): the overlap only happens in foreground, and opening a fresh `openStagingDB()` connection + writing from the background-detached badge Task while suspended aborts (or worse — a fresh connection that misses the suspend notification writes-while-suspended → 0xdead10cc). See the "GRDB database-suspension abort handling" note below.
  - **`db == nil` fallback**: no staging DB → dedup via an App Group map (`nse.badgeCountedIds`, `[countedId: epoch]`, retention-pruned) so a duplicate delivery still doesn't double-bump (previously this path unconditionally incremented).
- `NSEState.incrementBadgeCount`/`decrementBadgeCount` remain ONLY for the legacy Gmail history.list delta-adjust path (payload has no `messageId`). `markedUnreadCount` now **excludes `addedSet`** (`GmailNSEClient.fetchNewMessageIds`, fixed 2026-06-15): a brand-new message is badge-counted on delivery (head via `badgeForDelivery`) or by main-app sync (tail) — counting it again as a `labelsAdded(UNREAD)` delta double-bumped. The delta path now only moves the badge for messages ALREADY in the inbox that got re-marked unread on another client.
- **`updateBadge()` only SUMs `folder.unreadCount`; it does NOT recompute it.** So any path that inserts/deletes inbox `messageHeader` rows must FIRST update `folder.unreadCount` (optimistic delta in the same tx, like `markRead`) OR call `UnreadCountManager.requestRecount(folderIds:)` (authoritative COUNT recompute) — only THEN is `updateBadge` correct. Every sync path does this (`SyncEngineFullSync` requestRecount per folder after commit; delta `requestUnreadRecount`; backfill; actions). **Badge-desync bug fixed 2026-06-15:** `NSEDataBridge.mergeNSEStagingData` inserted NSE-delivered headers + did sleep-time removals but called `updateBadge()` (sum-only), so the badge/sidebar undercounted new mail until the next full sync (worse once "fast read" made the inbox UI update instantly, exposing the lag). Fix: `mergeNSEStagingData` calls `UnreadCountManager.recountInboxFolders()` (blanket authoritative recompute of ALL inbox-role folders) whenever the merge mutated. No per-message folder tracking — the merge only ever touches inbox folders (new arrivals are `isInInbox`; removals delete from inbox folders; the existing-header branch only updates AI fields, never `isRead`), so recounting all inbox folders is sufficient and self-healing. Sync paths still call `requestRecount(folderIds:)` with their specific changed folders (the thin `SyncEngine.requestUnreadRecount` wrapper was removed as redundant — call `UnreadCountManager.shared.requestRecount` directly).
- **Foreground is safe by construction**: `NotificationDelegate.willPresent` returns `[]` for email pushes (badge never applied) and triggers merge + sync recount.
- Push-worker sets **no `apns-collapse-id`** — a true APNs duplicate shows two banners; badge dedup is entirely client-side via Gate 1.
- Tests: `TabMailTests/NSE/NSEBadgeTests.swift`.

### Sync-status subtitle ("Updated N min ago")

- Rendered by `SyncStatusFormatter.statusText` (`SyncStatusSubtitle.swift`) from `AccountManagerState.shared.lastSyncCompletedAt` + `lastSyncFailed` + a `now` ticked by a 60s timer in `SyncStatusObservationModifier` (attached once at RootView, fanned out via Environment).
- **THREE tiers write `lastSyncCompletedAt`, all on success only**: (1) pull-to-refresh / on-appear debounced sync — `InboxViewModel.performSync` (`:1226`); (2) foreground poll tier — `SyncScheduler.poll()` (`:676`); (3) **background/push/startup (NSE-coalesce) tier — `SyncScheduler.backgroundPoll`** (added 2026-06-15; the no-network early-return is correctly skipped). Before (3), a successful silent-push/NSE wake left the subtitle stale ("Updated 48 min ago" after a coalesce) because that tier never stamped — that was the bug, not a freeze.
- On a *failed* sync only `lastSyncFailed=true` is set (timestamp preserved, prefix flips to "Last updated …") — by design (ADR-003, no fallbacks). A genuinely failing pull-to-refresh therefore won't reset the subtitle.
- `InboxViewModel.performSync` clears `isRefreshing` in a `defer` (2026-06-15) — a cancelled/torn-down sync used to be able to leave it stuck `true`, which makes `refreshSync()` silently no-op (wait-then-return) for ALL future pull-to-refreshes → a stuck-stale subtitle with no way to refresh.
- Latent fragility (not the active bug): the `minuteTimer` is recreated whenever RootView's body re-runs (classic `Timer.publish().autoconnect()`-in-modifier reset); a *successful* sync still resets the text via the observation `.task`, so it manifests at most as a non-advancing minute counter, not a failure-to-reset.

---

### Stuck-`isLoading` rule: async view loads must defer-clear their spinner flag — and `.task` must NOT hang on the conditional it flips

- **`.task`-on-conditional self-cancellation (Account page infinite spinner, root-caused on-device 2026-07-08):** attaching `.task { await load() }` (or `.onAppear`) directly to a `@ViewBuilder` conditional (`if isLoading { spinner } else if errorMessage { … } else { content }`) is a footgun: every branch flip fires disappear/appear on the conditional, cancelling and RESTARTING the plain `.task` — and the load's own state writes flip the branch (`isLoading = true` on entry, `isLoading = false`/`errorMessage` on exit). Observed as a ~100Hz self-sustaining cancel/restart storm (`URLError.cancelled` instantly, `cancelled=true`, view identity STABLE — same `[Dashboard] id=`, appear+disappear each cycle). With the old happy-path-only clear it instead deadlocked silently: first success flipped the branch → task restarted → cancelled itself → `isLoading` stranded true → permanent spinner, zero logs. **Fix + rule:** hang `.task`/lifecycle modifiers on a stable wrapper (`ZStack { content }`) that survives branch flips; see `AccountDashboardView.body`. Full narrative in the ADR-IOS-054 amendment.
- **The defer half of the bug class (bitten twice now):** a `.task`-driven load that clears its spinner flag only on the happy path (`if !Task.isCancelled { isLoading = false }`, or no clear at all on early returns) strands the view on a forever-spinner when the task is cancelled — and cancellation paths are typically the SILENT ones (no logs). Instances: `InboxViewModel.performSync`/`isRefreshing` (fixed 2026-06-15, above) and `AccountDashboardView.loadData`/`isLoading` (fixed 2026-07-08).
- **The rule:** clear the flag in a `defer`, decide what to SHOW based on what data actually arrived (e.g. set an `errorMessage` + Retry when a cancelled load produced nothing), and give every cancellation catch a debug-gated trace so the silent path is never silent in debug builds.
- **Codebase sweep (2026-07-08, all 45 `.task` sites):** 4 true positives fixed with the same stable-anchor treatment — `RootView.body` (all four `.task(id:)`s hung on the unwrapped `routedContent` conditional; two write its branch vars `isCheckingConsent`/`isStartupComplete`), `TemplateMarketplaceView`, `MySharedTemplatesView` (both had `.task` on a `Group`-wrapped `isLoading` conditional). **`Group` does NOT stabilize — it's a transparent pass-through; `ZStack`/`VStack`/`List`/`Form`/`NavigationSplitView` do.** Remaining borderline sites (one-directional or terminal flips, no oscillation risk, left as-is): `TabMailApp.swift:337` splash, `MailNavigationView` ServerDraftComposeLoader, `ChatHistoryView`, `ReminderTopCard`, `SummaryBubbleView` timeout failsafe, `PlanPickerView` products-empty arm, `RootView:105` demo seeding (terminal exit-on-error).
- **Extra hardening on AccountDashboardView only:** the collapsed-split-view navigation transition fires ONE spurious disappear on a freshly-appeared content-column page (onAppear → onDisappear, view still visible, no re-appear) — enough to cancel even a stably-anchored structured `.task` mid-fetch once. Its initial load therefore runs on an UNSTRUCTURED `Task` with a `loadInFlight` dedup guard. Apply the same treatment to other content-column pages only if their logs ever show the one-shot `CANCELLED (URLError.cancelled)` signature.
- `AccountDashboardView` diagnostics: `[Dashboard] loadData START/exit id=… cancelled=… hasInfo=…`, `[Dashboard] onAppear/onDisappear id=…` (per-identity instance id distinguishes remount loops from task restarts), `[Dashboard] fetch… CANCELLED` (all debug-gated); navigation traces `[NavAccount] navigateToAccount → push` and `[NavAccount] selection changed old → new` in `MailNavigationView`.
- Reminder: `InboxView.body`'s modifier chain is AT the Swift type-checker budget — even ONE more `.onReceive` or `.modifier()` entry fails the build with "unable to type-check this expression in reasonable time." New notification listeners must merge into an existing extracted `ViewModifier` (see `CollapseChatOnNavigateModifier` — template-pill + navigate-to-account merged into one publisher — or `StagedRowsReceiver`) rather than adding chain entries.

---

---

### Cron Reminders (ScheduledItem Architecture)

- **Crons are a subclass of reminders** in the architecture. Both flow through the same unified builder (`ReminderBuilder` / `reminderBuilder.js`), the same disable/enable store (`DisabledRemindersStore` with `c:` hash prefix for crons), and the same Device Sync fields.
- **`[Cron]` KB format**: `[Cron] Schedule <days> <HH:MM> [<timezone>], <instruction>` — stored in KB text, synced via Device Sync KB field, programmatically protected from LLM rewriting on the backend (`splitKbEntries` + `isProtectedEntry`)
- **`generateKBReminders()`** (TB only) handles the KB re-parse trigger for BOTH `[Reminder]` and `[Cron]` entries. TB cron tools call it after KB changes. On iOS, the re-parse cascade is automatic: `PromptStore.shared.rawKB` setter → Device Sync broadcast → `ChatPillState` observation → `ReminderBuilder` re-evaluates. No explicit `generateKBReminders()` call needed on iOS.
- **`KBCronParser`** (iOS) / `kbCronParser.js` (TB) — separate parsers for `[Cron]` entries. Returns raw `scheduleDays` string (not expanded array) — the scheduler resolves it.
- **`CronScheduler`** — evaluates `shouldFire()` and `detectMiss()` per cron. TB fires at T-5min, iOS at T-3min (staggered for LLM cost optimization). Both devices always notify independently.
- **`CronExecutionCache`** — stores LLM results keyed by `{cronHash}_{YYYY-MM-DD}`. Synced via Device Sync `cronCache` field. Occurrence-based eviction (last 10 per cron). iOS reuses TB's cached result when available (skips LLM call) but still delivers its own notification.
- **Execution state is device-local** — `lastFired`, `lastMissed`, `consecutiveErrors` are NOT synced. Both devices evaluate and notify independently.
- **Tools**: `cron_add` / `cron_del` (separate from `reminder_add` / `reminder_del`). `change_setting` extended with `cron.enabled` and `cron.advance_minutes`.
- **Push worker**: alarm registration with absolute UTC wake times. Client computes locally with timezone awareness. Server is a dumb alarm clock.

---

## Banner Flash Prevention (MailNavigationView)

Two notification banners (Subscription / NSE consent) must stay hidden on boot until their backing state is confirmed — otherwise default-closed state flashes a false-positive banner before async checks complete. **Pattern:** use a `hasCheckedOnce`-style gate (persisted for subscription, per-process for consent) and only render the banner once an authoritative response is in.

- **Subscription banner** — `AISubscriptionGate` hydrates `isActive` + `hasCheckedOnce` from UserDefaults (keys `ai_subscription_last_known_active` / `ai_subscription_has_checked_once`). Both are stamped on every `openGate`/`closeGate`. MailNavigationView mirrors `hasCheckedOnce` via `@State` + `.onChange`. Banner condition: `!hasTabMailSession || (hasCheckedSubscription && !hasActiveSubscription)`. First-ever launch: banner hidden until whoami responds. Returning user: last-known state surfaces immediately (no flash).
- **NSE consent banner** — `PushNotificationService.checkPushConsentStatusForForeground` (renamed from `checkGmailConsentStatusForForeground` since the scan covers Gmail + Outlook) tracks `hasSucceededConsentScanOnce` (per-process). On first scan, thrown errors → `nil` (unknown, not broken) — cold-launch timeouts no longer populate the banner. Sticky-on-error resumes only after the first authoritative probe. Per-account timeout raised from 3s → `PushConfig.consentStatusCheckTimeoutSeconds = 10s` since cold-launch RTT regularly exceeded 3s. If zero probes are authoritative, the notification post is suppressed entirely (banner state untouched). View gate: `hasCompletedFirstConsentScan` flips true only when a definite post arrives or when `PendingConsentErrorStore.consume()` returns cold-start tap data. **Offline gates (two layers):** (1) `checkPushConsentStatusForForeground` returns early when `NetworkMonitor.checkConnected() == false` — every probe would throw while offline, which either leaves the banner empty (pre-priming) or pins a stale banner (post-priming) that misattributes "no internet" as "consent broken". (2) `MailNavigationView` banner render gates on `NetworkMonitor.shared.isConnected` too, so a banner raised while online disappears when connectivity drops (the tap launches OAuth which can't complete offline — showing the CTA is misleading). Next foreground / reconnect re-runs the scan and the `@Observable` `isConnected` re-renders the view.
- **Naming rule**: the push-consent route is provider-agnostic. State/notification names covering both Gmail + Outlook use `push` prefix (`pushConsentErrorsDetected`, `pushConsentErrorEmails`, `isFixingPushConsent`, `checkPushConsentStatusForForeground`). Per-provider flows keep `gmail`/`outlook` prefix (e.g., `.gmailPushConsentExplainerNeeded` + `.outlookPushConsentExplainerNeeded`, `GmailPushConsentExplainerAlert`).
- **Non-blocking**: all 5 callers of `checkPushConsentStatusForForeground` wrap in `.task` / `Task {}`. Never awaited inline with sync work.

---

## Test Isolation for `UserDefaults`-Backed State

`UserDefaults.standard` is a process-wide singleton. Using it directly in tests creates cross-suite races — Swift Testing runs different `@Suite`s in parallel, and even `.serialized` only orders tests *within* one suite. The pattern adopted for `DisabledRemindersStore` + `ReminderBuilder.autoDisableOverdueOnFirstLaunch`:

- Production code reads from an overridable static: `DisabledRemindersStore.defaults` (falls back to `.standard`). A `Mutex<DefaultsRef?>` (with a `@unchecked Sendable` class wrapper around the thread-safe `UserDefaults`) holds the override so it's Swift 6-safe.
- `DisabledRemindersStore._setTestDefaults(_:)` swaps in a per-test `UserDefaults(suiteName: UUID().uuidString)`.
- Entry points that read *ambient* keys on their own (`firstLaunchDate`, `didAutoDisableOverdueReminders`) take `defaults: UserDefaults = .standard` as a default parameter so tests can pass the isolated instance through.
- Tests wrap the body in a `withIsolatedDefaults { defaults in ... }` helper that sets up, runs, and tears down (including `removePersistentDomain`) the per-test `UserDefaults`. This lets the suite drop `.serialized` entirely — tests run in parallel.

Apply this same pattern to any future `UserDefaults`-backed service before introducing tests for it.

---

## Delivered-Notification Cleanup

`NotificationCleanupService` (`Shared/Notifications/`) sweeps stale delivered notifications. Wired at four entry points: NSE `didReceive`, silent-push `application(_:didReceiveRemoteNotification:)`, BGAppRefresh `SyncScheduler.handleBackgroundSync`, and app foreground (`scenePhase == .active` in `TabMailApp`). Policy:

- **Active** (interruption-level `.active`) — TTL 24h. Survives FG.
- **Passive** (interruption-level `.passive`) — TTL 24h, OR all cleared on FG.
- **`consent_error`** — never auto-cleared. Re-auth is user-actionable; user must tap or dismiss.
- **`imap_reconnect`** failure / in-progress — kept until `PushHealthStore.lastNonReconnectPushAt[accountEmail]` is newer than the notification's delivery date (push proven restored, per-account). Two stamping sources: (a) **strong** — NSE + silent-push handlers stamp on receipt of any non-error push (`provider not in {imap_reconnect, consent_error}`); (b) **weaker** — NSE stamps on successful silent re-subscribe (2xx from `/subscribe-imap` inside `attemptSilentResubscribe`). The weaker proof means: push-worker accepted the enrollment, but the IDLE socket may still drop afterwards — push-worker's retry ladder is the safety net.
- **`imap_reconnect`** success-ack ("Restored push notification connection") — distinguished by NSE-stamped `userInfo["reconnect_state"] = "ok"` at `NotificationService.swift:631-641`. Treated as normal active (1h TTL).

`PushHealthStore` lives in shared App Group UserDefaults (`group.ai.tabmail`, same suite as `AIService.optOutStore`) so NSE + main app share the timestamp map.

The bucketing logic is the pure function `NotificationCleanupService.identifiersToRemove(_:now:passiveAllAges:lastNonReconnectPushAt:)` — fully covered by `TabMailTests/Notifications/NotificationCleanupServiceTests.swift` (20 tests, 100% on `identifiersToRemove` + `shouldExclude`). Side-effect wrappers (`sweep`, `snapshot(from:)`, `PushHealthStore`) are validated manually.

### State-based clear: `InboxNotificationObserver`

Complementary axis: when a row leaves the inbox (`isInInbox` flips `true → false`, OR a previously-inbox row is deleted), `InboxNotificationObserver` clears `email-{accountId}-{messageId}` immediately — no waiting on TTL. Covers BOTH local actions (`AccountManagerActions.move`/`archive`/`delete` via `optimisticMoveToFolder`) AND remote sync paths (`SyncEngineDeltaSync`, `SyncEngineMaintenance`, `SyncEngineSelfHeal`, full sync) without per-site discipline — single GRDB `TransactionObserver` on `messageHeader`.

Performance: per-commit cost is one rowid B-tree `SELECT … WHERE rowid IN (touched rowids)` (sub-millisecond, scoped to the write batch). Startup populate via `WHERE isInInbox = 1` once during `AppDatabase.init`, served by index `messageHeader_isInInbox_date`. Callbacks fire on the GRDB writer queue (background, never main); in-memory `[rowid: (acct, mid)]` map is touched only by callbacks → no Mutex needed.

GRDB holds observers **weakly** at `.observerLifetime` — `AppDatabase.inboxNotificationObserver` provides the strong ref. Tests bypassing `AppDatabase` MUST capture the observer too (see helper in `InboxNotificationObserverTests`).

`clear` is invoked synchronously on the writer queue (`UNUserNotificationCenter.removeDeliveredNotifications` is non-blocking IPC). Contract: clients injecting `clear` MUST NOT re-enter GRDB writes.

---

## Startup Data Migrations (`StartupMigrations.swift`)

NOT GRDB schema migrations (those are in `AppDatabase.runMigrations`, the `DatabaseMigrator` v1…vN). `StartupMigrations.run(_ writer:resetFTS:)` is a separate set of **one-time destructive cached-mail resets**, each gated by its own `UserDefaults` bool (`didMigrateHeaderIds_v2`, `didClearBodiesForAttachmentEncoding_v1`, `didResetImapDatesForInternalDate_v1`, `didCleanResetMessageData_v1`). They `DELETE FROM messageHeader/messageBody` + reset backfill cursors + (clean reset) delete the FTS dir, to recover from cache-format changes across app upgrades; safe for real accounts because the server re-syncs. Kept `UserDefaults`-gated (not folded into the GRDB migrator) specifically so existing users who already ran them aren't re-wiped.

- **Where called (2026-06-04):** **synchronously in `AppDatabase.init()`**, right after `runMigrations` and BEFORE the pool is exposed (`AppDatabase.shared` set in `TabMailApp.init`) or the inbox observer is wired. So: DB opens → schema migrates → data resets → only THEN can sync / NSE merge / demo+screenshot seed touch the DB. **NOT** run in the test `init(dbPool:)` (would hit global flags + the real FTS dir). The only production caller of `AppDatabase()` is `TabMailApp.swift`.
- **No async gate anymore:** because nothing can race the resets, the old `SyncScheduler.migrationsComplete` / `awaitMigrations()` / `signalMigrationsComplete()` latch and RootView's `async let migrations` were **deleted**. `DemoModeService.completeSetup` no longer runs migrations either.
- **FTS reset = delete the dir.** At DB-open SearchIndex hasn't initialized (no open pool), so the clean reset just `removeItem`s `Application Support/tabmail_fts`; `SearchIndex.initialize()` recreates it fresh. `resetFTS` is injected into `run(...)` so tests don't touch the real FS; it's invoked in lockstep with the main-DB deletes so the flag is set only once both halves are done (crash → re-run next launch).
- **Cost note:** the first launch after a new reset is introduced runs its DELETE synchronously at launch (blocks first frame briefly, one-time). Correctness (don't show-then-wipe) was chosen over that one-time delay.

---

## TextEditor/TextField Caret Jump — Never Save Per Keystroke (2026-07-02)

- **A `TextEditor` (or `TextField`) bound via `Binding(get:set:)` whose setter writes to the DB or mutates `@Observable` store state (e.g. `navigationStore.accounts[idx] = account`) resets the caret to the end** — the mutation invalidates the view mid-edit while the editor is first responder. Same effect from notification-driven reloads (`reloadAccount()`/`reloadFolders()` on `.unreadCountsDidChange`/`.backgroundDataDidChange`) mutating `@State` while the user types. Symptom: first character inserts fine, then the cursor jumps to the end.
- **Pattern:** bind the editor to a plain local `@State` string; commit (diff-guarded DB write + store propagation) on focus loss (`@FocusState` + `onChange`), `.onDisappear`, and `scenePhase != .active` — never per keystroke. Reference: signature editor in `AccountDetailView.swift` (`commitSignature()`). Bonus: removes the synchronous main-thread `dbPool.write` per keystroke (which can stall behind the `DatabaseWriteQueue` sync backlog).
- The displayName/email/imapUsername `TextField`s in the same view still save per keystroke — single-line trailing-aligned fields where the jump hasn't been reported; migrate them to the same pattern if it ever is.

---

## Migration Splash / iOS Indeterminate Linear ProgressView Pitfall (2026-07-01)

- **`ProgressView().progressViewStyle(.linear)` with no value renders a STATIC empty track on iOS** — UIKit has no indeterminate `UIProgressView`, so only the circular style animates when value-less. A value-less linear bar looks frozen (exactly the "frozen launch" impression the migration splash exists to avoid). `SplashView.IndeterminateActivityBar` (a `withAnimation(.repeatForever)` sweeping capsule) is the in-repo replacement — reuse it if another indeterminate linear bar is ever needed.
- **No hard `\n` inside wrapping `Text`** — a forced break combined with natural wrapping (narrow devices / large Dynamic Type) produces unbalanced orphan lines. Let sentences flow in one string with `.multilineTextAlignment(.center)` + `.fixedSize(horizontal: false, vertical: true)`.

---

## Zero (BYOK) Plan — IAP Surface (ADR-IOS-040)

- Products `ai.tabmail.byok.monthly`/`.yearly` — tier string "BYOK" everywhere internal; **only display renders "Zero"** via `StoreKitManager.displayPlanName(forTier:)`. Ranks: Unknown=0/BYOK=1/Basic=2/Pro=3 (`tierRank(for:)`/`tierRank(forTier:)`).
- `checkTrialEligibility()` must read a product **with** an intro offer (Zero has none and sorts first). PlanCard trial badge = group-eligible AND `product.subscription?.introductoryOffer != nil`.
- `Configuration.storekit` group levels follow Apple's convention (1 = highest): Pro=1, Basic=2, BYOK=3 (was inverted Basic=1/Pro=2 before 2026-06-10). Local testing only; ASC group order must match (Pro → Basic → Zero). Sandbox-verify upgrade/downgrade direction before release.
- **No scheme sets `storeKitConfiguration`** (verified 2026-06-10): the simulator loads REAL ASC sandbox product metadata (live prices/names) with no local config — plan-picker screenshots 12-15 show actual store prices. Don't pin Configuration.storekit to a scheme; it would swap in placeholder prices. The file is for manual Xcode StoreKit-testing only.
- Dashboard quota card shows **N/A** for `planTier == "BYOK"` (no priority budget); daily-quota chart already hidden via `maxMonthlyCostCents > 0`.

---

## BYOK Model Picker — Two-Section Query-All (ADR-026, 2026-07-08)

- The backend catalog (`GET /byok/models`) is now the **"Recommended" list only** — the backend accepts any well-formed model id on the user's own key (global ADR-026). `BYOKTierRows` (Views/Settings/ProviderSettingsView.swift) renders the model Picker with two Sections: `"Recommended"` (catalog order; first entry = default, newest tier-appropriate family first — Haiku for Light, Sonnet for Heavy; Opus listed but never default) and `"All models (from your API key)"` (live `POST /byok/list-models`, fetched only when a Keychain key exists, cached per provider for the view's lifetime, failure = debug-gated log + Recommended-only).
- Shared pure helpers on `BYOKProviderInfo`: `liveIdMatches(_:recommendedId:)` (exact or `<id>-<digits>` dated-variant match; digits-only guard prevents `gpt-5.5` matching `gpt-5.5-pro`), `isAvailable(_:in:)` (hoisted from APIKeysView's ProviderTestRunner — both call sites now share it), `mergeModels(recommended:available:)` → `(recommended, additional)` with dedupe. Tests: `BYOKModelMergeTests` suite in `TabMailTests/Services/BYOKProviderInfoTests.swift` (placeholder ids only — never assert real catalog contents).
- Group labels must stay **byte-identical to the TB addon's** (`config/modules/byokSettings.js` optgroups): "Recommended" / "All models (from your API key)".
- A stored model id that's live-only (or no longer in the catalog) stays selected — `ensureDefaultModel()` only auto-picks when the stored value is empty; do not "clean up" unknown stored ids, the backend accepts them.

---

## HTML-Document Guard for text/plain Extraction (2026-06-11)

- Some senders put the **full HTML document inside the text/plain MIME part** (observed: survey-platform mail, `multipart/alternative` with identical HTML in both parts). iOS was immune for snippets/FTS only because `EmailFilter.extractPlainText` prefers `htmlBody`; the single-part-mislabeled variant was not covered.
- `EmailFilter.looksLikeHTMLDocument(_:)` (Shared/Parse/EmailFilter.swift) now guards the textBody fallback in `extractPlainText` and the inlined Tier-2 snippet fallback (`InboxViewModel.loadSnippetBatch`). Document-start markers ONLY (`<!DOCTYPE` / `<html` + ws/`>`): never matches prose with angle brackets (`<user@example.com>`, `List<String>`, `a < b`). Do NOT broaden into a generic tag heuristic — `BodyRenderer`'s display path deliberately avoids that (double-escape saga); the display path is unchanged.
- TB addon counterpart: `fts/bodyExtract.js looksLikeHtmlDocument` + HTML-first flip — see `tabmail-thunderbird/DECISIONS.md` ADR-018. Keep the two markers in sync.
- Tests: `TabMailTests/Helpers/EmailFilterHTMLTests.swift` (suites `EmailFilter.looksLikeHTMLDocument`, `EmailFilter.extractPlainText HTML-document guard`).

---

## GRDB Database Suspension — 0xdead10cc Defense (ADR-IOS-041, 2026-06-12)

- TestFlight 1.6.3/1.6.5 crashed with `RUNNINGBOARD 0xdead10cc` (suspended holding SQLite locks: `BodyAssetMaintenance.pruneOrphans`, `refreshAICacheTTLAndPurge`, `selfHealFTSBodyMembership`). Fix = GRDB suspension, mechanically enforced at the DB layer.
- **Rule: every main-app GRDB `Configuration` MUST set `observesSuspensionNotifications = true`.** Currently 6 sites: `AppDatabase` (×2 incl. NSE staging schema setup), `SearchIndex`, `MemoryIndex`, `BodyAssetStore.manifestQueue`, `NSEDataBridge` (×2). Inert in the NSE process (nothing posts the notification there).
- **Rule: every background execution entry point MUST bracket with `DatabaseSuspension.shared.beginBackgroundWork`/`endBackgroundWork`** (resumes DBs, re-arms the quiesce window on exit). Covered today: silent push (`AppDelegate.didReceiveRemoteNotification`), BGAppRefresh/BGProcessing task bodies (`SyncScheduler`), background notification actions (MARK_READ/ARCHIVE/DELETE — they run WITHOUT foregrounding), foreground return. A new BGTask/push/action handler that skips this gets `SQLITE_ABORT` on every write.
- Suspend is posted from the `db-quiesce` assertion's **expiration handler** (app-wide deadline, same instant as `backfill-grace`/`ai-job-*` expirations) — never at `didEnterBackground` — so in-flight work keeps its full grace window. BGTask expiration handlers also call `DatabaseSuspension.postSuspendImmediately` as the wind-down backstop.
- While suspended: WAL reads keep working (GRDB checks actual `PRAGMA journal_mode` — SearchIndex/MemoryIndex set WAL via pragma, still exempt); writes throw `DatabaseError` `SQLITE_ABORT`/`SQLITE_INTERRUPT` (treat as retryable-later, never permanent). Non-WAL queues (BodyAssetStore manifest, NSE staging) abort reads too.
- GRDB suspension API is 🔥 EXPERIMENTAL (pinned v7.10.0) — `DatabaseSuspensionTests` pins the semantics; re-check on GRDB upgrades.
- Same change hardened the outbox `sentAt` stamp (double-send firewall) from single-attempt to `retryWrite` 3 attempts (Outbox rule 2).
- **Rule (added 2026-06-15): a suspended-write abort is BENIGN — never log it as a failure.** A DETACHED/debounced write can run AFTER its `beginBackgroundWork` window closes (e.g. the `UnreadCountManager` recount debounce Task with its cooldown sleep) → DBs re-suspended → `SQLITE_ABORT` "Database is suspended". Was the user-visible 1.6.8 build-302 bug. Catch handlers on background-reachable DB writes MUST filter: `if !error.isDatabaseSuspensionAbort { log(...) }`. Helper `Error.isDatabaseSuspensionAbort` (`(self as? DatabaseError)?.isInterruptionError ?? false`) in `DatabaseSuspension.swift`. Applied at all 13 such sites (UnreadCountManager ×2, BackfillAIQueue ×4, BackfillBodyQueue ×1, MemoryIndex ×4, NSEDataBridge ×2). Foreground-only writes (MemoryIndex `deleteAll`/`purgeForSessionPrefix` — Clear-History/demo) are NOT filtered — a real failure there should always log.
- **Rule (added 2026-06-15): never open a FRESH GRDB connection + write from a possibly-suspended detached context.** A connection opened AFTER the process-wide suspend notification can miss it (`isSuspended` starts false) → writes while truly suspended → 0xdead10cc. `UnreadCountManager.recordRecentUnreadForNSE` (opened `openStagingDB()` + `NSEBadge.markCounted` from the badge-update Task) now gates on `UIApplication.shared.applicationState == .active`. New tests in `DatabaseSuspensionTests` (the `.serialized` suite — suspend/resume are process-wide): `Error.isDatabaseSuspensionAbort` classification + `NSEBadge.markCounted` graceful no-op while suspended.

---

## Dictation leaves AVAudioSession active → haptics die process-wide (2026-06-27)

- Symptom: `UIFeedbackGenerator`/SwiftUI `.sensoryFeedback` haptics (tag-tap ticks in `MessageRowView`/`TriageCardView`/`MessageCardView`) silently stop firing mid-session; only quitting + relaunching the app brings them back. Correlates with prior **dictation** use (`SpeechRecognizer` via `DynamicIslandChatButton`/`AgentChatSheet`).
- Root cause: iOS mutes haptics and certain system sounds while an `AVAudioSession` is **active in a recording-capable category** (`allowHapticsAndSystemSoundsDuringRecording` defaults to `false`). `SpeechRecognizer.beginRecording` sets `.playAndRecord` + `setActive(true)` but the teardown only stopped the `AVAudioEngine` — it **never called `setActive(false)`**, so the session stayed active for the whole process and haptics stayed muted until relaunch. Restart resets the session → haptics return.
- Fix (2026-06-27): `SpeechRecognizer.deactivateAudioSession()` (`nonisolated`, runs off main per iOS 26 AVAudioSession requirement, `setActive(false, .notifyOthersOnDeactivation)`) is now called on **every exit path after activation**: committed-recording teardown in `stop()` (guarded by the `generation` counter so a late teardown can't deactivate a session a concurrent restart just activated), the stale-teardown path, the invalid-format early return, and the `catch`. `stop()`'s deactivation lives inside its `engine != nil` teardown; the stale/error paths (engine never committed to state) own their own deactivation.
- Did **not** use `setAllowHapticsAndSystemSoundsDuringRecording(true)` — that only masks the symptom, leaves the session leaked (other apps' audio stays interrupted, mic capability held), and could let Taptic buzz bleed into the dictation mic. Deactivating is the correct fix; haptics and dictation are on different screens so haptics-during-recording isn't needed.
- Not unit-tested (hardware; simulator doesn't reproduce haptic muting) — verify on device: dictate in chat → stop → tap a tag in inbox → haptic fires.

---

## Knowledge Gaps

(none currently)
