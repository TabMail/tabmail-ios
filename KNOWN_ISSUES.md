# TabMail iOS Known Issues

This is the executive index for released `v1.7.5` (`50e8f63a4`). The governing policy is simple: server sync/reopen/retry is valid recovery for a fail-closed edge; guessing identity, mutating the wrong item, losing local-only authored data, exposing a secret, or wedging a durable lane is not.

**Current executive census (2026-08-09):** 17 user-impacting records — Open **2** · Accepted recoverable limitation **15**. The full register contains 138 main records and 2 historical D4 records; its 121 fixed, settled-decision, non-defect, decomposed, historical, or provenance-only main records are intentionally omitted from this dashboard.

The full records are split into [`Companion/Process/Current/KnownIssues/`](Companion/Process/Current/KnownIssues/README.md). The exact pre-split register—including all superseded reasoning, corrections, audit history, predicates, and old counts—is preserved byte-for-byte in [`Companion/Process/History/KnownIssues/known-issues-pre-hierarchy-2026-08-09.txt`](Companion/Process/History/KnownIssues/known-issues-pre-hierarchy-2026-08-09.txt) (`SHA-256 513497704ad37e977e2fb86e4623e956e6f1ca99844122948ff74995dfa9a309`), with row-level integrity in [`Companion/Process/Current/KnownIssues/manifest.tsv`](Companion/Process/Current/KnownIssues/manifest.tsv).

## Executive summary

- The 2026-08-09 refresh covered all 423 production Swift files in `Shared/`, `TabMail/`, and `TabMailNotificationService/`, plus the released SwiftMail resolution and current PR boundary.
- Four differently shaped passes covered durable/crash recovery, UI/lifecycle and swallowed errors, provider/dependency boundaries, then local-only ownership/privacy/terminal-state readers. The fourth pass produced no new issue class.
- This top-level file lists only current open or accepted user-impacting limitations. All fixed and non-problem records remain available through the companion directory and hash-pinned archive.
- The audit added seven records. Five are explicitly recoverable accepted limitations; two remain open because removal/reset can silently become partial and stale remote push state can survive account removal.
- Atomic MOVE remains intentionally conservative: uncertain crash recovery may drop the gesture and let sync restore server truth (`IOS-MOVE-003`); it does not blindly replay or guess a destination identity.

## Open issues requiring attention

- [IOS-CLEANUP-001](Companion/Process/Current/KnownIssues/ios-cleanup-001.md): 🔓 OPEN (2026-08-09) — several live destructive settings paths swallow the authoritative GRDB transaction and continue with later cleanup or success presentation, so a database failure can leave a partial local reset/removal
- [IOS-PUSH-001](Companion/Process/Current/KnownIssues/ios-push-001.md): 🔓 OPEN (2026-08-09) — account removal and the latent factory-reset path treat remote push unsubscribe, consent revocation, and device unregister as best-effort with no durable retry; a stale push can still produce an old-account warning…

## Issue index

### BILLING (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-BILLING-001](Companion/Process/Current/KnownIssues/ios-billing-001.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-09) — failure to present Apple's subscription-management sheet is log-only on both account surfaces, leaving the button with no visible result |

### BODY (5)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-BODY-001](Companion/Process/Current/KnownIssues/ios-body-001.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-08) — bodies ALREADY corrupted by shipped v1.6.38 keep their wrong content; the gate protects new fetches only, and there is no remediation pass |
| [IOS-BODY-002](Companion/Process/Current/KnownIssues/ios-body-002.md) | `accepted` | 📋 ACCEPTED LIMITATION (re-baselined 2026-08-09 at released v1.7.5) — pull-to-refresh on a message whose address is mid-move is still DECLINED rather than performed; ordinary refresh no longer deletes the readable body first |
| [IOS-BODY-003](Companion/Process/Current/KnownIssues/ios-body-003.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-08) — the BATCH body path still pays one provider round trip per retry cycle while a move stays undrained |
| [IOS-BODY-004](Companion/Process/Current/KnownIssues/ios-body-004.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-08) — an open message whose row is re-keyed by finishMove WHILE the detail view is polling for its body stops loading until the user leaves and reopens it; the automatic recovery was removed rather than… |
| [IOS-BODY-005](Companion/Process/Current/KnownIssues/ios-body-005.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-08) — tapping an attachment on a message whose address is mid-move shows "This message is still being moved. Go back to the message list and open it again in a moment." instead of downloading it |

### CAL (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-CAL-007](Companion/Process/Current/KnownIssues/ios-cal-007.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-07, round-17 R17-4) — registered under THE MANTRA, not deferred and not a defect. TabMail has no persistent surface for a terminally-failed calendar operation. Delivery of the failure reason is in-memory… |

### CHAT (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-CHAT-001](Companion/Process/Current/KnownIssues/ios-chat-001.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-09) — Agent Chat deliberately keeps the live conversation usable when local chat persistence fails, so the visible session and durable history can diverge until reopen |

### CLEANUP (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-CLEANUP-001](Companion/Process/Current/KnownIssues/ios-cleanup-001.md) | `open` | 🔓 OPEN (2026-08-09) — several live destructive settings paths swallow the authoritative GRDB transaction and continue with later cleanup or success presentation, so a database failure can leave a partial local reset/removal |

### IMAP (2)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-IMAP-009](Companion/Process/Current/KnownIssues/ios-imap-009.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-05) — registered under THE MANTRA, not deferred and not a defect. The disposition has been verified and accepted by the coordinator; do not re-litigate it, and do not "fix" it locally |
| [IOS-IMAP-010](Companion/Process/Current/KnownIssues/ios-imap-010.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-05) — registered under THE MANTRA; recoverable, zero wire mutation, do not build a mechanism for it |

### MOVE (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-MOVE-003](Companion/Process/Current/KnownIssues/ios-move-003.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-09, released v1.7.5 / 9d10c65d1) — a process death can drop a MOVE that was durably claimed but had not yet emitted any provider command; sync restores server truth and the user may repeat the move |

### OUTBOX (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-OUTBOX-007](Companion/Process/Current/KnownIssues/ios-outbox-007.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-07, round-18 item E) — an account whose IMAP server advertises no SPECIAL-USE and names its Sent folder outside the four-name English fallback gets no recognized .sent Folder row, so a successful SMTP send… |

### PUSH (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-PUSH-001](Companion/Process/Current/KnownIssues/ios-push-001.md) | `open` | 🔓 OPEN (2026-08-09) — account removal and the latent factory-reset path treat remote push unsubscribe, consent revocation, and device unregister as best-effort with no durable retry; a stale push can still produce an old-account warning… |

### REMINDER (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-REMINDER-001](Companion/Process/Current/KnownIssues/ios-reminder-001.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-09) — one thrown GRDB reminder observation silently ends reminder refresh for that in-memory chat-pill session |

### SETTINGS (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-SETTINGS-002](Companion/Process/Current/KnownIssues/ios-settings-002.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-09) — account-field and folder-role edits can appear accepted in Settings even when their synchronous GRDB write failed |

### UI (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-UI-001](Companion/Process/Current/KnownIssues/ios-ui-001.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-09) — several optional/read-only surfaces collapse a transient load or re-authentication error to empty, stale, or indefinitely loading UI with only a diagnostic log |
<!-- KNOWN-ISSUES-AMENDMENT-BEGIN -->

## Post-freeze amendments

> **Current tracker census (2026-08-15): 13 `open` records.** The “Open 2” census above is the
> hash-preserved 2026-08-09 snapshot, not the live post-freeze count. GitHub migration and release
> triage must include the open rows in this amendment block plus the still-open base
> `IOS-PUSH-001`; the base `IOS-CLEANUP-001` row is superseded by the resolved override below.

Records added after the 2026-08-09 hierarchy freeze, through the amendment surface in
`Scripts/compact_known_issues.rb`. They have no row in the hash-pinned archive and are not
regenerated from it; their detail files live in
[`Companion/Process/Current/KnownIssues/Amendments/`](Companion/Process/Current/KnownIssues/Amendments/).

### Base-register current disposition overrides

| ID | Class | Current disposition |
|---|---|---|
| [IOS-CLEANUP-001](Companion/Process/Current/KnownIssues/ios-cleanup-001.md) | `resolved` | ✅ **RESOLVED (2026-08-14)** — destructive account removal, account reset, and local-index deletion now use the authoritative GRDB write as their side-effect boundary, surface precommit failure without deleting credentials/files/indexes, and report postcommit derived-index failure truthfully. The routed detail carries the exact implementation and failure-injection proof. GitHub issue [#6](https://github.com/TabMail/tabmail-ios/issues/6) closes with this fix. |

### BILLING (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-BILLING-002](Companion/Process/Current/KnownIssues/Amendments/ios-billing-002.md) | `open` | 🔓 OPEN (2026-08-12) — `cancelAccountDeletion` discards the response body, so the new four-valued `subscription_outcome` never reaches the UI and a user whose subscription lapsed irrecoverably during the grace window (`expired_during_grace`, unreactivatable in Stripe) sees the same silent success as one whose subscription was restored; `status` is `'restored'` in all four cases. Not yet reachable — the field is absent from the deployed worker. Old builds are safe: unknown keys are ignored and the body is discarded |

### IMAP (2)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-IMAP-015](Companion/Process/Current/KnownIssues/Amendments/ios-imap-015.md) | `open` | 🔓 OPEN (2026-08-12; re-audited 2026-08-15) — one SwiftMail pipelined-FETCH gap remains before dispatcher installation: an install failure strands the just-created part promises, so debug/test builds can abort mid-run while the run still reports "passed". The install error reaches the app before any caller awaits those futures; the connection path releases the production closed-channel case as unhealthy and the body queue retries, so there is no release hang or app-side fix. The six-line candidate belongs upstream at Cocoanetics; the fork remains deviation-free and upstream work still needs owner authorization |
| [IOS-IMAP-016](Companion/Process/Current/KnownIssues/Amendments/ios-imap-016.md) | `open` | 🔓 OPEN (2026-08-12) — SwiftMail's `rfc2047EncodedHeader()` guards on `!$0.isASCII` and **CR/LF ARE ASCII**, so `Email+Content.swift:84` emits a CRLF-bearing `Subject:` raw and the field ends mid-value; byte-for-byte the same inverted guard the app-side helper had, because `RFC2047.swift`'s doc comment says it is kept in step with it — the defect was copied with the helper (`MIS-019`×29). Current pin `a2d4a94` also has six raw filename and four raw content-type interpolations; TabMail reaches two of each, including the calendar-alternative content-type arm. **Mitigated app-side for Subject only** — `IMAPProvider.buildEmail` pre-encodes via `RFC2047.encodeIfNotEmittableLiterally`, which composes as a no-op with SwiftMail's own encoder — but filename/content-type residuals and every other library caller remain open. Remedy is UPSTREAM at Cocoanetics, not the fork |

### COMPOSE (2)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-COMPOSE-002](Companion/Process/Current/KnownIssues/Amendments/ios-compose-002.md) | `open` | 🔓 OPEN (2026-08-12) — Gmail interpolates regular `attachment.filename` into two quoted-string parameters unescaped and propagates provider-parsed `attachment.mimeType` without outbound validation. No hostile sender is required: Files import preserves APFS-legal `"`/`\`; outbox send and saved-draft upload both reach the builder. Arbitrary original Content-Type bytes were not proven to survive provider parsing, and a CRLF retained by the persisted-draft metadata splitter has no proven production writer, so MIME header injection remains a lead. Gmail's `alt.mimeType` arm is legacy/model-state capable, not current UI/forward reachable. SwiftMail carries six raw filename and four raw content-type interpolations; two of each are TabMail-reachable, including its calendar route. The proven filename effect is confined to one part's parameter list because controls are refused. DEFERRED as not-minimal; the related SwiftMail residual is tracked separately by the still-open `IOS-IMAP-016` record |
| [IOS-COMPOSE-003](Companion/Process/Current/KnownIssues/Amendments/ios-compose-003.md) | `open` | 🔓 OPEN (2026-08-12) — **pre-existing**, not introduced by `1d28552c1`: a subject that is printable ASCII and contains valid RFC 2047 encoded-word syntax **as literal text** (`Re: =?UTF-8?B?SGVsbG8=?= explained`) is passed through unchanged by `RFC2047.encodeHeaderValue` (trigger: non-ASCII or a control), by `RFC2047.encodeIfNotEmittableLiterally` (trigger: control only) **and** by SwiftMail's `rfc2047EncodedHeader()` (trigger: `!$0.isASCII`), so a receiving decoder — including our own `RFC5322Parse.decodeRFC2047` — interprets it and shows text the sender never wrote. **Display fidelity of one header; it cannot forge a field, add a recipient or change structure** — that needs a control character, closed on every path by `1d28552c1`. Not minimal: widening the trigger to the literal `=?` changes the wire bytes of every ordinary subject containing it, on every provider, and the same widening is needed inside SwiftMail (`IOS-IMAP-016`) |

### ASSET (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-ASSET-002](Companion/Process/Current/KnownIssues/Amendments/ios-asset-002.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-12) — P1d binds the `tabmail-asset://` scheme handler to the rendered body's `MessageBody.id`, and the ownership predicate reads the **mutable** `bodyAsset.headerId` column. `AccountManagerQueue` publishes the message-header re-key and the asset-manifest re-key as two steps, so in the window between them a view already rebound to the **destination** `ContentKey` meets rows still carrying the **source** key: every asset request resolves a real row, fails the comparison, and is refused `not-owned`. **Effect: for a moment after archiving or moving a message you had just read, its cached inline images render broken.** No wrong image is ever shown, nothing is deleted, and it self-corrects the instant the manifest re-key lands — or on one ordinary reopen — so it is registered under THE MANTRA rather than mechanised, consistent with ADR-IOS-076 decision 5. ⚠️ **Do NOT "fix" it by widening the predicate** — accepting the source key too re-opens the cross-message read C5 forbids, and re-deriving ownership from the URL's `headerHash` is worse than useless: `rekeyContentKey` preserves the row id and the bytes, so the baked URL keeps the OLD hash forever and a computed hash would reject **every** moved message's images. The correct narrowing is publisher-side ordering. Pinned in both directions by `BodyAssetOwnershipTests.movedMessageAssetsStayServableUnderTheDestinationKey` |

### ATTACH (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-ATTACH-001](Companion/Process/Current/KnownIssues/Amendments/ios-attach-001.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-12) — `ADR-IOS-077`'s predicate shipped with a MIGRATION GUARANTEE that is false: **no shipped build ever ran the reducer** (`v1.7.6`/`v1.7.7`/`v1.7.8` contain neither `safeAttachmentFileComponent` nor `isSafeFileComponent`, and write `\(index)_\(att.filename)` verbatim and unchecked), so legacy on-disk names are RAW sender-authored. The stranded set is `refused-by-predicate ∩ writable-by-v1.7.8` — measured as **three shapes**: any of the 79 refused control/bidi scalars (all 79 wrote, 0 filesystem refusals), 231–248 NFD UTF-16 units at a single-digit index (18 wide — the 230 budget reserves `String(Int.max).count`), and a canonical combining run of exactly 31 (1 wide — the budget is `32 − 2`). Unassigned scalars, runs ≥ 32 and separators CANNOT be on disk; `v1.7.8` could not write them. Such a draft still **loads and displays**, but every re-SAVE throws `AttachmentFilenameError.unsupported` (`saveDraftAndDismiss`, `send` COW staging, `autoSaveDraft`, `queueSend`→`persistQueuedSend`→`OutboxMessage.saveAttachments`) and compose refuses to dismiss. **Recovery is removing the attachment**; an outbox row queued BEFORE the upgrade is unaffected because the drain loads and never re-saves. Registered, not mechanised — security fixes are forward-only by owner verdict (2026-08-12): **do NOT add a migration, rename-on-load or grandfathering path** |

### PERF (5)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-PERF-008](Companion/Process/Current/KnownIssues/Amendments/ios-perf-008.md) | `not-defect` | ✅ NOT A DEFECT (2026-08-14) — the owner confirmed the elevated write windows coincided with initial sync, when mailbox download plus FTS and embedding derivation necessarily produce a large one-time write burst. The reports therefore do not establish a steady-state regression. Reopen only if elevated writes persist after sync, migrations, indexing, and embedding backfill settle |
| [IOS-PERF-009](Companion/Process/Current/KnownIssues/Amendments/ios-perf-009.md) | `open` | 🔓 **OPEN — MEASURED AND NARROWED (2026-08-15).** The multi-inbox query is reachable, but stale and fresh-statistics measurements at 20,000 unread stayed around **6 ms warm** on a 250,000-row, four-inbox synthetic fixture; the earlier ~5 ms observation stands. One inbox has no sorter and measured 0.110 ms. At four inboxes / 80,000 unread the unhinted statement reached roughly **48–58 ms median**, still off-main, foreground-only, best-effort, and self-healing. ⚠️ **The former remedy was wrong:** the temporary sort is bounded by LIMIT; the cost is fetching `date` from table rows after the planner chooses `messageHeader_folderId_isRead`. Forcing the existing `messageHeader_folderId_isRead_date` index keeps the sorter yet measured **58.344 → 0.356 ms** with the same 200 rows, so do not add a per-folder rewrite or a date tie-break. Ship no code from this evidence. Keep #12 open for an exact device measurement with at least two inbox folders and a realistically much larger unread population; cross the implementation gate only if the statement exceeds ~100 ms |
| [IOS-PERF-010](Companion/Process/Current/KnownIssues/Amendments/ios-perf-010.md) | `open` | 🔓 **OPEN — NARROWED (2026-08-14).** The two concrete `SearchView` stalls named by the record are fixed: the pre-debounce legacy substring pass now uses one raw async reader acquisition, and ranked FTS conversion resolves accounts plus exact header ids set-wise under one raw async acquisition, with rare stable-provider drift fallback grouped per account. Result timing/order, cancellation, remote-first identity ownership, scope, and FTS self-heals are covered without adding a per-keystroke NSE merge trigger. A one-reader contention test waits ~361 ms while the 20 ms MainActor heartbeat advances 17 times; the synchronous inverse waits ~364 ms with zero ticks. **Keep #13 open:** the broader by-state census is not certified; `SearchView.openResult` retains one synchronous indexed lookup on explicit tap, and debug-gated `UndoService.push` reads still need explicit classification in a complete inventory. |
| [IOS-PERF-011](Companion/Process/Current/KnownIssues/Amendments/ios-perf-011.md) | `resolved` | ✅ **RESOLVED (2026-08-14), with severity narrowed.** `SettingsView.loadOldMessageCount` now awaits the async read overload, so contention suspends `MainActor`; a one-reader regression test advances the heartbeat during a ~361 ms wait and its synchronous inverse starves it. **No user-visible stall was measured:** the covering-index count took roughly 2–3 ms on a synthetic 250k-row/100k-match database when uncontended. This is policy/correctness hygiene for a display-only value, not evidence of demonstrated field impact. No query, index, migration, threshold, or archive behavior changed. |
| [IOS-PERF-012](Companion/Process/Current/KnownIssues/Amendments/ios-perf-012.md) | `open` | 🔓 OPEN (2026-08-13) — with **stale `sqlite_stat1`**, `DurableIdentityLookup.find` step 3 (`accountId = ? AND rfc822MessageId = ?`) degrades from an index seek to an **account-wide scan**: **2.47–2.69 ms vs 0.003–0.099 ms** at 20k rows / 2 accounts, linear in per-account rows. **No index is missing** — `messageHeader_rfc822MessageId_date` exists and is simply not chosen; the planner takes the `accountId=?` prefix of `messageHeader_accountId_messageId` instead. **Stale is the SHIPPED regime**: `runRefreshPlannerStatisticsIfStale` latches on `schema_version` and its own comment excludes ordinary DML "however far the row counts move", so a fresh install carries statistics for an **empty** `messageHeader` (confirmed at `v84`: 4 rows, all partial indexes, all `0 0`). ⚠️ **Hit and miss cost the same when scanning**, and **the miss is the common case** — all five callers pass a *staged* row's identity, so steps 1/2 miss precisely for a freshly pushed message, i.e. the case the NSE exists to handle. No correctness impact; bounded by staged-set size; off-main on WAL reads. ⚠️⚠️ **DO NOT CONFIRM ON A FRESHLY-`ANALYZE`d DATABASE — the trap is SYMMETRIC and two agents hit it from opposite directions on 2026-08-12/13**: stale stats hid a real cost (this row), and fresh stats on a small fixture hid a real bug (`53d17514e`) and **would have produced a false negative on a red-first control**, certifying a broken system. **Any red-first proof whose predicate touches a query plan must state its stat regime.** Fix rejected as wrong-layer (a composite index is a migration compensating for a statistics problem); ADR-IOS-029's amendment already rejected re-arming on row growth (whole-DB `ANALYZE` up to **8.5 s at 500k**); **unevaluated option to cost first: re-arm ONCE at a row-count threshold.** ⚠️ Drafted as `IOS-PERF-009`, renumbered on placement; `IOS-PERF` namespace deliberate — `IOS-QUEUE` would file it by discoverer, and the queue is the *least* affected of its five callers. 🆕 **THIRD AXIS — WALK POSITION, independent of the stat regime (2026-08-13):** among values that all *exist*, **where the value sits in the walk** sets the cost — the same 200-member resolution measured **21 ms total (0.105 ms each) HEAD-drawn vs 12,229 ms TAIL-drawn**, a ~580× spread with no change to schema, statement or stat regime. A head-drawn probe makes a real defect look absent, and it cost a measurement round on `f60f41391`. **The tail is the operationally relevant end** (a bulk archive acts on recent mail, whose UIDs are highest), so any probe of this class must state which end its sample came from and should draw from the tail |
| [IOS-PERF-013](Companion/Process/Current/KnownIssues/Amendments/ios-perf-013.md) | `resolved` | ✅ **RESOLVED (2026-08-13, `f60f41391`)** — `AccountManager.executeSingleOp` step 1 and `retirePartiallyCompletedOp` resolved **each queued member with its own statement** (`WHERE (messageId = ? OR rfc822MessageId = ?) AND accountId = ?`). With **stale `sqlite_stat1` — the shipped regime** — SQLite cannot form a MULTI-INDEX OR and falls back to `SEARCH messageHeader USING INDEX messageHeader_accountId_messageId (accountId=?)`, i.e. **an account walk per member**: **12,229 ms for a 200-member archive vs <1 ms after** (260k rows, 189,800 in-account, tail-drawn). Fixed with two set-based `IN`-list arms chunked at `SyncConfig.sqlChunkSize`; **no new index, no migration**. `INDEXED BY messageHeader_rfc822MessageId` on the rfc822 arm is **load-bearing and fail-safe** — without it that arm walks in the stale regime (69 ms), and a migration dropping the index makes the statement **throw** rather than silently walk, which both callers already treat as "no identities collected" (same pattern as `ChatStore.findByStableIdSQL`). ⚠️ **Not a C3 exposure, and the sibling pick was ALREADY non-deterministic** — `messageId` is a per-folder UID that repeats across folders, so the old `fetchOne`-over-`OR` returned whichever row its plan reached first and **differed between stat regimes**; the pick is now deterministic (arm A over arm B, then `ORDER BY isInInbox DESC, id ASC`). See `IOS-PERF-012`'s walk-position axis before re-measuring |
| [IOS-PERF-016](Companion/Process/Current/KnownIssues/Amendments/ios-perf-016.md) | `resolved` | ✅ **RESOLVED (2026-08-13, `731856296`)** — `UndoService.push` performed **ungated, main-actor, N+1 database reads plus an unbounded `PendingOperation` fetch, in production, feeding nothing but `print()`**. It runs on **every archive, delete and move** — the most-travelled gesture path in the app. Three costs, none gated: one `dbPool.read` **per member** (200 for a bulk archive), an **unbounded `fetchAll`** over every `PendingOperation` row for the account, and **eager argument construction** (three `.map`s plus a large interpolated string) on the synchronous path before the `Task`. In a production build every one of those `print`s goes nowhere, so the work rendered output nobody can read. **Development rule 12 violation**; the file *looked* gated (11 `print(` sites against 5 `DebugModeManager.isLoggingEnabled` references), which is why it survived. Fixed by gating the **whole `Task` body**, not the individual emissions — the distinction `AccountManagerQueue.logStuckOpDiagnostic`'s comment exists to record. Empirically confirmed: the pre-fix suite emitted 48 `[UndoStack] PUSH` lines, 56 per-member DB-state dumps and 48 unbounded `PendingOps` dumps; after, **all three are zero**. ⚠️ **NOT the index-defeating sorter class** despite arriving from the same audit — no `ORDER BY`, nothing defeats a `LIMIT`, the per-message query is index-assisted. ⚠️ **Residual, deliberately out of scope:** `UndoService.dismissAll` still emits an ungated `[UndoStack] DISMISS ALL` (84 emissions in one suite) — bare print, no eager args, **no DB work**, so rule-12 hygiene rather than this performance class; and `SearchIndex.swift` carries ~35–38 further ungated `print(` sites, swept separately. **When sweeping, enumerate by STATE — every site whose *work* is ungated — not by grepping `print(`, which finds emissions and misses the reads that feed them** |

### PRIVACY (3)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-PRIVACY-001](Companion/Process/Current/KnownIssues/Amendments/ios-privacy-001.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-12) — **T5 is OPEN by explicit owner decision, taken AGAINST the implementing side's recommendation.** `HTMLWebView.makeUIView` does not set `config.websiteDataStore`, so the message render view uses `WKWebsiteDataStore.default()`: **ONE process-wide PERSISTENT cookie/localStorage/cache jar shared by every message and every sender, surviving app launches**. No sender script is needed — a remote **subresource** suffices — so `allowsContentJavaScript = false` and the CSP do not touch it. P1b closed T5 with a per-view `.nonPersistent()` store; the owner reversed it under *"no behaviour changes, just security"* (`v1.7.8` shipped the symbol absent). **Do NOT describe the render path as "isolated" or "per-message"**, and do not re-add a store — neither per-view ephemeral nor a shared ephemeral singleton — without asking the owner. P1d's `ContentKey` view identity no longer narrows any data boundary, because there is none. Pinned POSITIVELY in two directions by `EmailRenderSecurityCanaryTests.productionConfiguration`: `isPersistent == true` on both render paths **and** a store-**identity** assertion (`===`), because `isPersistent` alone cannot distinguish a shared-singleton refactor |
| [IOS-PRIVACY-002](Companion/Process/Current/KnownIssues/Amendments/ios-privacy-002.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-12) — **the font leg of T9 is OPEN by explicit owner decision.** P1b shipped `font-src 'none'`, closing the `@font-face` + `unicode-range` conditional-request channel; a runtime smoke test measured an HTTPS web font being refused and fallback typography rendering instead. The owner relaxed it to `font-src https:` under *"no behaviour changes, just security"*, judging the anti-tracking rationale inconsistent with the open `img-src https:` path, which already permits image subresource correlation. **Do NOT restore `'none'`, and do NOT widen beyond HTTPS.** `media-src 'none'` remains retained. Pinned two-sided by `EmailRenderSecurityCanaryTests.cspDirectiveCensus` |
| [IOS-PRIVACY-003](Companion/Process/Current/KnownIssues/Amendments/ios-privacy-003.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-12) — `deferredImageLoadJS` withholds URLs inside app-owned hidden `.eml` sections in both main and preview modes. It deliberately does not use general visibility predicates because quoted text, invite details, and media-query content can become visible without a reload. The check fails open and does not make rendering tracking-free: `img-src https:` remains open by owner decision, visible image subresource requests still leave the device, and CSS background URLs are untouched. Two-sided render-pipeline tests pin both withholding and reveal behaviour. |

### UI (5)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-UI-002](Companion/Process/Current/KnownIssues/Amendments/ios-ui-002.md) | `accepted` | 📋 ACCEPTED LIMITATION — **RE-SCOPED 2026-08-12; the original registration is SUPERSEDED IN PLACE, not deleted.** The UX limitation this row first recorded (P1b's `dataDetectorTypes = []`, costing tap-to-call and tappable bare URLs) **was never owner-accepted** — the owner rejected it on sight and directed the setting back to `[.link, .phoneNumber]`, overruling the narrower `[.phoneNumber]`-only fallback this row used to recommend. **That fallback advice and the "do NOT restore `[.link, .phoneNumber]`" instruction are WITHDRAWN.** Detectors are ON again and the UX limitation no longer exists. **What survives, and is the accepted limitation now, is the SECURITY exception:** §9.1 B2 (verified) — Data Detectors sit OUTSIDE the navigation delegate, so WebKit can present detector UI before `changeLocation` and a detected target may never reach `decidePolicyFor`; therefore *"every externally dispatched target passes our `http`/`https` allowlist"* is **FALSE while detectors are enabled**, and it is false in a way **no delegate-level test can observe** (`MIS-019` shape — never restate that absolute without this exception). Scope, so it is not overstated: authored `<a href>` links are UNAFFECTED by `dataDetectorTypes` — they are `WKNavigationAction`s reaching `decidePolicyFor`, where `mailto:` interception and P1c's allowlist live; detectors govern only PLAIN-TEXT numbers and BARE URLs. Pinned by `EmailRenderSecurityCanaryTests.productionConfiguration`, which now asserts `[.link, .phoneNumber]` positively so a silent change in either direction fails. **⚠️ AMENDED 2026-08-12 — no longer the ONLY exception: `IOS-UI-003` (long-press link preview) is a second, independent non-delegate route, so every restatement of the absolute must carry BOTH** |
| [IOS-UI-003](Companion/Process/Current/KnownIssues/Amendments/ios-ui-003.md) | `accepted` | 📋 ACCEPTED LIMITATION (2026-08-12) — `HTMLWebView.makeUIView` leaves `allowsLinkPreview` **unset**, so WebKit's default (ON) applies, restored by owner directive after P1b set it to `false` (*"no behaviour changes, just security"*; `v1.7.8` shipped the symbol absent). Long-press **fetches and presents** the remote URL with **no `WKNavigationAction`**, so it never reaches `decidePolicyFor` and never passes `RenderLinkPolicy`'s `http`/`https` allowlist. *"Every externally dispatched target passes our allowlist"* is therefore FALSE for a **SECOND, independent** reason alongside `IOS-UI-002`'s data detectors, and — like the first — false in a way **no delegate-level test can observe** (`MIS-019` shape; qualify every restatement with BOTH). Scope: authored `<a href>` activation is UNAFFECTED and still passes the allowlist; only the preview side channel is outside it. Compounds `IOS-PRIVACY-001` — the preview fetch runs against the shared persistent store. Pinned POSITIVELY by `EmailRenderSecurityCanaryTests.productionConfiguration` (`allowsLinkPreview == true`), so a silent change in either direction fails |
| [IOS-UI-004](Companion/Process/Current/KnownIssues/Amendments/ios-ui-004.md) | `resolved` | ✅ **RESOLVED AS A UI LIMITATION (2026-08-13)** — the non-settling withheld-image condition still exists in P4's diagnostic census, but `imageLoadFailure` has no user-visible sink, so the condition cannot suppress or delay message chrome. Routine 404/expired/authenticated/DNS/image-byte/ATS failures are silent. `WKWebView` exposes no exact `<img>` ATS error or supported per-resource timeout; no security-specific replacement banner, timing heuristic, retry, or duplicate probe is shipped. |
| [IOS-UI-005](Companion/Process/Current/KnownIssues/Amendments/ios-ui-005.md) | `open` | 🔓 OPEN — NARROWED FIX CANDIDATE (2026-08-15) — **the `tm-*` class namespace is NOT reserved against sender-authored markup.** Mechanism **CONFIRMED on all three providers**; impact deliberately assessed as **NEAR-NIL** and registered rather than fixed. ⚠️ **This row exists as much to stop the finding being RE-ESCALATED as to track it** — it was first framed as "attacker-chosen From/Subject/Date in native SwiftUI chrome", which sounds severe and is misleading. `EmailHTMLWrapper.wrapHTML` never strips, rewrites or namespaces author `class` (its only touch is `unwrapFullHTMLDocument` **prepending** `tm-email-body `), and markers are **strictly appended after the sender body** on IMAP (`IMAPFetchMapping.renderBodyWithEmbeddedHeaders`), Gmail (`extractBodyAndEmlMarkers`, `(topLevel ?? "") + tail`) and Exchange (`fetchMessage`, `htmlBody = (htmlBody ?? "") + markerHtml`), with **no marker-provenance boundary between generation and storage**. `EmailFilter.parseEmlSectionMetadata` **is first-match-wins** (no position/provenance/nesting check) and `data-filename` is **fully sender-controlled** (raw MIME `filename` / Gmail `part.filename` / Graph `att.name`), so a forged section wins. 🚨 **Why severity is near-nil — read before re-escalating: the genuine marker's envelope is ITSELF sender-authored bytes** (`rfc822.embeddedMessageInfo` / `envelopeFromHeaders` / Graph's parse), and **nothing authenticates a nested envelope anywhere**. A sender wanting the sheet to read `From: bank@example.com` just **attaches an `.eml` with that `From:` line** — no forgery needed. The forgery buys only a disagreement between the displayed envelope and the attachment's bytes, **both attacker-controlled**. Comparable to or lower than the already-possible census suppression via a hanging image URL. ⚠️ **One leg REFUTED outright:** `metadata.partSection` is **never read** (the nested-attachment strip comes from `AttachmentInfo.parentEmlSection == attachment.section`), so **no fetch, identity resolution or security decision hangs off a marker**; `EmlSectionMetadata`'s only consumer is `EmlAttachmentPreview.metadata`'s header view. ⚠️ **Falsifies `IOS-UI-004`'s "not reachable by a sender's choice" claim** for the `tm-eml-section` route, which gating the `tm-eml-headers` arm does **not** close. Side effect, same low severity: `EmailFilter.stripEmbeddedEmlSections` also drops a forged section, so what the user **quotes** can differ from what they **saw**. **The adjacent sender-writeable body-class gate is fixed:** deferred image policy now takes app-owned preview mode from Swift instead of `body.tm-preview-mode`. **A real future marker fix must not sanitize author classes or trust a nonce stored in the same HTML**; it would carry envelope/section provenance out-of-band in an app-owned typed record. Not statically determinable: whether Gmail/Graph pre-sanitize `class` server-side; **no test covers a sender-forged marker** |
| [IOS-UI-006](Companion/Process/Current/KnownIssues/Amendments/ios-ui-006.md) | `resolved` | ✅ **RESOLVED (2026-08-13)** — the generic bridge census is diagnostic-only and `AutoSizingHTMLView` no longer owns an image-failure banner binding, eliminating the registered outgoing-coordinator UI sink. `ImageLoadFailureReportDisposition` has only malformed and diagnostic-only outcomes; no unverified `dismantleUIView` ordering assumption remains. |

### CAL (2)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-CAL-011](Companion/Process/Current/KnownIssues/Amendments/ios-cal-011.md) | `accepted` | 📋 ACCEPTED PLATFORM LIMITATION (updated 2026-08-16) — iOS's system importer implements only part of iTIP. A real simulator matrix proved new `REQUEST` addition and matching-`CANCEL` reconciliation, but a same-UID higher-sequence `REQUEST` offered Update and then left the original unchanged; `REPLY`, `REFRESH`, and `COUNTER` no-op; `DECLINECOUNTER` is misread as a new event. The owner accepted the matrix as conclusive without a first-party Mail parity gate. |
| [IOS-CAL-010](Companion/Process/Current/KnownIssues/Amendments/ios-cal-010.md) | `open` | 🛠️ FIX CANDIDATE (2026-08-16) — the bounded gate refuses `REPLY`, `REFRESH`, `COUNTER`, and `DECLINECOUNTER` before system import and explains the refusal visibly, while retaining `CANCEL`. Conventional RED/GREEN passed 5/5 with 100% policy coverage; the real iOS 26.5 matrix independently proved the first three no-op and `DECLINECOUNTER` creates the wrong standalone event, while matching `CANCEL` is actionable. The refreshed draft remains open pending fresh-context exact-diff review and final owner approval; do not auto-merge this behavioral change. |

### AI (2)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-AI-004](Companion/Process/Current/KnownIssues/Amendments/ios-ai-004.md) | `open` | 🔓 OPEN (2026-08-13) **NARROWED — the trigger now EXISTS; do not restate the old "no trigger anywhere in the tree" claim.** `1eb41702e` added the ADR-IOS-008 decision-3 event that iOS lacked (TB's `onMoved.js` parity): `recordMembersThatEnteredInbox` captures drained move members landing `isInInbox` in the destination, keyed through `MessageHeaderRekey.currentHeaderId(afterHandoffFrom:)`, and `enqueueAIForMembersThatEnteredInbox` enqueues **after** the post-drain sync, where the durable id and the FTS key agree by construction. Every prescription this row made was honoured — POST-re-key id, **not** `publishMoveFinish`, post-drain destination-folder loop. ⚠️ **The surviving residual is RFC-less rows:** `resolveInboxEntryAITargets` **requires** `rfc822MessageId` (the only key surviving both the `COPYUID` re-key and the sync UID remap) and refuses without it, fail-closed with an observable log — correct, since resolving a stale source UID against the destination folder **did** hit an unrelated message, pinned by `MoveIntoInboxAIEnqueueTests.aiTargetIsNeverAUidCollisionVictim`. So the non-recovering case now needs THREE conjuncts: moved in from the list's move sheet, **never opened**, outside `repopulationCandidates`' `ORDER BY date DESC LIMIT SyncConfig.maxRecentEmails`, **and** no rfc822 Message-ID — the same population `IOS-AI-003` found expensive for the same reason. Not in the blocking set: AI output is derived, recomputable content, not a queued intention. Moves that never reach the drain (server-side move seen by sync, NSE direct delivery) are covered by their own enqueue sites, not this trigger. ⚠️ **No device pass has confirmed the user-visible outcome** — the owner re-test is now a confirmation test |
| [IOS-AI-005](Companion/Process/Current/KnownIssues/Amendments/ios-ai-005.md) | `open` | 🔓 OPEN (2026-08-12) — an AI result that **was** computed can be **silently discarded** before it reaches the row. `InboxViewModel.flushAIBatch` resolves each pending id twice and drops on either miss with **no log and no re-arm**; `pendingAIBatch` is re-keyed by `applyHeaderRekeys`, but that is driven by `.messageHeadersRekeyed`, which is posted from **exactly ONE place** — `AccountManagerQueue`'s `publishMoveFinish`, the `COPYUID` drain path. **The sync-path UID remap posts nothing**, so an identity reconciled by the sync healer rather than the drain leaves a stale id in the batch and the entry vanishes untraceably. Both 2026-08-12 device episodes took the sync path (one after a move attempt timed out), so the path is common, not exotic. ⚠️ **Mechanism established from code; OCCURRENCE NOT established** — the AI write and the remap fall inside one ~1.3 s interval with a 300 ms throttle between, and the log cannot resolve the ordering. Do NOT upgrade to "confirmed in the field" without instrumentation that does not yet exist. Registered because `reloadMessages` re-composes the row and AI output is recomputable. Also records that `MessageRowView`'s spinner is DERIVED (`isInInbox && anyAISourceEnabled`, no tag yet) with **no connection to any queue** — never read it as queue state |

### SEARCH (1)

| ID | Class | Executive statement |
|---|---|---|
| [IOS-SEARCH-004](Companion/Process/Current/KnownIssues/Amendments/ios-search-004.md) | `not-defect` | ✅ NOT A DEFECT (2026-08-15) — the filed multi-folder date-range query is not production- or test-reachable: `EmailSearchTool`, the sole `SearchIndex.search` caller, never supplies `folderIds`, and `SearchView` never reaches `scanByDateRange`. The actually reachable unscoped `"*"` + date arm remains an off-main 22 ms residual; adding index/build/write cost for that different arm is not proportionate without user-visible evidence. Reopen if a production `folderIds` caller, interactive date-filter UI, or measured agent delay makes either query live |
| [IOS-SEARCH-005](Companion/Process/Current/KnownIssues/Amendments/ios-search-005.md) | `resolved` | ✅ **RESOLVED (2026-08-13, `374a8b3c1`)** — `SearchIndex.searchFTSOnly` generated an FTS5 `snippet()` for **every row every arm matched**, then discarded all but the rows surviving the final `LIMIT`. The 6-arm `UNION ALL` carries **no per-arm `ORDER BY`/`LIMIT`** — ordering and limiting happen once over the union — so `snippet()`, which reassembles and tokenises the matched column, ran across the entire match set while only `SearchConfig.searchDefaultLimit` rows were displayed. Split into two phases (select `fts.rowid` + `bm25`, order and limit, then snippet only the survivors): **1,145 ms → ~597 ms per keystroke, −48%**. ⚠️ **NOT the index-defeating sorter class** despite the shared audit: that class's remedy is a per-partition rewrite, which works only where an ordering index exists, and **FTS5 `MATCH` output has no date order** — measured per-folder 187 ms vs IN-list 191 ms, so the rewrite buys nothing here. The cost was the **auxiliary function**, not the plan. 🚨 **A defect in the FIRST version was caught pre-commit and its lesson is the reusable part:** it threw `SearchIndexError.snippetMissing` for the "impossible" surviving-row-without-snippet case, but the only per-keystroke consumer is `SearchView`'s `(try? await …keywordSearch(…)) ?? []` — so the throw would have **silently discarded the entire ranked result set**, turning a cosmetic single-row defect into a whole-result-set one. Rule-4 pincer: the argument for the throw was that both phases share one `dbPool.read` snapshot, making the case impossible ⇒ the throw is either dead code or actively harmful, and **neither branch justifies it**. **Before adding a `throw` to a fail-closed guard, grep the call chain for `try?` and `?? []`** — fail-closed is only fail-*safe* if someone is listening. ⚠️ **Same shape survives in `SearchIndex.searchFTSCandidates`** (hybrid/agent-chat path via `EmailSearchTool`, not per-keystroke) — left untouched by scope; start there when sweeping |

## GitHub live trackers (2026-08-14)

Only records classified `open` were migrated. Accepted limitations, resolved records, and historical
audit/provenance files remain documentation-only. The future app-owned ATS image loader is
[issue #1](https://github.com/TabMail/tabmail-ios/issues/1). Open register mappings:

- `IOS-AI-004` [#2](https://github.com/TabMail/tabmail-ios/issues/2); `IOS-AI-005` [#3](https://github.com/TabMail/tabmail-ios/issues/3); `IOS-BILLING-002` [#4](https://github.com/TabMail/tabmail-ios/issues/4); `IOS-CAL-010` [#5](https://github.com/TabMail/tabmail-ios/issues/5)
- `IOS-COMPOSE-002` [#7](https://github.com/TabMail/tabmail-ios/issues/7); `IOS-COMPOSE-003` [#8](https://github.com/TabMail/tabmail-ios/issues/8); `IOS-IMAP-015` [#9](https://github.com/TabMail/tabmail-ios/issues/9); `IOS-IMAP-016` [#10](https://github.com/TabMail/tabmail-ios/issues/10)
- `IOS-PERF-009` [#12](https://github.com/TabMail/tabmail-ios/issues/12); `IOS-PERF-010` [#13](https://github.com/TabMail/tabmail-ios/issues/13); `IOS-PERF-012` [#15](https://github.com/TabMail/tabmail-ios/issues/15)
- `IOS-PUSH-001` [#16](https://github.com/TabMail/tabmail-ios/issues/16); `IOS-UI-005` [#20](https://github.com/TabMail/tabmail-ios/issues/20)
- Retired mapping: `IOS-SEARCH-004` [#18](https://github.com/TabMail/tabmail-ios/issues/18) — reclassified `not-defect` on 2026-08-15 after the exact caller census proved the filed multi-folder statement is never constructed; close on GitHub when this correction is accepted
- Retired mapping: `IOS-QUEUE-010` [#17](https://github.com/TabMail/tabmail-ios/issues/17) — reclassified `not-defect` on 2026-08-14 after the actor-isolation re-audit; close on GitHub when this correction is accepted
- Retired mapping: `IOS-TEST-001` [#19](https://github.com/TabMail/tabmail-ios/issues/19) — repaired in tests on 2026-08-14; close on GitHub when the PR is accepted
<!-- KNOWN-ISSUES-AMENDMENT-END -->
